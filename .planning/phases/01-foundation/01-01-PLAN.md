---
phase: "01-foundation"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - postcss.config.mjs
  - tsconfig.json
  - .prettierrc
  - eslint.config.mjs
  - src/app/globals.css
  - src/styles/tokens.css
  - src/app/layout.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - INFRA-06

must_haves:
  truths:
    - "`next build` completes without errors and produces an `out/` directory"
    - "`next.config.ts` has `output: 'export'` and `images: { unoptimized: true }` set"
    - "Design tokens (4 color tokens + 4 type scale tokens) exist in `src/styles/tokens.css` as CSS custom properties inside `@theme`"
    - "`app/layout.tsx` renders a valid HTML shell with `next/font/google` font loading (Sora + Instrument Serif) and a metadata export in place"
    - "Fonts are self-hosted at build time — no runtime Google Fonts requests in `out/`"
    - "D-01: Font pairing is Sora (body) + Instrument Serif (headings) — both imported from next/font/google"
    - "D-02: Both fonts load via next/font/google — zero runtime Google Fonts requests, self-hosted at build time"
    - "D-03: Sora loads weights 400, 500, 600, 700; Instrument Serif loads weight 400 only (higher weights unavailable)"
    - "D-04: Sora is assigned to --font-body (body/UI); Instrument Serif is assigned to --font-heading (headings only)"
    - "D-05: Type scale tokens in tokens.css all use clamp() for fluid responsive scaling"
    - "D-06: Color direction is warm ink / parchment — off-white surface (~oklch 97%), near-black text, warm amber/terracotta accent"
    - "D-07: Accent chroma is in the 0.18–0.22 range — oklch(62% 0.19 55)"
    - "D-08: All color tokens use oklch() format throughout"
    - "D-09: Exactly 4 color tokens: --color-surface, --color-text, --color-accent, --color-muted"
    - "D-10: Phase 1 tokens are colors + type scale only — spacing, radius, animation tokens deferred to Phase 2"
    - "D-11: When Phase 2 adds spacing tokens they go in the same tokens.css file"
    - "D-12: Tokens declared inside Tailwind v4 @theme directive — available as both CSS custom properties and Tailwind utilities"
    - "D-13: Project bootstrapped via create-next-app then boilerplate cleaned up"
    - "D-14: app/page.tsx contains empty shell with 4 commented section stubs: Hero, CV, Projects, Contact"
    - "D-15: File structure uses src/ directory — src/app/, src/components/, src/hooks/, src/lib/, src/styles/"
    - "D-16: Full linting/formatting setup in Phase 1 — ESLint (next/core-web-vitals) via eslint.config.mjs + Prettier with .prettierrc"
  artifacts:
    - path: "next.config.ts"
      provides: "Static export config"
      contains: "output: 'export'"
    - path: "src/styles/tokens.css"
      provides: "Design tokens"
      contains: "--color-surface"
    - path: "src/app/layout.tsx"
      provides: "HTML shell + font loading"
      exports: ["metadata", "RootLayout"]
    - path: "src/app/page.tsx"
      provides: "Page with section stubs"
      contains: "Hero"
    - path: ".prettierrc"
      provides: "Formatter config"
  key_links:
    - from: "src/styles/tokens.css"
      to: "src/app/globals.css"
      via: "@import"
      pattern: "@import.*tokens\\.css"
    - from: "src/app/globals.css"
      to: "Tailwind v4"
      via: "@import 'tailwindcss'"
      pattern: "@import 'tailwindcss'"
    - from: "src/app/layout.tsx"
      to: "next/font/google"
      via: "Sora + InstrumentSerif import"
      pattern: "next/font/google"
---

<objective>
Bootstrap the personal website Walking Skeleton: scaffold the project, lock the static export configuration, establish design tokens, load fonts, and produce a valid HTML shell — proving the full `next build` → `out/` pipeline works end-to-end.

Purpose: Every subsequent phase builds on these choices. The static export config, token system, and font loading are locked here and must not be revisited in later phases.

Output: A working Next.js 16 project that passes `next build` and produces `out/`, with design tokens and font loading ready for Phase 2 content work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/axel/code/website/.planning/ROADMAP.md
@/Users/axel/code/website/.planning/phases/01-foundation/01-CONTEXT.md
@/Users/axel/code/website/.planning/phases/01-foundation/01-UI-SPEC.md
@/Users/axel/code/website/.planning/phases/01-foundation/01-SKELETON.md
</context>

## Phase Goal

**As a** developer, **I want to** run `next build` and get a deployable `out/` directory with correct static export config, design tokens, and font loading, **so that** every subsequent phase can build visibly complete UI on a proven, correctly-configured foundation.

<tasks>

<task type="auto">
  <name>Task 1: Scaffold project and lock static export config</name>
  <files>
    next.config.ts,
    postcss.config.mjs,
    tsconfig.json,
    .prettierrc,
    package.json
  </files>
  <read_first>
    - /Users/axel/code/website/.planning/phases/01-foundation/01-CONTEXT.md (decisions D-13, D-15, D-16)
    - /Users/axel/code/website/CLAUDE.md (technology stack table, constraints section)
  </read_first>
  <action>
    Run `npx create-next-app@latest . --typescript --app --src-dir --no-tailwind --import-alias "@/*"` in `/Users/axel/code/website`. Answer no to ESLint prompt (we configure it manually). After scaffolding:

    1. Install dependencies: `npm install tailwindcss@4.3.0 @tailwindcss/postcss clsx@2.1.1 tailwind-merge@3.6.0 prettier eslint-config-next`

    2. In `next.config.ts`, add `output: 'export'` and `images: { unoptimized: true }` to the NextConfig object. No other changes.

    3. Replace `postcss.config.mjs` with the Tailwind v4 PostCSS plugin: single plugin key `@tailwindcss/postcss` with empty object value. Remove any existing tailwindcss/autoprefixer entries.

    4. In `tsconfig.json`, verify `strict: true` is set in `compilerOptions`. Set `paths` to `{ "@/*": ["./src/*"] }`. These are created by `create-next-app` but confirm they are present.

    5. Create `.prettierrc` with: `semi: false`, `singleQuote: true`, `trailingComma: 'all'`, `printWidth: 100`, `tabWidth: 2`.

    6. Create `eslint.config.mjs` with the `next/core-web-vitals` ruleset. Use the flat config format: import `{ dirname } from 'path'`, `{ fileURLToPath } from 'url'`, `{ FlatCompat } from '@eslint/eslintrc'`, create a `FlatCompat` instance with `baseDirectory: __dirname`, and export `compat.extends('next/core-web-vitals')`.

    7. In `package.json`, add scripts: `"format": "prettier --write ."`, `"format:check": "prettier --check ."`, `"lint": "next lint"`.

    After setup, delete the `src/app/globals.css` content (replace with single `@import 'tailwindcss';` line as a placeholder — Task 2 rewrites it fully). Delete `public/vercel.svg` and `public/next.svg`. Remove all Vercel/Next.js boilerplate from `src/app/page.tsx` (replace with a single `export default function Home() { return <main /> }` placeholder — Task 3 rewrites it).
  </action>
  <verify>
    <automated>cd /Users/axel/code/website && npx tsc --noEmit --pretty false 2>&1 | head -20 && cat next.config.ts | grep -c "output: 'export'"</automated>
  </verify>
  <acceptance_criteria>
    - `next.config.ts` contains `output: 'export'` and `images: { unoptimized: true }` (grep confirms both strings present)
    - `postcss.config.mjs` contains `@tailwindcss/postcss` as the only plugin entry
    - `tsconfig.json` has `"strict": true` and `"paths": { "@/*": ["./src/*"] }`
    - `.prettierrc` exists at repo root
    - `package.json` contains `"format"`, `"format:check"`, and `"lint"` script entries
    - `eslint.config.mjs` exists at repo root and extends `next/core-web-vitals`
    - `npx tsc --noEmit` exits 0 (no type errors)
    - Vercel SVG assets removed from `public/`
  </acceptance_criteria>
  <done>Project is scaffolded, static export locked, TypeScript compiles clean, Prettier config in place.</done>
</task>

<task type="auto">
  <name>Task 2: Design tokens and globals.css</name>
  <files>
    src/styles/tokens.css,
    src/app/globals.css
  </files>
  <read_first>
    - /Users/axel/code/website/.planning/phases/01-foundation/01-UI-SPEC.md (Color table, Typography table, @theme block)
    - /Users/axel/code/website/.planning/phases/01-foundation/01-CONTEXT.md (decisions D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12)
    - /Users/axel/.claude/rules/web/coding-style.md (CSS custom properties section)
  </read_first>
  <action>
    Create `src/styles/tokens.css`. This file declares all Phase 1 design tokens inside a `@theme` block (Tailwind v4 `@theme` directive) so they are available both as `var(--*)` CSS custom properties AND as Tailwind utility classes.

    Color tokens (all oklch, per D-08 and UI-SPEC Color table):
    - `--color-surface`: `oklch(97% 0.01 75)`
    - `--color-text`: `oklch(18% 0.01 75)`
    - `--color-accent`: `oklch(62% 0.19 55)`
    - `--color-muted`: `oklch(55% 0.03 75)`

    Type scale tokens (all clamp(), per D-05 and UI-SPEC Typography table):
    - `--text-ui`: `clamp(0.875rem, 0.82rem + 0.28vw, 1rem)`
    - `--text-body`: `clamp(1rem, 0.92rem + 0.4vw, 1.125rem)`
    - `--text-heading`: `clamp(1.75rem, 1.4rem + 1.75vw, 2.5rem)`
    - `--text-hero`: `clamp(3rem, 1.5rem + 7.5vw, 7rem)`

    Font family CSS variables (values will be injected by `next/font/google` in layout.tsx — declare the variable names here as stubs with a fallback so the token file is self-consistent):
    - `--font-body`: `Sora, system-ui, sans-serif`
    - `--font-heading`: `'Instrument Serif', Georgia, serif`

    These font variable stubs are overridden at runtime by the `next/font` CSS variable injection. Declaring them here makes the token file authoritative for naming convention.

    Spacing, radius, and animation tokens are deferred to Phase 2 per D-10. Do NOT add them here.

    Then rewrite `src/app/globals.css` with three sections in order:
    1. `@import 'tailwindcss';` — Tailwind v4 base import
    2. `@import '../styles/tokens.css';` — pull in the token file
    3. A `body` rule that sets `background-color: var(--color-surface)`, `color: var(--color-text)`, `font-family: var(--font-body)`, and `line-height: 1.6`
  </action>
  <verify>
    <automated>cd /Users/axel/code/website && grep -c "\-\-color-surface" src/styles/tokens.css && grep -c "\-\-text-hero" src/styles/tokens.css && grep -c "@import.*tokens" src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `src/styles/tokens.css` exists and contains all 4 color tokens (`--color-surface`, `--color-text`, `--color-accent`, `--color-muted`) inside an `@theme` block
    - `src/styles/tokens.css` contains all 4 type scale tokens (`--text-ui`, `--text-body`, `--text-heading`, `--text-hero`) with `clamp()` values inside the same `@theme` block
    - `src/styles/tokens.css` contains `--font-body` and `--font-heading` variable declarations
    - `src/app/globals.css` begins with `@import 'tailwindcss'`
    - `src/app/globals.css` contains `@import '../styles/tokens.css'`
    - `src/app/globals.css` contains a `body` rule referencing `var(--color-surface)` and `var(--font-body)`
    - All color values use oklch format
    - No spacing, radius, or animation tokens present in `tokens.css` (deferred to Phase 2)
  </acceptance_criteria>
  <done>Design tokens locked in `tokens.css`, imported into `globals.css`, Tailwind v4 wired via `@import 'tailwindcss'`.</done>
</task>

<task type="auto">
  <name>Task 3: Font loading, HTML shell, and `next build` verification</name>
  <files>
    src/app/layout.tsx,
    src/app/page.tsx
  </files>
  <read_first>
    - /Users/axel/code/website/.planning/phases/01-foundation/01-UI-SPEC.md (Typography section, Copywriting Contract table)
    - /Users/axel/code/website/.planning/phases/01-foundation/01-CONTEXT.md (decisions D-01, D-02, D-03, D-04, D-14)
    - /Users/axel/code/website/CLAUDE.md (Font Loading section, `next/font/google` notes)
  </read_first>
  <action>
    Rewrite `src/app/layout.tsx` as the root layout with font loading, metadata, and HTML shell.

    Font loading (per D-01, D-02, D-03, D-04):
    - Import `Sora` and `Instrument_Serif` from `next/font/google`
    - `Sora` config: `subsets: ['latin']`, `weight: ['400', '500', '600', '700']`, `variable: '--font-body'`, `display: 'swap'`
    - `Instrument_Serif` config: `subsets: ['latin']`, `weight: ['400']`, `variable: '--font-heading'`, `display: 'swap'` — note: Instrument Serif only has 400 weight available in Google Fonts; do not include 500/600/700 for this family as they do not exist and will cause a build error
    - Both font instances expose CSS variables. Apply both variables on the `<html>` element via `className`: `${sora.variable} ${instrumentSerif.variable}`

    Metadata export:
    - `title`: `'Axel W — Software Engineer'` (from UI-SPEC Copywriting Contract)
    - `description`: `'Software engineer — portfolio and CV'` (placeholder; will be replaced in Phase 2)

    HTML shell structure:
    - `<html lang="en">` with both font variable classes
    - `<body>` with `className="antialiased"` and `{children}` inside
    - Import `./globals.css`

    Then rewrite `src/app/page.tsx` with the section stubs per D-14:
    - `export default function Home()` returning a `<main>` element
    - Four JSX comment stubs inside `<main>`: `{/* Hero */}`, `{/* CV */}`, `{/* Projects */}`, `{/* Contact */}`
    - No visible text content — Phase 2 fills these in

    After writing both files, run `next build` to verify the full pipeline. The build must exit 0 and produce `out/index.html`.
  </action>
  <verify>
    <automated>cd /Users/axel/code/website && npm run build 2>&1 | tail -20 && ls out/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/layout.tsx` imports `Sora` and `Instrument_Serif` from `next/font/google`
    - Both font instances have `variable` set (`--font-body` and `--font-heading` respectively) and `display: 'swap'`
    - `Instrument_Serif` uses only `weight: ['400']` — no 500/600/700 (those weights do not exist for this family)
    - `src/app/layout.tsx` exports a `metadata` object with `title: 'Axel W — Software Engineer'`
    - `<html>` element has `lang="en"` and both font variable CSS class names applied
    - `src/app/page.tsx` contains the four section stub comments (`Hero`, `CV`, `Projects`, `Contact`) inside a `<main>` element
    - `npm run build` exits 0 with no errors
    - `out/` directory exists after build
    - `out/index.html` exists and contains `Axel W` in the title tag
    - `npx tsc --noEmit` exits 0 after all files are written
  </acceptance_criteria>
  <done>`next build` succeeds, `out/` directory produced, fonts self-hosted at build time, HTML shell valid — Walking Skeleton proven end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local install | Third-party packages installed into node_modules during scaffold |
| `create-next-app` CLI → project files | Scaffolded boilerplate copied to working directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm install (tailwindcss, clsx, tailwind-merge, prettier) | mitigate | Packages are well-known, high-download, audited libraries. Verify via `npm audit` after install. |
| T-01-02 | Information Disclosure | `next build` output in `out/` | accept | Static export; no secrets baked in — all content is public portfolio data. No env vars in Phase 1. |
| T-01-03 | Tampering | `create-next-app` scaffold | accept | Official Next.js CLI from the Vercel team. Low risk for initial scaffold. |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | Run `npm audit` after install; flag any HIGH/CRITICAL advisories before proceeding |
</threat_model>

<verification>
Full phase verification after all tasks complete:

1. `npm run build` exits 0 and `out/` directory exists with `out/index.html`
2. `grep "output: 'export'" next.config.ts` returns a match
3. `grep "unoptimized: true" next.config.ts` returns a match
4. `grep -c "\-\-color-accent" src/styles/tokens.css` returns 1
5. `grep -c "\-\-text-hero" src/styles/tokens.css` returns 1
6. `grep "Instrument_Serif" src/app/layout.tsx` returns a match
7. `grep "Sora" src/app/layout.tsx` returns a match
8. `grep "font-display: swap" out/index.html` OR verify font asset URLs are relative (self-hosted)
9. `npx tsc --noEmit` exits 0
10. `cat out/index.html | grep -i "axel w"` returns a match (title in output HTML)
</verification>

<success_criteria>
- `next build` completes without errors (exit code 0)
- `out/` directory produced with `out/index.html` present
- `next.config.ts` has both `output: 'export'` and `images: { unoptimized: true }`
- `src/styles/tokens.css` contains 4 color tokens (oklch format) and 4 type scale tokens (clamp format) inside `@theme`
- `src/app/layout.tsx` loads Sora and Instrument Serif via `next/font/google` with `display: 'swap'` and CSS variable exposure
- `src/app/page.tsx` has four section stub comments and no visible page content
- TypeScript strict mode compiles clean
- Prettier config in place with `format` and `format:check` scripts
- ESLint config (`eslint.config.mjs`) in place with `next/core-web-vitals` ruleset and `lint` script in `package.json`
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done, using the summary template.
</output>
