---
phase: 02-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/tokens.css
  - src/data/cv.ts
autonomous: true
requirements:
  - INFRA-02
must_haves:
  truths:
    - "Design tokens for section spacing, card radius, and animation are available as CSS custom properties"
    - "CV content (work entries, education, skills, bio, title) is exported from a single typed TypeScript file"
    - "All component-tier plans can import data types and tokens without re-implementing them"
  artifacts:
    - path: "src/styles/tokens.css"
      provides: "Extended @theme block with --space-section, --radius-card, --duration-reveal, --ease-reveal"
      contains: "--space-section"
    - path: "src/data/cv.ts"
      provides: "WorkEntry[], EducationEntry[], skills string[], bio, title exports"
      exports:
        - WorkEntry
        - EducationEntry
        - workEntries
        - educationEntries
        - skills
        - bio
        - title
  key_links:
    - from: "src/data/cv.ts"
      to: "src/components/hero/Hero.tsx"
      via: "import { bio, title } from '@/data/cv'"
      pattern: "bio.*title"
    - from: "src/data/cv.ts"
      to: "src/components/cv/CV.tsx"
      via: "import { workEntries, educationEntries, skills } from '@/data/cv'"
      pattern: "workEntries.*educationEntries"
    - from: "src/styles/tokens.css"
      to: "src/components/cv/CV.tsx"
      via: "var(--space-section)"
      pattern: "--space-section"
---

## Phase Goal

**As a** recruiter, **I want to** read Axel's name, bio, work experience, education, skills, and contact details and download the CV, **so that** I can evaluate him as a candidate within 30 seconds.

<objective>
Establish the data contracts and design token extensions that all Phase 2 components depend on. This plan ships no visible UI, but creates the two foundation files every other plan imports from. Without these, wave 2 and wave 3 plans cannot proceed.

Purpose: Interface-first ordering — data types in cv.ts and spacing/animation tokens in tokens.css are contracts consumed by Hero, CV, Contact, Header, and FadeUp. Separating this into wave 1 ensures no file-ownership conflicts in waves 2 and 3.

Output: `src/data/cv.ts` with 5 typed exports, `src/styles/tokens.css` extended with 4 new tokens inside the existing @theme block.
</objective>

<execution_context>
@/Users/axel/code/website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/axel/code/website/.planning/PROJECT.md
@/Users/axel/code/website/.planning/ROADMAP.md
@/Users/axel/code/website/.planning/STATE.md

<interfaces>
From src/styles/tokens.css (existing @theme block — append inside, do NOT create a second @theme block):
```css
@theme {
  --color-surface: oklch(97% 0.01 75);
  --color-text: oklch(18% 0.01 75);
  --color-accent: oklch(62% 0.19 55);
  --color-muted: oklch(55% 0.03 75);
  --text-ui: clamp(0.875rem, 0.82rem + 0.28vw, 1rem);
  --text-body: clamp(1rem, 0.92rem + 0.4vw, 1.125rem);
  --text-heading: clamp(1.75rem, 1.4rem + 1.75vw, 2.5rem);
  --text-hero: clamp(3rem, 1.5rem + 7.5vw, 7rem);
  --font-body: Sora, system-ui, sans-serif;
  --font-heading: 'Instrument Serif', Georgia, serif;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend tokens.css with spacing, radius, and animation tokens</name>
  <files>src/styles/tokens.css</files>
  <read_first>
    - src/styles/tokens.css — read the full file to see the existing @theme block before modifying it
    - .planning/phases/02-content/02-CONTEXT.md — D-12 for spacing/radius/animation token values
    - .planning/phases/02-content/02-UI-SPEC.md — "New Tokens to Add" section for exact values
    - .planning/phases/02-content/02-PATTERNS.md — tokens.css "Addition pattern" section for the exact append format
  </read_first>
  <action>
    Append four new tokens inside the EXISTING @theme block in src/styles/tokens.css. Do NOT create a second @theme block — Tailwind v4 processes only one per file. Add the tokens after the existing --font-heading line, with a comment group header:

    Token group "Spacing — section-level vertical padding": --space-section with value clamp(4rem, 3rem + 5vw, 10rem)
    Token group "Radius": --radius-card with value 0.75rem
    Token group "Animation": --duration-reveal with value 400ms, --ease-reveal with value cubic-bezier(0.16, 1, 0.3, 1)

    Decision reference: per D-12 (CONTEXT.md) and UI-SPEC.md "New Tokens to Add" section.

    No other changes to the file. Keep all existing color, type scale, and font tokens unchanged.
  </action>
  <verify>
    <automated>grep -c "\-\-space-section\|--radius-card\|--duration-reveal\|--ease-reveal" /Users/axel/code/website/src/styles/tokens.css</automated>
  </verify>
  <acceptance_criteria>
    - src/styles/tokens.css contains exactly one @theme block (grep -c "@theme" returns 1)
    - src/styles/tokens.css contains --space-section: clamp(4rem, 3rem + 5vw, 10rem)
    - src/styles/tokens.css contains --radius-card: 0.75rem
    - src/styles/tokens.css contains --duration-reveal: 400ms
    - src/styles/tokens.css contains --ease-reveal: cubic-bezier(0.16, 1, 0.3, 1)
    - All existing tokens (--color-surface, --color-text, --color-accent, --color-muted, --text-ui, --text-body, --text-heading, --text-hero, --font-body, --font-heading) remain present and unchanged
    - npm run build exits 0 (Tailwind processes the updated tokens without error)
  </acceptance_criteria>
  <done>tokens.css has 14 total tokens in one @theme block; next build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Create cv.ts typed data file with placeholder content</name>
  <files>src/data/cv.ts</files>
  <read_first>
    - .planning/phases/02-content/02-CONTEXT.md — D-05 (typed data file structure), D-06 (3–5 work entries), D-08 (placeholder bio)
    - .planning/phases/02-content/02-PATTERNS.md — src/data/cv.ts full pattern (complete interface and export shapes)
    - .planning/phases/02-content/02-RESEARCH.md — Pattern 6 (Typed CV Data File) for interface definitions
    - .planning/phases/02-content/02-UI-SPEC.md — Copywriting Contract for exact placeholder copy (bio, title)
  </read_first>
  <action>
    Create src/data/cv.ts. The src/data/ directory does not exist yet — create it.

    The file must export: two interfaces (WorkEntry and EducationEntry) plus five named const exports (bio, title, workEntries, educationEntries, skills). No default export.

    Interface WorkEntry: fields role (string), company (string), dates (string — e.g. "2022 — Present"), description (string).
    Interface EducationEntry: fields degree (string), institution (string), years (string — e.g. "2018 — 2022").

    Const exports (per D-08 placeholder content):
    - bio: string = "Building thoughtful software. Open to new opportunities." (exact copy from UI-SPEC copywriting contract)
    - title: string = "Software Engineer" (exact copy from UI-SPEC copywriting contract)
    - workEntries: WorkEntry[] — 4 entries following the WorkEntry interface shape (per D-06: 3–5 entries). All four use "Placeholder Company / Role / Description — Axel fills this in before launch." as content, with varied dates (e.g. "2023 — Present", "2021 — 2023", "2019 — 2021", "2017 — 2019").
    - educationEntries: EducationEntry[] — 1 entry: degree "Placeholder Degree", institution "Placeholder University", years "2019 — 2023"
    - skills: string[] — at least 8 items covering the categories: TypeScript, React, Next.js, Node.js, Python, PostgreSQL, Git, Docker

    TypeScript strict mode constraints (per CLAUDE.md strict: true):
    - Interfaces are exported with the `export` keyword
    - All string fields use the `string` type (no `any`)
    - Array generics are explicit: `WorkEntry[]`, `EducationEntry[]`, `string[]`
  </action>
  <verify>
    <automated>cd /Users/axel/code/website && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/data/cv.ts exists
    - File exports the WorkEntry interface with fields: role, company, dates, description (all string)
    - File exports the EducationEntry interface with fields: degree, institution, years (all string)
    - File exports const bio as a string (grep shows: export const bio)
    - File exports const title as a string (grep shows: export const title)
    - File exports const workEntries as WorkEntry[] with exactly 4 entries
    - File exports const educationEntries as EducationEntry[] with exactly 1 entry
    - File exports const skills as string[] with at least 8 items
    - No default export in the file
    - npx tsc --noEmit exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>src/data/cv.ts exists, exports all 7 named items, tsc --noEmit exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static content | All content is hardcoded TypeScript — no user input, no external data sources in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | src/data/cv.ts | accept | Content is hardcoded TypeScript; no user input; no `dangerouslySetInnerHTML`; no XSS surface |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 2 (RESEARCH.md Package Legitimacy Audit confirmed — all deps already in package.json from Phase 1) |
</threat_model>

<verification>
- src/styles/tokens.css has exactly one @theme block containing 14 tokens (10 existing + 4 new)
- src/data/cv.ts exists and exports: WorkEntry, EducationEntry, bio, title, workEntries, educationEntries, skills
- npm run build exits 0 (Tailwind processes new tokens; TypeScript compiles cv.ts without errors)
</verification>

<success_criteria>
- tokens.css: 4 new tokens added inside existing @theme block, build succeeds
- cv.ts: 2 interfaces + 5 const exports, strict TypeScript, no default export, tsc exits 0
</success_criteria>

<output>
Create `.planning/phases/02-content/02-01-SUMMARY.md` when done
</output>
