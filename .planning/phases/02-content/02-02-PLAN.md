---
phase: 02-content
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/app/layout.tsx
  - src/components/header/Header.tsx
  - src/components/hero/Hero.tsx
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - INFRA-01
  - INFRA-02
  - INFRA-03
must_haves:
  truths:
    - "Visitor sees Axel W (display-scale name), Software Engineer title, bio, and four CTA links on first page load without scrolling"
    - "A sticky header is present at the top with 'Axel W' on the left and four nav links on the right (hidden on mobile)"
    - "Header background transitions from transparent to surface color when scrolled past 100px"
    - "OpenGraph meta tags (title, description, og:image, twitter:card) are present in the static HTML output"
    - "Page renders correctly at 320px mobile (no horizontal overflow) and 1440px desktop"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Extended metadata export with metadataBase, openGraph, and twitter fields"
      contains: "metadataBase"
    - path: "src/components/header/Header.tsx"
      provides: "Sticky client component with scroll-state background toggle and anchor nav"
      exports: ["Header"]
    - path: "src/components/hero/Hero.tsx"
      provides: "Server component — name, title, bio, and CTA links section"
      exports: ["Hero"]
  key_links:
    - from: "src/components/header/Header.tsx"
      to: "window.scrollY"
      via: "useEffect scroll listener"
      pattern: "window\\.scrollY > 100"
    - from: "src/components/hero/Hero.tsx"
      to: "src/data/cv.ts"
      via: "import { bio, title } from '@/data/cv'"
      pattern: "bio.*title"
    - from: "src/app/layout.tsx"
      to: "og:image"
      via: "metadata.openGraph.images"
      pattern: "metadataBase"
---

<objective>
Build the first visible vertical slice: a recruiter lands on the page and immediately sees Axel's name, title, bio, and CTA links (Hero), plus a functional sticky header with anchor navigation. OpenGraph metadata is also wired in this plan because it lives in layout.tsx — the same file that needs touching for the header context.

Purpose: After this plan ships, a user visiting localhost:3000 can read the hero section and navigate via the sticky header. This is the first moment the site has meaningful content.

Output: Header.tsx (sticky nav, scroll-state), Hero.tsx (name/bio/CTAs), layout.tsx extended with OpenGraph metadata. All three are required to make the first above-the-fold experience complete.
</objective>

<execution_context>
@/Users/axel/code/website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/axel/code/website/.planning/PROJECT.md
@/Users/axel/code/website/.planning/ROADMAP.md
@/Users/axel/code/website/.planning/STATE.md
@/Users/axel/code/website/.planning/phases/02-content/02-01-SUMMARY.md

<interfaces>
From src/styles/tokens.css (after Plan 01 — all tokens available):
- --color-surface, --color-text, --color-accent, --color-muted
- --text-ui, --text-body, --text-heading, --text-hero
- --font-body, --font-heading
- --space-section, --radius-card, --duration-reveal, --ease-reveal

From src/data/cv.ts (created in Plan 01):
```typescript
export const bio: string
export const title: string
export interface WorkEntry { role: string; company: string; dates: string; description: string }
export interface EducationEntry { degree: string; institution: string; years: string }
export const workEntries: WorkEntry[]
export const educationEntries: EducationEntry[]
export const skills: string[]
```

From src/app/layout.tsx (existing — extend metadata only, keep all other code):
```typescript
import type { Metadata } from 'next'
import { Sora, Instrument_Serif } from 'next/font/google'
import './globals.css'
// ... font config ...
export const metadata: Metadata = {
  title: 'Axel W — Software Engineer',
  description: 'Software engineer — portfolio and CV',
}
export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" className={`${sora.variable} ${instrumentSerif.variable}`}>
      <body className="antialiased">{children}</body>
    </html>
  )
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend OpenGraph metadata in layout.tsx</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx — read the full file before modifying; the metadata export must be replaced, not duplicated
    - .planning/phases/02-content/02-PATTERNS.md — src/app/layout.tsx extension pattern (exact metadata shape)
    - .planning/phases/02-content/02-RESEARCH.md — Pattern 5 (OpenGraph Metadata Static), Pitfall 3 (never add 'use client' to layout.tsx), Pitfall 7 (metadataBase required for og:image)
    - .planning/phases/02-content/02-RESEARCH.md — Code Examples section "OpenGraph metadata in layout.tsx"
  </read_first>
  <action>
    Replace the existing minimal `metadata` export in src/app/layout.tsx with the full OpenGraph-extended version. Do NOT change any other code in the file — the font loading, RootLayout function, html/body structure, and imports must remain identical.

    The new metadata export shape (per RESEARCH.md Pattern 5 and PATTERNS.md extension pattern):
    - title: 'Axel W — Software Engineer'
    - description: 'Software engineer — portfolio and CV'
    - metadataBase: new URL('https://axelw.github.io') — REQUIRED for og:image relative URL resolution (Pitfall 7)
    - openGraph object with: title, description, url: 'https://axelw.github.io', siteName: 'Axel W', type: 'website', images array with one entry: url '/og-image.png', width 1200, height 630, alt 'Axel W — Software Engineer'
    - twitter object with: card 'summary_large_image', title 'Axel W — Software Engineer', description 'Software engineer — portfolio and CV'

    Critical constraint: layout.tsx must NOT have 'use client' directive — the metadata export is only valid in Server Components (Pitfall 3). The file currently has no 'use client' and must remain that way.

    Note for executor: public/og-image.png does not exist yet — that is a human-provided asset. The metadata tag still renders correctly; social preview image will be missing until Axel provides the file before launch.
  </action>
  <verify>
    <automated>cd /Users/axel/code/website && npm run build 2>&1 | grep -E "error|warn|✓|Route" | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/layout.tsx does NOT contain 'use client'
    - src/app/layout.tsx contains metadataBase: new URL('https://axelw.github.io')
    - src/app/layout.tsx contains openGraph: with url: 'https://axelw.github.io'
    - src/app/layout.tsx contains twitter: with card: 'summary_large_image'
    - src/app/layout.tsx contains images: [{ url: '/og-image.png', width: 1200, height: 630 }]
    - npm run build exits 0 with no metadata-related errors or warnings about metadataBase
  </acceptance_criteria>
  <done>layout.tsx has full OpenGraph metadata; npm run build exits 0 without metadataBase warnings</done>
</task>

<task type="auto">
  <name>Task 2: Build Header client component with sticky nav and scroll-state</name>
  <files>src/components/header/Header.tsx</files>
  <read_first>
    - .planning/phases/02-content/02-PATTERNS.md — Header.tsx full client component pattern (complete implementation shape)
    - .planning/phases/02-content/02-RESEARCH.md — Pattern 2 (Client Component for Browser APIs), Pitfall 1 (window must be inside useEffect), Pitfall 5 (sticky not fixed to avoid content overlap), Pitfall 6 (scroll-mt-16 on sections)
    - .planning/phases/02-content/02-UI-SPEC.md — Header layout specification (height 64px, name left, links right, D-09/D-10/D-11)
    - .planning/phases/02-content/02-UI-SPEC.md — Interaction States table (nav link hover/focus/active states with --color-accent)
    - .planning/phases/02-content/02-UI-SPEC.md — Copywriting Contract (nav link labels: About, CV, Projects, Contact)
  </read_first>
  <action>
    Create the directory src/components/header/ and create src/components/header/Header.tsx as a 'use client' component.

    Component structure (per D-09, D-10, D-11 and UI-SPEC):
    - 'use client' directive at top
    - Import useEffect, useState from 'react' and clsx from 'clsx'
    - useState(false) for scrolled state
    - useEffect that adds a passive scroll listener: sets scrolled to true when window.scrollY > 100, false otherwise. Cleanup removes the listener. All window access is inside useEffect (Pitfall 1).
    - Outer element: <header> with Tailwind classes for sticky top-0 z-50 h-16 px-6 flex items-center justify-between transition-colors duration-300; background toggles between bg-transparent (default) and bg-[var(--color-surface)] shadow-sm (scrolled) via clsx (per D-11)
    - Left side: <a href="#hero"> with text "Axel W", font-family var(--font-heading), text size var(--text-body), font-weight semibold
    - Right side: <nav aria-label="Main navigation"> with hidden sm:flex gap-8; contains four <a> links: href="#hero" text "About", href="#cv" text "CV", href="#projects" text "Projects", href="#contact" text "Contact" (per UI-SPEC Copywriting Contract and D-09)
    - Nav link styling: text-[var(--color-text)] no underline by default; hover:text-[var(--color-accent)] hover:underline; focus:outline-2 focus:outline-[var(--color-accent)] focus:outline-offset-2 (per UI-SPEC Interaction States)
    - All color references use var(--token-name) — never hardcoded hex (per PATTERNS.md CSS Token Reference Pattern)
    - Use sticky positioning (position: sticky), NOT fixed — sticky keeps header in document flow and prevents content overlap (Pitfall 5)

    Note: Sections will need scroll-mt-16 class to offset the 64px sticky header when anchor links are clicked (Pitfall 6). This class is applied in the section components built in Plan 03, not here.
  </action>
  <verify>
    <automated>cd /Users/axel/code/website && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/header/Header.tsx exists
    - File starts with 'use client' directive
    - File imports useEffect and useState from 'react'
    - File imports clsx from 'clsx'
    - File contains window.scrollY > 100 (inside useEffect only — not at module scope)
    - File contains { passive: true } on the addEventListener call
    - Header element uses className with sticky top-0 z-50 h-16
    - Nav element has aria-label="Main navigation"
    - Nav has className containing hidden sm:flex
    - Four anchor links present: #hero, #cv, #projects, #contact
    - No hardcoded color hex values (#xxx or rgb/rgba) — all colors via var(--token-name)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Header.tsx exists with 'use client', sticky positioning, scroll-state toggle, mobile-hidden nav; tsc exits 0</done>
</task>

<task type="auto">
  <name>Task 3: Build Hero server component with name, bio, and CTA links</name>
  <files>src/components/hero/Hero.tsx</files>
  <read_first>
    - .planning/phases/02-content/02-PATTERNS.md — Hero.tsx full server component pattern (complete shape)
    - .planning/phases/02-content/02-RESEARCH.md — Pattern 1 (Server Component Default), Anti-Patterns (no 'use client' on content components)
    - .planning/phases/02-content/02-UI-SPEC.md — Hero Section layout specification (min-h-screen, left-aligned, stack order, text roles)
    - .planning/phases/02-content/02-UI-SPEC.md — Interaction States (CTA links hover/focus with --color-accent)
    - .planning/phases/02-content/02-UI-SPEC.md — Copywriting Contract (CTA labels: GitHub, LinkedIn, Email, Download CV)
    - .planning/phases/02-content/02-CONTEXT.md — D-01 (left-aligned), D-02 (no photo), D-03 (--text-hero name), D-04 (min-h-screen)
    - src/data/cv.ts — read to confirm exact export names (bio, title) before importing
  </read_first>
  <action>
    Create the directory src/components/hero/ and create src/components/hero/Hero.tsx as a Server Component (no 'use client' directive).

    Component structure (per D-01, D-02, D-03, D-04 and UI-SPEC Hero Section specification):
    - No 'use client' directive — Hero is purely static, receives no browser APIs (per RESEARCH.md Anti-Patterns)
    - Import bio and title from '@/data/cv' (named imports — not default)
    - Outer element: <section id="hero" aria-labelledby="hero-heading"> with classes min-h-screen flex flex-col justify-center px-6 max-w-4xl scroll-mt-16
    - Stack order top-to-bottom (per UI-SPEC):
      1. <h1 id="hero-heading"> — display name "Axel W" (hardcoded, not from cv.ts); font-family var(--font-heading) (Instrument Serif); text size var(--text-hero); line-height leading-[1.05]; color var(--color-text); bottom margin mb-6
      2. <p> — {title} from cv.ts import; text size var(--text-body); font-weight semibold; color var(--color-muted); bottom margin mb-4
      3. <p> — {bio} from cv.ts import; text size var(--text-body); color var(--color-text); max-width max-w-[60ch]; bottom margin mb-10
      4. <div> with flex flex-wrap gap-4 — four CTA <a> links:
         - href="https://github.com/axelw" target="_blank" rel="noopener noreferrer" — label "GitHub"
         - href="https://linkedin.com/in/axelw" target="_blank" rel="noopener noreferrer" — label "LinkedIn"
         - href="mailto:axel@example.com" — label "Email"
         - href="/cv.pdf" download — label "Download CV"

    CTA link styling: text size var(--text-ui); color var(--color-text); underline with underline-offset-2; underline color var(--color-muted) by default, changes to var(--color-accent) on hover; focus:outline-2 focus:outline-[var(--color-accent)] focus:outline-offset-2; minimum touch target height of 44px (py-2 or equivalent)

    Constraints:
    - No centering — left-aligned only (D-01). Do not add text-center or mx-auto to the section.
    - No photo, no decorative elements (D-02)
    - Semantic HTML: section with aria-labelledby pointing to h1 id (per coding-style.md)
    - External links: always target="_blank" rel="noopener noreferrer" (security)
    - All colors via var(--token-name) — no hardcoded hex

    Note: public/cv.pdf does not exist yet — it is a human-provided asset. The link renders correctly; the download will 404 until Axel provides the file.
  </action>
  <verify>
    <automated>cd /Users/axel/code/website && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/hero/Hero.tsx exists
    - File does NOT contain 'use client'
    - File imports bio and title from '@/data/cv'
    - Section element has id="hero" and aria-labelledby="hero-heading"
    - h1 element has id="hero-heading"
    - h1 contains hardcoded text "Axel W"
    - A <p> element renders {title} (the Software Engineer tagline)
    - A <p> element renders {bio} with max-w-[60ch]
    - Four <a> tags present with href values: https://github.com/axelw, https://linkedin.com/in/axelw, mailto:axel@example.com, /cv.pdf
    - External links (github, linkedin) have rel="noopener noreferrer"
    - /cv.pdf link has download attribute
    - No hardcoded color hex values — all colors via var(--token-name)
    - npm run build exits 0
  </acceptance_criteria>
  <done>Hero.tsx is a Server Component; name, title, bio, and four CTAs render; npm run build exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External links | Hero CTA links point to external URLs (github, linkedin) — all hardcoded, no user-supplied URLs |
| OG image | metadataBase set to production URL; og:image resolves to /og-image.png |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | Hero.tsx external links | mitigate | All external hrefs hardcoded in TypeScript — no user input; rel="noopener noreferrer" on all target="_blank" links prevents tab-napping |
| T-02-03 | Information Disclosure | layout.tsx metadata | accept | OG metadata is intentionally public; exposes only name and description — no PII beyond what is displayed on the page |
| T-02-SC | Tampering | npm installs | accept | No new packages installed — all deps from Phase 1 (RESEARCH.md Package Legitimacy Audit confirmed) |
</threat_model>

<verification>
- npm run build exits 0 with Hero, Header, and updated layout.tsx
- out/index.html contains og:title, og:image, and twitter:card meta tags (verify with grep)
- src/components/header/Header.tsx has 'use client', sticky positioning, hidden sm:flex nav
- src/components/hero/Hero.tsx has no 'use client', renders name/title/bio/CTAs
- All four CTA links present in Hero: github.com, linkedin.com, mailto:, /cv.pdf
</verification>

<success_criteria>
- Visiting localhost:3000 shows hero section (name at display scale, title, bio, 4 CTA links) and sticky header with nav
- Header is transparent over hero; gains background color when scrolled past 100px
- Nav links hidden on 320px viewport, visible at 640px+
- Static HTML output contains OpenGraph meta tags for social sharing
- npm run build exits 0
</success_criteria>

<output>
Create `.planning/phases/02-content/02-02-SUMMARY.md` when done
</output>
