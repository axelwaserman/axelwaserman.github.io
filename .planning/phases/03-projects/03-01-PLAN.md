---
phase: 03-projects
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/projects.ts
  - src/lib/projects.ts
  - src/components/projects/Projects.tsx
  - src/components/projects/ProjectCard.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03
must_haves:
  truths:
    - "D-03: Running `next build` fetches Axel's public, non-archived GitHub repos at build time inside a React Server Component and bakes them into the static HTML — out/index.html contains real repo names, no client-side fetch is performed at runtime"
    - "D-07: Each rendered project card displays the repo name, description, primary language, last pushed date, repo link, and a live demo link only when the API homepage field is set (relative-date polish lands in Plan 03)"
    - "D-08: Star count is deliberately excluded from every rendered card — the Project type has no stars field and ProjectCard never references one"
    - "D-04: All non-archived public repos are fetched (no count cap from product perspective; the per_page=100 GitHub API ceiling is documented as an accepted concession with a build-time soft warn that fires when rawList.length >= 100)"
    - "D-05: Repos render sorted by most recently pushed (pushed_at descending) — the fetcher sorts before mapping to Project[]"
    - "D-01: The card layout is a responsive grid — multi-column on desktop, single-column on mobile — and avoids the generic Tailwind card grid look (intentional hierarchy and editorial surface; hover-state polish lands in Plan 03)"
    - "D-02: Projects section renders inside <FadeUp> at #projects, between CV and Contact, matching the header nav order (#hero, #cv, #projects, #contact) and consistent with the FadeUp wrapper used for CV and Contact"
  artifacts:
    - path: "src/data/projects.ts"
      provides: "Project type definition (TypeScript interface) — shape of normalized GitHub repo data consumed by ProjectCard"
      exports: ["Project"]
    - path: "src/lib/projects.ts"
      provides: "fetchProjects() async function — calls GitHub REST /users/axelw/repos at build time, filters archived, sorts by pushed_at desc, returns Project[]"
      exports: ["fetchProjects"]
    - path: "src/components/projects/Projects.tsx"
      provides: "Server Component section — calls fetchProjects() at build, renders eyebrow + visible h2 + <ul> grid of ProjectCard"
      exports: ["Projects"]
    - path: "src/components/projects/ProjectCard.tsx"
      provides: "Server Component presentational card — renders name (h3), meta line, optional description, repo link, and conditional live-demo link from a Project prop"
      exports: ["ProjectCard"]
    - path: "src/app/page.tsx"
      provides: "Root page composing Header, Hero, CV, Projects, Contact inside FadeUp wrappers — Projects sits between CV and Contact"
      exports: ["Home"]
  key_links:
    - from: "src/components/projects/Projects.tsx"
      to: "src/lib/projects.ts"
      via: "import { fetchProjects } from '@/lib/projects'"
      pattern: "fetchProjects"
    - from: "src/components/projects/Projects.tsx"
      to: "src/components/projects/ProjectCard.tsx"
      via: "import ProjectCard from './ProjectCard' inside grid <ul><li>"
      pattern: "ProjectCard"
    - from: "src/components/projects/ProjectCard.tsx"
      to: "src/data/projects.ts"
      via: "import type { Project } from '@/data/projects'"
      pattern: "import type.*Project.*@/data/projects"
    - from: "src/lib/projects.ts"
      to: "https://api.github.com/users/axelw/repos"
      via: "fetch() at build time inside Server Component tree"
      pattern: "api\\.github\\.com/users/axelw/repos"
    - from: "src/app/page.tsx"
      to: "src/components/projects/Projects.tsx"
      via: "import Projects from '@/components/projects/Projects' wrapped in <FadeUp>"
      pattern: "<FadeUp>\\s*<Projects"
---

<objective>
Deliver the thinnest end-to-end vertical slice for Phase 3: typed Project shape, build-time GitHub fetcher (no fallback yet — that comes in Plan 02), Server Component section, presentational ProjectCard, and page wiring. After this plan ships, running `next build` produces an `out/index.html` that contains Axel's real, non-archived GitHub repos rendered as cards in the Projects section. The user can visit the dev server (with network access) and see real repo names, descriptions, and a Repo link per card. Conditional Live demo links appear only when the API supplies `homepage`.

Purpose: This is the MVP slice for Phase 3. It satisfies all three Phase 3 requirements (PROJ-01, PROJ-02, PROJ-03) end-to-end with the simplest possible fetcher (no fallback resilience yet, no relative-date utility, no empty-state, no extra polish). Subsequent plans add fallback (Plan 02) and polish + relative date + empty state + UI-SPEC interaction states (Plan 03).

Output: 4 new files (src/data/projects.ts, src/lib/projects.ts, src/components/projects/Projects.tsx, src/components/projects/ProjectCard.tsx) + page.tsx modified. Real GitHub data baked into static HTML.
</objective>

<execution_context>
@/Users/axel/code/website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/axel/code/website/.planning/ROADMAP.md
@/Users/axel/code/website/.planning/REQUIREMENTS.md
@/Users/axel/code/website/.planning/STATE.md
@/Users/axel/code/website/.planning/phases/03-projects/03-CONTEXT.md
@/Users/axel/code/website/.planning/phases/03-projects/03-PATTERNS.md
@/Users/axel/code/website/.planning/phases/03-projects/03-UI-SPEC.md
@/Users/axel/code/website/CLAUDE.md

## Phase Goal (User Story)

**As a** recruiter or curious visitor, **I want to** see Axel's public GitHub projects rendered on the page with names, descriptions, and links, **so that** I can quickly evaluate his real, current work without leaving the site.

<interfaces>
<!-- Existing project interfaces the executor must compose with. Extracted from codebase. -->
<!-- Use these directly — no codebase exploration needed. -->

From src/data/cv.ts (existing pattern to mirror — named-export typed data file):
```typescript
export interface WorkEntry { role: string; company: string; dates: string; description: string }
export interface EducationEntry { degree: string; institution: string; years: string }
export const workEntries: WorkEntry[]
export const educationEntries: EducationEntry[]
export const skills: string[]
```

From src/components/cv/WorkEntry.tsx (presentational pattern to clone for ProjectCard):
```typescript
import type { WorkEntry as WorkEntryType } from '@/data/cv'
interface WorkEntryProps { entry: WorkEntryType }
export default function WorkEntry({ entry }: WorkEntryProps): JSX.Element
```

From src/components/contact/Contact.tsx (visible h2 pattern + external-link styling — copy verbatim):
- Section: className="py-[var(--space-section)] px-6 max-w-4xl scroll-mt-16"
- h2: className="font-[var(--font-heading)] text-[length:var(--text-heading)] mb-4"
- External link: className="text-[length:var(--text-ui)] text-[var(--color-text)] underline underline-offset-2 decoration-[var(--color-muted)] hover:decoration-[var(--color-accent)] hover:text-[var(--color-accent)] focus:outline-2 focus:outline-[var(--color-accent)] focus:outline-offset-2 py-2"

From src/components/cv/CV.tsx (eyebrow label pattern):
- Eyebrow: className="text-[length:var(--text-ui)] font-semibold text-[var(--color-muted)] uppercase tracking-[0.08em]"

From src/components/ui/FadeUp.tsx (sealed dependency, do not modify):
```typescript
interface FadeUpProps { children: React.ReactNode; className?: string }
export default function FadeUp({ children, className }: FadeUpProps): JSX.Element
```

From src/components/header/Header.tsx (anchor target already wired — section needs id="projects"):
- nav <a href="#projects">Projects</a> exists at line 45

From src/styles/tokens.css (existing tokens — no new tokens needed):
- --color-surface, --color-text, --color-accent, --color-muted
- --text-ui, --text-body, --text-heading
- --font-body, --font-heading
- --space-section, --radius-card

From tsconfig.json:
- "@/*" path alias maps to "./src/*"
- "resolveJsonModule": true (relevant for Plan 02)
- "strict": true (no any allowed)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Project type and write build-time GitHub fetcher</name>
  <files>src/data/projects.ts, src/lib/projects.ts</files>
  <read_first>
    - .planning/phases/03-projects/03-PATTERNS.md — full patterns for src/data/projects.ts (Project interface) and src/lib/projects.ts (fetchProjects shape, header pattern, error handling)
    - .planning/phases/03-projects/03-UI-SPEC.md — Component Inventory row "Project (type)" specifying shape: { name, description: string | null, language: string | null, pushedAt: string (ISO), repoUrl, homepage: string | null }
    - .planning/phases/03-projects/03-CONTEXT.md — D-03 (build-time fetch in Server Component), D-04 (all non-archived public repos, no count cap), D-05 (sort by pushed_at desc), D-08 (star count excluded)
    - src/data/cv.ts — confirm the named-export typed data convention used in this project (no default export, types exported alongside data)
    - CLAUDE.md — Technology Stack section: native fetch only (no @octokit/rest), GitHub REST API, GITHUB_TOKEN auth optional
  </read_first>
  <action>
    Create src/data/projects.ts as a type-definition file with named exports only (no default export, no value exports yet — fallback data file lives at src/data/projects.json in Plan 02; this file owns the type contract).

    Define and export the Project interface with exactly these fields (camelCase per TypeScript convention, nullable fields explicitly typed):
    - name: string
    - description: string | null
    - language: string | null
    - pushedAt: string (ISO 8601 timestamp from the GitHub API pushed_at field)
    - repoUrl: string
    - homepage: string | null

    Use `interface` (object shape may be extended later) per TypeScript coding-style rules. No `any`. No default export.

    Create src/lib/ directory (does not yet exist) and src/lib/projects.ts.

    File header: a short comment noting the file is consumed only by Server Components at build time, never shipped to the client.

    Inside the file:
    1. Import the Project type with `import type { Project } from '@/data/projects'`.
    2. Define a private internal interface `GitHubRepo` describing only the GitHub REST fields this fetcher reads (name, description, language, pushed_at, html_url, homepage, archived, fork, disabled — all snake_case to match the API). Do not export this. Treat any field that may be null per the API as `string | null`. Type the boolean fields explicitly (archived: boolean, fork: boolean, disabled: boolean).
    3. Export a single async function `fetchProjects(): Promise<Project[]>`.

    fetchProjects implementation contract:
    - Endpoint constant: `https://api.github.com/users/axelw/repos?per_page=100&type=owner&sort=pushed&direction=desc`
    - D-04 ceiling concession (per W-04): D-04 says "all non-archived public repos, no count cap", but PATTERNS.md and the GitHub REST API enforce a per_page maximum of 100 per request. Implementing Link-header pagination is deferred (axelw currently has fewer than 100 public non-archived repos). This plan documents the 100-repo ceiling as an accepted concession AND emits a build-time soft warning when the raw response approaches the limit, so a future overrun is visible in CI logs and triggers the pagination upgrade.
    - Build a Headers object with: `Accept: application/vnd.github+json`, `User-Agent: axelw-website-build`, and conditionally `Authorization: Bearer <token>` only when `process.env.GITHUB_TOKEN` is a non-empty string. Read the env var via `process.env.GITHUB_TOKEN` (no `NEXT_PUBLIC_` prefix — must NOT be exposed to client).
    - Call native `fetch(endpoint, { headers })`. Do not add `cache: 'force-cache'` — Next.js caches build-time fetches by default and `output: 'export'` runs the fetch only at build time.
    - If `!res.ok`, throw `new Error(`GitHub API ${res.status}`)`. This plan does NOT catch the error (Plan 02 adds the try/catch + JSON fallback). A 4xx/5xx in this plan halts the build — that is acceptable for the MVP slice.
    - Parse the JSON body as `unknown` first, then narrow: assert it is an array, then map each entry to `GitHubRepo` (cast through `unknown` and validate the minimum required fields are present — at least `name: string`, `html_url: string`, `pushed_at: string`, `archived: boolean`).
    - After narrowing the parsed array (call this `rawList: GitHubRepo[]`) and BEFORE the archived/disabled/fork filter, emit the ceiling soft check. Use a single `console.warn` line with the exact wording: `[fetchProjects] reached the 100-repo per_page ceiling — implement pagination per PATTERNS.md if axelw exceeds 100 public non-archived repos.` The check fires when `rawList.length >= 100`. Add an eslint-disable-next-line comment directly above it: `// eslint-disable-next-line no-console -- intentional build-time ceiling warn per W-04 / D-04` to keep lint clean (matches the Plan 02 fallback-warn style). This is the ONLY console.* call permitted in this plan.
    - Filter: keep only entries where `archived === false` AND `disabled === false` AND `fork === false` (D-04 says all non-archived public; PATTERNS.md recommends excluding forks unless deliberately included — exclude forks to keep the list to original work).
    - Sort the filtered array by `pushed_at` descending (defensive resort even though the URL already requested it).
    - Map each filtered/sorted GitHubRepo into a Project object with field renames: name → name, description → description, language → language, pushed_at → pushedAt, html_url → repoUrl, homepage → homepage. Coerce empty-string homepages to null (`raw.homepage && raw.homepage.length > 0 ? raw.homepage : null`) so the UI's null-check correctly hides the live-demo link for whitespace-only or empty values.

    Other than the documented ceiling warn above, no `console.log`, no other `console.warn` in this plan (Plan 02 introduces a separate documented fallback warn inside its catch block). No `any` anywhere. Use `unknown` at the JSON-parse boundary and narrow.

    GITHUB_TOKEN handling — security: the token is read from process.env at build time only. It MUST NOT be inlined into any committed source string, logged, or echoed to console. The header is constructed in memory, used by fetch, and discarded. Do not import or read process.env.GITHUB_TOKEN from any client component.
  </action>
  <verify>
    <automated>cd /Users/axel/code/website &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File src/data/projects.ts exists
    - src/data/projects.ts exports an interface named Project (grep -c "export interface Project" src/data/projects.ts returns 1)
    - Project interface contains exactly these field names: name, description, language, pushedAt, repoUrl, homepage (grep "pushedAt" and "repoUrl" present; no "pushed_at" or "html_url" in the type)
    - description, language, homepage are typed as `string | null` (grep "string | null" appears at least 3 times in src/data/projects.ts)
    - src/data/projects.ts has no default export (grep -c "export default" src/data/projects.ts returns 0)
    - File src/lib/projects.ts exists
    - src/lib/projects.ts exports an async function named fetchProjects with return type Promise&lt;Project[]&gt; (grep "export async function fetchProjects" src/lib/projects.ts returns a match)
    - src/lib/projects.ts imports Project as a type-only import (grep "import type { Project }" src/lib/projects.ts returns 1)
    - src/lib/projects.ts URL contains "api.github.com/users/axelw/repos" and "per_page=100"
    - src/lib/projects.ts sets the User-Agent header (grep "User-Agent" returns at least 1 match)
    - src/lib/projects.ts conditionally sets Authorization only when process.env.GITHUB_TOKEN is set (grep "process.env.GITHUB_TOKEN" appears; grep for the literal value `Bearer ${`)
    - src/lib/projects.ts contains the D-04 ceiling soft-check (grep -c "reached the 100-repo per_page ceiling" src/lib/projects.ts returns 1)
    - src/lib/projects.ts contains the ceiling warn's eslint-disable line (grep "eslint-disable-next-line no-console" src/lib/projects.ts returns at least 1)
    - src/lib/projects.ts contains the conditional `rawList.length >= 100` (or equivalent identifier-name) gating the ceiling warn (grep -E "length\\s*>=\\s*100" src/lib/projects.ts returns at least 1)
    - src/lib/projects.ts filters archived === false AND disabled === false AND fork === false (grep "archived" and "disabled" and "fork" all return matches)
    - src/lib/projects.ts contains exactly one console.warn (the ceiling warn) and zero console.log (grep -c "console.warn" src/lib/projects.ts returns 1; grep -c "console.log" src/lib/projects.ts returns 0)
    - src/lib/projects.ts does NOT have 'use client' (grep -c "use client" src/lib/projects.ts returns 0)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Project interface defined with all six fields and correct null types; fetchProjects pulls from the documented endpoint, filters archived/disabled/fork, sorts pushed_at desc, maps snake_case to camelCase; emits the documented 100-repo ceiling warn when the raw response hits the per_page maximum; tsc exits 0</done>
</task>

<task type="auto">
  <name>Task 2: Build ProjectCard and Projects Server Components</name>
  <files>src/components/projects/Projects.tsx, src/components/projects/ProjectCard.tsx</files>
  <read_first>
    - .planning/phases/03-projects/03-PATTERNS.md — full patterns for ProjectCard.tsx (clone of WorkEntry.tsx with card surface added, conditional description and homepage rendering, link styling) and Projects.tsx (Server Component async section, eyebrow + visible h2 + grid pattern)
    - .planning/phases/03-projects/03-UI-SPEC.md — Component Inventory (Projects, ProjectCard responsibilities), Spacing Scale (card grid: 1-col mobile, 2-col ≥640px, gap-6, p-6 internal), Typography (card title --text-body weight 600, meta --text-ui muted), Color (card surface treatment: flat-on-surface with hairline border, accent reserved for hover/focus only), Copywriting (eyebrow "Projects" UPPERCASE; visible h2 "Selected work on GitHub"; repo link label "Repo →"; live demo label "Live demo →"; description omitted entirely if null/empty)
    - .planning/phases/03-projects/03-CONTEXT.md — D-01 (responsive card grid, anti-template), D-02 (FadeUp wrapping handled in page.tsx Task 3), D-07 (each card: name, description, language, last-pushed date, repo link, optional live demo)
    - src/components/cv/WorkEntry.tsx — exact analog for the presentational card; copy structure verbatim, then add card surface treatment and link row
    - src/components/contact/Contact.tsx — exact analog for the visible h2 + section shell + external-link className string
    - src/components/cv/CV.tsx — eyebrow label className pattern (lines 17-19); section shell className pattern (lines 8-12)
  </read_first>
  <action>
    Create src/components/projects/ directory.

    --- src/components/projects/ProjectCard.tsx (Server Component, no 'use client') ---

    Type-only import the Project interface: `import type { Project } from '@/data/projects'`.

    Define ProjectCardProps interface: `{ project: Project }`.

    Default export ProjectCard({ project }: ProjectCardProps).

    Plan 01 scope note: this plan does NOT use the relative-date utility (introduced in Plan 03). The meta line shows `language` only when non-null; the relative date is omitted in this slice and added in Plan 03. If `language` is null, omit the meta line entirely (no orphan separator). This keeps the slice minimal but valid.

    Render <article> with className `p-6 rounded-[var(--radius-card)] border border-[var(--color-muted)]/20`:

    1. <h3> with className `text-[length:var(--text-body)] font-semibold mb-1` containing `{project.name}` verbatim (no title-casing — UI-SPEC explicitly says preserve casing).

    2. Meta line — render only when `project.language` is non-null:
       <p className="text-[length:var(--text-ui)] text-[var(--color-muted)] mb-3">{project.language}</p>

    3. Description — render only when `project.description` is a non-empty string (use `project.description && project.description.length > 0 &&`):
       <p className="text-[length:var(--text-body)] mb-6 max-w-[55ch]">{project.description}</p>
       Omit the entire <p> when description is null or empty (no placeholder dash, no "No description" — UI-SPEC §Copywriting "Description fallback").

    4. Link row <div className="flex flex-wrap gap-4">:
       - Repo link: anchor with href={project.repoUrl}, target="_blank", rel="noopener noreferrer", aria-label={`View ${project.name} repository on GitHub`}, className="text-[length:var(--text-ui)] text-[var(--color-text)] underline underline-offset-2 decoration-[var(--color-muted)] hover:decoration-[var(--color-accent)] hover:text-[var(--color-accent)] focus:outline-2 focus:outline-[var(--color-accent)] focus:outline-offset-2 py-2". Inside the link: "Repo " then a separate <span aria-hidden="true">→</span>.
       - Live demo link, rendered conditionally with `{project.homepage && (...)}`: same className and rel/target attributes; href={project.homepage}; aria-label={`View ${project.name} live demo`}; inner content "Live demo " + <span aria-hidden="true">→</span>.

    The `→` glyph is wrapped in `aria-hidden="true"` per UI-SPEC §Accessibility.

    No 'use client' directive. No imports of the relative-date utility (Plan 03). No card-hover edge highlight in this slice (Plan 03 adds the transition + accent border swap per UI-SPEC §Interaction).

    --- src/components/projects/Projects.tsx (Server Component, async) ---

    Imports:
    - `import { fetchProjects } from '@/lib/projects'`
    - `import ProjectCard from './ProjectCard'`

    Default export: `export default async function Projects()`.

    Function body: `const projects = await fetchProjects()`.

    Plan 01 scope note: this plan does NOT include an empty-state component (introduced in Plan 03). If projects.length is 0 the grid simply renders no children — Plan 03 adds ProjectsEmptyState and the conditional. This is fine because in normal operation Axel has multiple public repos.

    Return JSX:
    <section id="projects" aria-labelledby="projects-heading" className="py-[var(--space-section)] px-6 max-w-4xl scroll-mt-16">

    1. Eyebrow label div: className="text-[length:var(--text-ui)] font-semibold text-[var(--color-muted)] uppercase tracking-[0.08em] mb-4" with text "Projects" (verbatim from UI-SPEC §Copywriting; matches CV section labels).

    2. Visible h2 with id="projects-heading", className="font-[var(--font-heading)] text-[length:var(--text-heading)] mb-8", text "Selected work on GitHub" (verbatim from UI-SPEC §Copywriting).

    3. <ul className="grid grid-cols-1 sm:grid-cols-2 gap-6"> wrapping projects.map. Each item: <li key={p.repoUrl}><ProjectCard project={p} /></li>. The key uses p.repoUrl because it is unique per repo and stable across builds.

    No 'use client'. No useReducedMotion (server scope). The async function is valid in Next 16 App Router; FadeUp wraps this section in page.tsx (Task 3) — Server async children inside Client parents are allowed by the React composition model.
  </action>
  <verify>
    <automated>cd /Users/axel/code/website &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/projects/ProjectCard.tsx exists
    - ProjectCard.tsx does NOT contain 'use client' (grep -c "use client" returns 0)
    - ProjectCard.tsx imports Project as a type-only import (grep "import type { Project }" returns 1)
    - ProjectCard.tsx renders an <article> element
    - ProjectCard.tsx contains the conditional pattern for homepage (grep "project.homepage &amp;&amp;" returns at least 1 match)
    - ProjectCard.tsx contains the conditional pattern for description (grep "project.description" appears in a JSX conditional — at least 1 match)
    - ProjectCard.tsx Repo link sets target="_blank" AND rel="noopener noreferrer" (grep both attributes)
    - ProjectCard.tsx Repo link sets aria-label containing the literal phrase "repository on GitHub" (grep returns 1)
    - ProjectCard.tsx Live demo link sets aria-label containing the literal phrase "live demo" (grep returns 1)
    - ProjectCard.tsx contains aria-hidden="true" on the arrow span (grep returns at least 2 matches: one per link)
    - File src/components/projects/Projects.tsx exists
    - Projects.tsx does NOT contain 'use client' (grep -c "use client" returns 0)
    - Projects.tsx is declared async (grep "async function Projects" returns 1)
    - Projects.tsx imports fetchProjects from '@/lib/projects' (grep returns 1)
    - Projects.tsx imports ProjectCard from './ProjectCard' (grep returns 1)
    - Projects.tsx <section> has id="projects" (grep id=\"projects\" returns 1)
    - Projects.tsx <section> has aria-labelledby="projects-heading" (grep returns 1)
    - Projects.tsx contains scroll-mt-16 in the section className (grep returns 1)
    - Projects.tsx contains the literal text "Selected work on GitHub" (grep returns 1)
    - Projects.tsx contains the literal text "Projects" inside the eyebrow div (grep returns at least 1 match — accept multiple, the section name appears in id and aria-labelledby too)
    - Projects.tsx wraps the map output in a <ul> with grid-cols-1 sm:grid-cols-2 gap-6 classes (grep "grid-cols-1 sm:grid-cols-2" returns 1)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>ProjectCard renders name + optional language + optional description + Repo link + optional Live demo link with correct accessibility and external-link attributes; Projects async Server Component fetches and renders the grid; tsc exits 0</done>
</task>

<task type="auto">
  <name>Task 3: Wire Projects into page.tsx between CV and Contact</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - .planning/phases/03-projects/03-PATTERNS.md — section "src/app/page.tsx (modified)" specifying the import addition and the FadeUp wrapping pattern, plus the position decision (Projects between CV and Contact to match header nav order)
    - .planning/phases/03-projects/03-CONTEXT.md — D-02 (Projects wrapped in existing FadeUp component, consistent with CV/Contact)
    - src/app/page.tsx — current composition before modification (must be read so the modification preserves Header/Hero/CV/Contact order)
    - src/components/header/Header.tsx — confirms nav order is hero → cv → projects → contact, so visible page order must match
  </read_first>
  <action>
    Modify src/app/page.tsx by adding a single new import and a single new wrapped section between CV and Contact.

    The current file imports Header, Hero, CV, Contact, FadeUp and renders Header outside main, then Hero, FadeUp(CV), FadeUp(Contact), then a `{/* Projects section — Phase 3 */}` comment.

    Replace the file with the following composition:
    1. Add import statement: `import Projects from '@/components/projects/Projects'` (place it alphabetically near the other component imports, between CV and Contact imports, matching the existing import order convention).
    2. Inside <main>, between the existing `<FadeUp><CV /></FadeUp>` and `<FadeUp><Contact /></FadeUp>` wrappers, insert `<FadeUp><Projects /></FadeUp>`.
    3. Remove the `{/* Projects section — Phase 3 */}` comment — it is replaced by the actual component.

    Resulting visible order in the rendered page: Hero → CV → Projects → Contact. This matches the header nav order (`#hero, #cv, #projects, #contact`) so anchor jumps land in the visible-order sequence.

    Do NOT add 'use client' to page.tsx — it remains a Server Component composing both Server children (Hero, CV, Projects, Contact) and Client children (Header, FadeUp). React's RSC composition allows async Server children (Projects is async) inside Client parents (FadeUp).

    Do NOT modify FadeUp.tsx itself — it is a sealed dependency.
  </action>
  <verify>
    <automated>cd /Users/axel/code/website &amp;&amp; npm run build 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx imports Projects from '@/components/projects/Projects' (grep "import Projects from '@/components/projects/Projects'" returns 1)
    - src/app/page.tsx contains a FadeUp-wrapped Projects element (grep -E "FadeUp&gt;\\s*&lt;Projects" or equivalent matches; for simplicity grep for the substring "&lt;Projects" returns 1 in JSX position and is preceded by &lt;FadeUp&gt; in the file — verify by visual inspection of the diff if regex is brittle)
    - src/app/page.tsx does NOT contain the literal stub comment "Projects section — Phase 3" (grep -c "Projects section — Phase 3" returns 0)
    - src/app/page.tsx does NOT contain 'use client' (grep -c "use client" returns 0)
    - The visible JSX order in page.tsx is: &lt;Hero /&gt; appears before &lt;CV /&gt; appears before &lt;Projects /&gt; appears before &lt;Contact /&gt; (verify by reading the file — line numbers must be ascending in this order)
    - npm run build exits 0 (build succeeds end-to-end with the GitHub fetch — requires network access at build time; in a sandboxed environment without network the executor must export GITHUB_TOKEN or run on a host with network. If the build fails specifically due to network, the executor should note this in the SUMMARY and re-run on a network-enabled host before declaring the plan done — Plan 02 will add the JSON fallback that removes this dependency)
    - The generated out/index.html contains id="projects" (grep -c 'id="projects"' out/index.html returns at least 1)
    - The generated out/index.html contains the literal text "Selected work on GitHub" (grep -c "Selected work on GitHub" out/index.html returns at least 1)
    - The generated out/index.html contains at least one href starting with "https://github.com/axelw/" (grep -E 'href="https://github.com/axelw/[^"]+"' out/index.html returns at least 1 match; this confirms real fetched repo data was baked in)
    - The generated out/index.html does NOT contain any text indicating a GitHub-token leak: grep -c "Bearer " out/index.html returns 0; grep -c "GITHUB_TOKEN" out/index.html returns 0
  </acceptance_criteria>
  <done>page.tsx composes Header + Hero + FadeUp(CV) + FadeUp(Projects) + FadeUp(Contact); npm run build exits 0; out/index.html contains real GitHub repo data and the Projects section anchor</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build host → GitHub REST API | Untrusted external response crosses into our build pipeline. JSON fields (name, description, homepage) become rendered HTML attributes and link targets. |
| Build environment → committed source | GITHUB_TOKEN env var is read at build time. Must not appear in any committed source file or shipped HTML/JS. |
| External repo data → page DOM | Repo names/descriptions/homepage URLs from the API are rendered as text and href attributes. JSX escapes text content automatically; URLs need scheme validation if attacker-controlled — Axel's own repos are not attacker-controlled, but the homepage field accepts arbitrary user-set URLs (set by Axel himself in repo settings). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | src/lib/projects.ts (GITHUB_TOKEN handling) | mitigate | Token is read only via process.env.GITHUB_TOKEN inside the fetcher. It is never written to disk, never logged, never echoed to console. The Authorization header is constructed in memory and discarded after fetch. The only console.* statement in this plan is the static-string ceiling warn (no token interpolation). Acceptance criteria explicitly check that out/index.html contains no "Bearer " string and no "GITHUB_TOKEN" string. |
| T-03-02 | Tampering | src/components/projects/ProjectCard.tsx (rendering API-supplied text) | mitigate | All text fields (name, description, language) are rendered via JSX text-children, which React escapes by default — no dangerouslySetInnerHTML, no innerHTML. Confirmed by code inspection in acceptance criteria (no dangerouslySetInnerHTML in any new file). |
| T-03-03 | Tampering | src/components/projects/ProjectCard.tsx (homepage href) | mitigate | The homepage field is set by Axel himself in his repo settings — the trust boundary on this URL is the GitHub repo owner, not an attacker. Plan 01 accepts the homepage URL as-is. The repo link href is project.repoUrl which always begins with https://github.com/ (mapped from html_url, controlled by GitHub). External-link rel="noopener noreferrer" prevents window.opener tampering. (Note: Plan 03 may add a defensive scheme allowlist on homepage if the design review surfaces concern; for the MVP slice, homepage is treated as Axel-trusted data.) |
| T-03-04 | Denial of Service | src/lib/projects.ts (no fallback yet) | accept | This plan deliberately defers fallback to Plan 02 per the MVP-mode vertical-slice progression. Build will fail if GitHub API is unreachable. Mitigation is queued for Plan 02 (D-06). Risk is low — GitHub API is highly available, builds run on a schedule with retry, and the consequence is a build failure (not user-facing downtime, since the previous build remains deployed). |
| T-03-SC | Tampering | npm installs | accept | No new packages installed in this plan — all dependencies (clsx, tailwind-merge, next, react) come from Phase 1. No package.json modification. No legitimacy gate triggered. |
</threat_model>

<verification>
- npx tsc --noEmit exits 0
- npm run build exits 0 with network access (a clean rebuild from scratch produces an out/ directory)
- out/index.html contains id="projects", "Selected work on GitHub", and at least one href to https://github.com/axelw/
- out/index.html does NOT contain "Bearer " or "GITHUB_TOKEN" (token-leak smoke test)
- out/index.html does NOT contain any archived-repo data — manual cross-check by listing https://api.github.com/users/axelw/repos and confirming no archived-true repo name appears in out/index.html (full automated verification of this is in Plan 03's e2e suite)
- No new files contain 'use client' except components that legitimately need it (this plan adds NO Client Components — all four new files are Server scope)
- src/lib/projects.ts contains exactly one console.warn (the documented D-04 ceiling warn) and no console.log; Plan 02 adds a second, distinct warn inside its catch block
</verification>

<success_criteria>
- The four new files exist with the contracts specified above; page.tsx is updated to wire Projects between CV and Contact
- Running `next build` (with network access) produces an out/index.html that contains real, fetched, non-archived GitHub repo data rendered as cards with Repo links and conditional Live demo links
- All three Phase 3 requirements (PROJ-01 build-time fetch, PROJ-02 name + description, PROJ-03 conditional homepage link) are observably satisfied in the rendered HTML
- The slice is intentionally minimal — no fallback (Plan 02), no relative date / empty state / hover polish (Plan 03). The first end-to-end thread is alive.
</success_criteria>

<output>
Create `.planning/phases/03-projects/03-01-SUMMARY.md` when done
</output>
