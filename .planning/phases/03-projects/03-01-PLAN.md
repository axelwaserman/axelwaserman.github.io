---
phase: 03-projects
plan: 01
type: execute
wave: 1
revision: 2
revision_notes: "addresses 03-REVIEWS.md findings C-1, C-2, C-3, H-4, H-6, H-7, H-8, M-12, M-13, L-14, L-16"
depends_on: []
files_modified:
  - src/data/projects.ts
  - src/data/projects.json
  - src/lib/projects.ts
  - src/components/ui/FadeUp.tsx
  - src/components/projects/Projects.tsx
  - src/components/projects/ProjectCard.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03
must_haves:
  truths:
    - "D-03: Running `next build` fetches Axel's public, non-archived GitHub repos at build time inside a React Server Component and bakes them into the static HTML — out/index.html contains real repo names, no client-side fetch is performed at runtime"
    - "D-03 + C-2: src/lib/projects.ts is server-only — `import 'server-only'` is the first non-comment line so any future Client-Component import path-fails the build, blocking GITHUB_TOKEN inlining into client chunks"
    - "D-06: Wave 1 ships with the fallback path live (path-a from REVIEW H-4) — try/catch around the fetcher returns the committed src/data/projects.json placeholder when the GitHub call fails, so `npm run build` always succeeds; Wave 2 enriches the fallback with real seed data and adds the forced-401 verification test"
    - "D-07: Each rendered project card displays the repo name, description, primary language, last pushed date (when non-null), repo link, and a live demo link only when the API homepage field is set AND its protocol is http(s) (relative-date utility lands in Plan 03)"
    - "D-08: Star count is deliberately excluded from every rendered card — the Project type has no stars field and ProjectCard never references one"
    - "D-04: All non-archived public repos are fetched via Link-header pagination across pages, capped at 5 pages (500 repos) as a sanity guard with a hard error if exceeded — addresses REVIEW H-6 (D-04 must not be silently violated)"
    - "D-05: Repos render sorted by most recently pushed (pushed_at descending) — the fetcher sorts before mapping to Project[]"
    - "D-01: The card layout is a responsive grid — multi-column on desktop, single-column on mobile — and avoids the generic Tailwind card grid look (intentional hierarchy and editorial surface; hover-state polish lands in Plan 03)"
    - "D-01 + REVIEW H-7: ProjectCard ships with focus-visible:outline-* from the start — keyboard-only focus rings, no plain focus: regression between Wave 1 and Wave 3"
    - "D-02: Projects section renders inside <FadeUp> at #projects, between CV and Contact, matching the header nav order (#hero, #cv, #projects, #contact) and consistent with the FadeUp wrapper used for CV and Contact"
    - "REVIEW C-1: FadeUp.tsx's wrapper <div> emits a stable `data-fadein` attribute so test selectors can hard-target the animated wrapper without falling through to a never-faded ancestor"
    - "REVIEW C-3: homepage URLs are protocol-validated via `new URL(...).protocol` — only http: and https: pass through; javascript:, data:, file:, etc. are coerced to null at fetch time, defeating XSS via attacker-controlled href"
    - "REVIEW H-5: error.message is NEVER logged — the fallback warn uses error.name only, with `::warning::` prefix so GitHub Actions surfaces it in the job summary UI"
    - "REVIEW H-8: pushedAt is `string | null` end-to-end — Project type, JSON fallback, ProjectCard render, and (Plan 03) formatRelativeDate signature all handle null without producing 'Invalid Date' or `1970-01-01`"
  artifacts:
    - path: "src/data/projects.ts"
      provides: "Project type definition (TypeScript interface) — shape of normalized GitHub repo data consumed by ProjectCard"
      exports: ["Project"]
    - path: "src/data/projects.json"
      provides: "Wave 1 placeholder fallback — single seed entry (axelw.github.io) so the build never hard-fails on API errors; Wave 2 enriches with real recent repos"
      exports: []
    - path: "src/lib/projects.ts"
      provides: "fetchProjects() async function — server-only, calls GitHub REST /users/axelw/repos with Link-header pagination at build time, validates URL schemes, filters archived/disabled/fork, sorts by pushed_at desc, falls back to projects.json on error"
      exports: ["fetchProjects"]
    - path: "src/components/ui/FadeUp.tsx"
      provides: "FadeUp wrapper now emits data-fadein on the wrapper <div> — enables hard-targeting from Playwright UAT in Plan 03 (closes REVIEW C-1 from Phase 2)"
      exports: ["default"]
    - path: "src/components/projects/Projects.tsx"
      provides: "Server Component section — calls fetchProjects() at build, renders eyebrow + visible h2 + <ul> grid of ProjectCard"
      exports: ["Projects"]
    - path: "src/components/projects/ProjectCard.tsx"
      provides: "Server Component presentational card — renders name (h3), meta line, optional description, repo link, conditional live-demo link, and optional pushedAt-derived <time> element from a Project prop. Focus-visible outlines from day one."
      exports: ["ProjectCard"]
    - path: "src/app/page.tsx"
      provides: "Root page composing Header, Hero, CV, Projects, Contact inside FadeUp wrappers — Projects sits between CV and Contact"
      exports: ["Home"]
  key_links:
    - from: "src/components/projects/Projects.tsx"
      to: "src/lib/projects.ts"
      via: "import { fetchProjects } from '@/lib/projects'"
      pattern: "fetchProjects"
    - from: "src/components/projects/Projects.tsx"
      to: "src/components/projects/ProjectCard.tsx"
      via: "import ProjectCard from './ProjectCard' inside grid <ul><li>"
      pattern: "ProjectCard"
    - from: "src/components/projects/ProjectCard.tsx"
      to: "src/data/projects.ts"
      via: "import type { Project } from '@/data/projects'"
      pattern: "import type.*Project.*@/data/projects"
    - from: "src/lib/projects.ts"
      to: "https://api.github.com/users/axelw/repos"
      via: "fetch() at build time inside Server Component tree, with Link-header pagination"
      pattern: "api\\.github\\.com/users/.+/repos"
    - from: "src/lib/projects.ts"
      to: "src/data/projects.json"
      via: "import projectsFallback from '@/data/projects.json' inside catch fallback"
      pattern: "from '@/data/projects.json'"
    - from: "src/app/page.tsx"
      to: "src/components/projects/Projects.tsx"
      via: "import Projects from '@/components/projects/Projects' wrapped in <FadeUp>"
      pattern: "<FadeUp>\\s*<Projects"
---

<objective>
Deliver the thinnest end-to-end vertical slice for Phase 3 with build-resilient guarantees: typed Project shape, server-only build-time GitHub fetcher with Link-header pagination AND fallback (per REVIEW H-4 path-a), Server Component section, presentational ProjectCard with focus-visible styling, FadeUp data-fadein attribute (closes REVIEW C-1), URL protocol validation (closes REVIEW C-3), and page wiring. After this plan ships, running `next build` always succeeds — with real GitHub data when the API is reachable, or with the committed src/data/projects.json placeholder otherwise. The user can visit the dev server and see real repo names, descriptions, and a Repo link per card. Conditional Live demo links appear only when the API supplies a valid http(s) homepage.

Purpose: This is the MVP slice for Phase 3, hardened per the cross-AI adversarial review. It satisfies all three Phase 3 requirements (PROJ-01, PROJ-02, PROJ-03) end-to-end AND ships a build that does not depend on network availability — the slice is genuinely vertical because day-one merge does not break developer builds. Plan 02 enriches the placeholder JSON with a real seed snapshot and adds the forced-fallback verification test. Plan 03 adds polish (relative date, hover state, empty state) and the Playwright UAT.

Output: 6 new files (src/data/projects.ts, src/data/projects.json, src/lib/projects.ts, src/components/projects/Projects.tsx, src/components/projects/ProjectCard.tsx) + 2 modified files (src/components/ui/FadeUp.tsx adds data-fadein, src/app/page.tsx wires Projects). Real GitHub data baked into static HTML when reachable, deterministic placeholder otherwise.
</objective>

<execution_context>
@/Users/axel/code/website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/axel/code/website/.planning/ROADMAP.md
@/Users/axel/code/website/.planning/REQUIREMENTS.md
@/Users/axel/code/website/.planning/STATE.md
@/Users/axel/code/website/.planning/phases/03-projects/03-CONTEXT.md
@/Users/axel/code/website/.planning/phases/03-projects/03-PATTERNS.md
@/Users/axel/code/website/.planning/phases/03-projects/03-UI-SPEC.md
@/Users/axel/code/website/.planning/phases/03-projects/03-REVIEWS.md
@/Users/axel/code/website/CLAUDE.md

## Phase Goal (User Story)

**As a** recruiter or curious visitor, **I want to** see Axel's public GitHub projects rendered on the page with names, descriptions, and links, **so that** I can quickly evaluate his real, current work without leaving the site.

<interfaces>
<!-- Existing project interfaces the executor must compose with. Extracted from codebase. -->
<!-- Use these directly — no codebase exploration needed. -->

From src/data/cv.ts (existing pattern to mirror — named-export typed data file):
```typescript
export interface WorkEntry { role: string; company: string; dates: string; description: string }
export interface EducationEntry { degree: string; institution: string; years: string }
export const workEntries: WorkEntry[]
export const educationEntries: EducationEntry[]
export const skills: string[]
```

From src/components/cv/WorkEntry.tsx (presentational pattern to clone for ProjectCard):
```typescript
import type { WorkEntry as WorkEntryType } from '@/data/cv'
interface WorkEntryProps { entry: WorkEntryType }
export default function WorkEntry({ entry }: WorkEntryProps): JSX.Element
```

From src/components/contact/Contact.tsx (visible h2 pattern + external-link styling — copy verbatim except focus → focus-visible per REVIEW H-7):
- Section: className="py-[var(--space-section)] px-6 max-w-4xl scroll-mt-16"
- h2: className="font-[var(--font-heading)] text-[length:var(--text-heading)] mb-4"

From src/components/cv/CV.tsx (eyebrow label pattern):
- Eyebrow: className="text-[length:var(--text-ui)] font-semibold text-[var(--color-muted)] uppercase tracking-[0.08em]"

From src/components/ui/FadeUp.tsx (Wave 1 modifies this file to add data-fadein per REVIEW C-1; the rest of the component is sealed):
```typescript
interface FadeUpProps { children: React.ReactNode; className?: string }
export default function FadeUp({ children, className }: FadeUpProps): JSX.Element
// Render returns <div ref={ref} data-fadein className={className}>{children}</div>
```

From src/components/header/Header.tsx (anchor target already wired — section needs id="projects"):
- nav <a href="#projects">Projects</a> exists at line 45

From src/styles/tokens.css (existing tokens — no new tokens needed):
- --color-surface, --color-text, --color-accent, --color-muted
- --text-ui, --text-body, --text-heading
- --font-body, --font-heading
- --space-section, --radius-card

From tsconfig.json:
- "@/*" path alias maps to "./src/*"
- "resolveJsonModule": true (used by the fallback JSON import)
- "strict": true (no any allowed)

From npm:
- The `server-only` package is included with Next.js 16 and is installable via `npm install server-only` if not already a transitive dep (it is shipped by Next 16 and resolves at the workspace level — no version pin needed). Importing it from a Client Component throws a build error.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Project type, server-only fetcher with pagination + fallback, placeholder JSON, and FadeUp data-fadein attribute</name>
  <files>src/data/projects.ts, src/data/projects.json, src/lib/projects.ts, src/components/ui/FadeUp.tsx</files>
  <read_first>
    - .planning/phases/03-projects/03-PATTERNS.md — full patterns for src/data/projects.ts (Project interface) and src/lib/projects.ts (fetchProjects shape, header pattern, error handling)
    - .planning/phases/03-projects/03-UI-SPEC.md — Component Inventory row "Project (type)" specifying shape: { name, description: string | null, language: string | null, pushedAt: string | null, repoUrl, homepage: string | null }
    - .planning/phases/03-projects/03-CONTEXT.md — D-03 (build-time fetch in Server Component), D-04 (all non-archived public repos), D-05 (sort by pushed_at desc), D-06 (fallback on error), D-08 (star count excluded)
    - .planning/phases/03-projects/03-REVIEWS.md — CRITICAL findings C-2 (server-only), C-3 (URL protocol validation), HIGH findings H-4 (Wave 1 must include fallback), H-5 (sanitized warn), H-6 (Link-header pagination), H-8 (pushedAt nullable), L-16 (centralize username constant), MEDIUM M-12 (drop the false build-cache claim)
    - src/data/cv.ts — confirm the named-export typed data convention used in this project (no default export, types exported alongside data)
    - src/components/ui/FadeUp.tsx — current implementation; the only modification is adding `data-fadein` to the rendered wrapper <div>
    - CLAUDE.md — Technology Stack section: native fetch only (no @octokit/rest), GitHub REST API, GITHUB_TOKEN auth optional
  </read_first>
  <action>
    Step 1 — Modify src/components/ui/FadeUp.tsx (per REVIEW C-1):

    The only change is to the JSX return: add a literal `data-fadein` attribute to the wrapper <div>. The new return is:

        return (
          <div ref={ref} data-fadein className={className}>
            {children}
          </div>
        )

    Do NOT touch the 'use client' directive, the hooks, the IntersectionObserver logic, the reduced-motion branch, the prop signature, or anything else. The attribute is a static string presence (no value needed) — Playwright selectors will use `[data-fadein]` to bind to this exact wrapper, removing the broken `?? el.parentElement` fallback in Phase 2's UAT pattern.

    Step 2 — Create src/data/projects.ts (typed-data file, named exports only).

    Define and export the Project interface with exactly these fields (camelCase per TypeScript convention; nullable fields explicitly typed; pushedAt is now `string | null` per REVIEW H-8 because GitHub returns null for repos with no pushes):
    - name: string
    - description: string | null
    - language: string | null
    - pushedAt: string | null    // ISO 8601 OR null (REVIEW H-8)
    - repoUrl: string
    - homepage: string | null

    Use `interface`. No `any`. No default export. No value exports yet.

    Step 3 — Create src/data/projects.json (Wave 1 placeholder per REVIEW H-4 path-a).

    A JSON array containing exactly ONE entry — the axelw.github.io repo — so the catch fallback returns valid data without requiring Wave 2 to land first. Wave 2 enriches the file with 4–7 additional real recent repos.

    The single entry MUST be:
    ```
    [
      {
        "name": "axelw.github.io",
        "description": "Personal website — single-page CV and GitHub projects, statically exported via Next.js 16.",
        "language": "TypeScript",
        "pushedAt": "2026-06-02T00:00:00Z",
        "repoUrl": "https://github.com/axelw/axelw.github.io",
        "homepage": null
      }
    ]
    ```
    (Use `null` for homepage at this stage — Wave 2 may flip to "https://axelw.github.io" once deploy lands.)

    File format: 2-space indent, trailing newline. No comments (JSON does not support them).

    Step 4 — Create src/lib/ directory and src/lib/projects.ts.

    Line 1 of the file MUST be: `import 'server-only'` (no semicolon variation matters for the grep gate; we will gate on `import 'server-only'` exact substring). This is REVIEW C-2: importing this module from any Client Component will fail the Next.js build, blocking GITHUB_TOKEN inlining.

    File header (after the server-only import): a short comment noting the file is consumed only by Server Components at build time, never shipped to the client.

    Top-level constants:
    - `const GITHUB_USERNAME = process.env.GITHUB_USERNAME ?? 'axelw'` (REVIEW L-16 — centralize the username)
    - `const PER_PAGE = 100`
    - `const MAX_PAGES = 5` (sanity guard for pagination — 500 repos)

    Type-only import the Project interface: `import type { Project } from '@/data/projects'`.

    Value import the placeholder JSON: `import projectsFallback from '@/data/projects.json'`.

    Define a private internal interface `GitHubRepo` describing the GitHub REST fields this fetcher reads (snake_case to match the API): name, description, language, pushed_at (typed `string | null` per REVIEW H-8), html_url, homepage, archived, fork, disabled. Do not export.

    Implementation contract for `export async function fetchProjects(): Promise<Project[]>`:

    Wrap the entire fetcher body in `try { ... } catch (error: unknown) { ... }`. The catch block MUST run on ANY throw path (network error, !res.ok, malformed JSON, narrowing failure, exceeded MAX_PAGES) — that is path-a from REVIEW H-4.

    --- Inside the try block ---

    1. Helper: `function isHttpUrl(raw: unknown): raw is string` — accepts a value, returns true if it is a non-empty string AND `new URL(raw).protocol` is `'http:'` or `'https:'`. The implementation MUST use a try/catch around `new URL(...)` to handle malformed strings and return false on throw. This is the REVIEW C-3 defense; the symbol `new URL(` MUST appear in the source and the protocol check MUST be present.

       Exact implementation:
       ```
       function isHttpUrl(raw: unknown): raw is string {
         if (typeof raw !== 'string' || raw.length === 0) return false
         try {
           const u = new URL(raw)
           return u.protocol === 'http:' || u.protocol === 'https:'
         } catch {
           return false
         }
       }
       ```

    2. Helper: `function parseLinkNext(linkHeader: string | null): string | null` — parses an RFC 5988 Link header and returns the URL with `rel="next"`, or null. Inline minimal implementation (no dependencies):
       ```
       function parseLinkNext(linkHeader: string | null): string | null {
         if (!linkHeader) return null
         for (const part of linkHeader.split(',')) {
           const m = part.match(/<([^>]+)>;\s*rel="next"/)
           if (m) return m[1]
         }
         return null
       }
       ```
       The grep gate scans for the symbol `parseLinkNext` and the substring `rel="next"` in the source.

    3. Build a Headers object with: `Accept: application/vnd.github+json`, `User-Agent: ${GITHUB_USERNAME}-website-build`, `X-GitHub-Api-Version: 2022-11-28`, and conditionally `Authorization: Bearer ${process.env.GITHUB_TOKEN}` only when `process.env.GITHUB_TOKEN` is a non-empty string. Read via `process.env.GITHUB_TOKEN`. (No NEXT_PUBLIC_ prefix — server-only.)

    4. Initial endpoint: `let nextUrl: string | null = `https://api.github.com/users/${GITHUB_USERNAME}/repos?per_page=${PER_PAGE}&type=owner&sort=pushed&direction=desc``.

    5. Pagination loop (REVIEW H-6):
       ```
       const rawList: GitHubRepo[] = []
       let pageCount = 0
       while (nextUrl) {
         if (pageCount >= MAX_PAGES) {
           throw new Error(`fetchProjects: exceeded MAX_PAGES (${MAX_PAGES}) — implement deeper pagination if axelw exceeds 500 repos`)
         }
         const res = await fetch(nextUrl, { headers })
         if (!res.ok) throw new Error(`GitHub API ${res.status}`)
         const body: unknown = await res.json()
         if (!Array.isArray(body)) throw new Error('GitHub API: expected array body')
         for (const entry of body) {
           // narrow each entry: name/html_url/archived must be present, pushed_at may be null per REVIEW H-8
           if (entry === null || typeof entry !== 'object') continue
           const e = entry as Record<string, unknown>
           if (typeof e.name !== 'string' || typeof e.html_url !== 'string' || typeof e.archived !== 'boolean') continue
           rawList.push(entry as GitHubRepo)
         }
         nextUrl = parseLinkNext(res.headers.get('link'))
         pageCount++
       }
       ```

       Build-time fetch — runs once per build, no across-build caching needed. Per REVIEW M-12, do NOT add a comment claiming "Next.js caches build-time fetches by default" (that claim is incorrect for cross-build behavior). The single line above is the comment we ship.

    6. Filter: keep only entries where `archived === false` AND `disabled === false` AND `fork === false`. (REVIEW L-14 — fork filter is intentional; documented in the acceptance criteria.)

    7. Sort the filtered array by `pushed_at` descending. Treat `pushed_at: null` as the smallest value (sorts to the end). Implementation: `(a, b) => (b.pushed_at ?? '').localeCompare(a.pushed_at ?? '')`.

    8. Map each filtered/sorted GitHubRepo into a Project:
       - name: raw.name
       - description: typeof raw.description === 'string' && raw.description.length > 0 ? raw.description : null
       - language: typeof raw.language === 'string' && raw.language.length > 0 ? raw.language : null
       - pushedAt: typeof raw.pushed_at === 'string' && raw.pushed_at.length > 0 ? raw.pushed_at : null   // REVIEW H-8
       - repoUrl: raw.html_url
       - homepage: isHttpUrl(raw.homepage) ? raw.homepage : null   // REVIEW C-3

    9. Return the mapped Project[].

    --- Inside the catch block ---

    1. Add an eslint-disable line directly above the warn: `// eslint-disable-next-line no-console -- intentional build-time warning per CTX D-06; this code only runs during `next build`, never in the client bundle`

    2. The warn line MUST use only `error.name` (REVIEW H-5 — no message, no stack, no request frames):
       ```
       console.warn(
         '::warning::[projects] GitHub fetch failed, using src/data/projects.json fallback. Reason:',
         error instanceof Error ? error.name : 'unknown'
       )
       ```
       The `::warning::` prefix is REVIEW H-6 / D-06 surfacing — GitHub Actions parses this prefix and renders the line in the job summary UI. The grep gate will check that `::warning::` appears in src/lib/projects.ts and that no `console.warn` call references `.message`.

    3. Return the fallback dataset cast to Project[]:
       ```
       return projectsFallback as unknown as Project[]
       ```
       The double cast through `unknown` is required because the JSON literal's inferred type does not structurally line up with `Project[]` for nullable fields. Wave 2's task hardens this by adding more entries; the type-shape contract is enforced by Wave 1's acceptance criteria below.

    --- Constraints across the file ---

    - No `console.log`. No `console.error`. EXACTLY one `console.warn` (the catch-block fallback warn). The Wave-1 ceiling check from the previous revision is removed — pagination subsumes it.
    - No `any` anywhere. Use `unknown` at the JSON-parse boundary and narrow.
    - No 'use client' directive. The `import 'server-only'` line is the load-bearing guard.
    - All references to the username use the `GITHUB_USERNAME` constant (REVIEW L-16) — grep `axelw` should appear ONLY inside the constant declaration line and the placeholder fallback path; no other inline string literal contains `axelw`.

    GITHUB_TOKEN handling — security: token is read from process.env at build time only. It MUST NOT be inlined into any committed source string, logged, or echoed to console. The Authorization header is constructed in memory, used by fetch, and discarded. The `import 'server-only'` line is the structural guarantee that no Client Component can ever import this file.
  </action>
  <verify>
    <automated>cd /Users/axel/code/website &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/ui/FadeUp.tsx still has 'use client' (regression check) AND the wrapper div now includes `data-fadein` (grep -c "data-fadein" src/components/ui/FadeUp.tsx returns at least 1)
    - File src/data/projects.ts exists
    - src/data/projects.ts exports an interface named Project (grep -c "export interface Project" src/data/projects.ts returns 1)
    - Project interface contains exactly these field names: name, description, language, pushedAt, repoUrl, homepage (grep "pushedAt" and "repoUrl" present; no "pushed_at" or "html_url" in the type)
    - Project.pushedAt is typed `string | null` per REVIEW H-8 (grep -E "pushedAt:\\s*string \\| null" src/data/projects.ts returns 1)
    - description, language, homepage are typed as `string | null` (grep "string | null" appears at least 4 times in src/data/projects.ts — description, language, pushedAt, homepage)
    - src/data/projects.ts has no default export (grep -c "export default" src/data/projects.ts returns 0)
    - File src/data/projects.json exists, is valid JSON (node -e 'JSON.parse(require("fs").readFileSync("src/data/projects.json","utf-8"))' exits 0)
    - src/data/projects.json contains at least 1 entry with name "axelw.github.io" (grep -c "axelw.github.io" src/data/projects.json returns at least 2 — name + repoUrl)
    - File src/lib/projects.ts exists
    - **REVIEW C-2:** First non-comment, non-blank line of src/lib/projects.ts is `import 'server-only'` (run: `head -1 src/lib/projects.ts | grep -q "^import 'server-only'"` exits 0)
    - src/lib/projects.ts exports an async function named fetchProjects with return type Promise&lt;Project[]&gt; (grep "export async function fetchProjects" src/lib/projects.ts returns a match)
    - src/lib/projects.ts imports Project as a type-only import (grep "import type { Project }" src/lib/projects.ts returns 1)
    - src/lib/projects.ts imports the JSON fallback (grep -c "from '@/data/projects.json'" src/lib/projects.ts returns 1)
    - **REVIEW L-16:** src/lib/projects.ts declares a `GITHUB_USERNAME` constant; the literal string `axelw` appears at most twice in the file — once in the constant default fallback and at most once via interpolation in error messages (grep -c "'axelw'" src/lib/projects.ts returns at most 2; verify visually that no inline URL like `https://api.github.com/users/axelw/repos` exists — must be interpolated via `${GITHUB_USERNAME}`)
    - src/lib/projects.ts URL is built via template interpolation containing `${GITHUB_USERNAME}` and `per_page=${PER_PAGE}` (grep '${GITHUB_USERNAME}' returns at least 1; grep "per_page=" returns at least 1)
    - src/lib/projects.ts sets the User-Agent header (grep "User-Agent" returns at least 1 match)
    - src/lib/projects.ts conditionally sets Authorization only when process.env.GITHUB_TOKEN is set (grep "process.env.GITHUB_TOKEN" appears; grep for the literal token-prefix `Bearer ${`)
    - **REVIEW C-3:** src/lib/projects.ts contains a URL protocol allowlist using `new URL(` AND a check on `protocol` (grep -c "new URL(" src/lib/projects.ts returns at least 1; grep -E "protocol\\s*===?\\s*'https?:'" src/lib/projects.ts returns at least 2 — http and https)
    - **REVIEW C-3:** src/lib/projects.ts defines an `isHttpUrl` helper used in the homepage mapping (grep "isHttpUrl" returns at least 2 — definition + usage)
    - **REVIEW H-6:** src/lib/projects.ts implements Link-header pagination (grep "parseLinkNext" returns at least 2; grep 'rel="next"' returns at least 1; grep "MAX_PAGES" returns at least 2)
    - src/lib/projects.ts filters archived === false AND disabled === false AND fork === false (grep "archived" and "disabled" and "fork" all return matches; **REVIEW L-14** documented inline)
    - **REVIEW H-4 path-a:** src/lib/projects.ts wraps the entire fetcher body in a try/catch (grep -c "try {" returns at least 1; grep "catch (error: unknown)" returns 1)
    - **REVIEW H-4 path-a:** the catch block returns the JSON fallback (grep -E "return projectsFallback as unknown as Project\\[\\]" returns at least 1, OR `as Project[]` appears within 5 lines of `return projectsFallback`)
    - **REVIEW H-5:** src/lib/projects.ts contains exactly one console.warn (grep -c "console.warn" returns 1) and zero console.log (grep -c "console.log" returns 0) and zero console.error (grep -c "console.error" returns 0)
    - **REVIEW H-5:** src/lib/projects.ts NEVER references `.message` from an error in any console.* call (run: `grep -E "console\\.[a-z]+\\([^)]*\\.message" src/lib/projects.ts` returns nothing — zero matches)
    - **REVIEW H-5:** the catch warn references `error.name` (grep "error.name" returns at least 1 match) within 5 lines of `console.warn`
    - **REVIEW H-6:** the warn message is prefixed with `::warning::` (grep "::warning::" returns at least 1)
    - src/lib/projects.ts does NOT have 'use client' (grep -c "use client" returns 0)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>FadeUp.tsx emits data-fadein on the wrapper div; Project type is defined with pushedAt: string | null and the other nullable fields; projects.json placeholder exists with 1 entry; fetchProjects starts with `import 'server-only'`, uses a centralized GITHUB_USERNAME, paginates via Link headers, validates URL schemes via new URL().protocol, wraps the body in try/catch, falls back to projects.json on any error, logs a single ::warning:: prefixed console.warn using only error.name (no .message); tsc exits 0</done>
</task>

<task type="auto">
  <name>Task 2: Build ProjectCard and Projects Server Components — focus-visible from day one, nullable pushedAt rendering</name>
  <files>src/components/projects/Projects.tsx, src/components/projects/ProjectCard.tsx</files>
  <read_first>
    - .planning/phases/03-projects/03-PATTERNS.md — full patterns for ProjectCard.tsx and Projects.tsx
    - .planning/phases/03-projects/03-UI-SPEC.md — Component Inventory, Spacing Scale, Typography, Color, Copywriting (eyebrow "Projects" UPPERCASE; visible h2 "Selected work on GitHub"; repo link label "Repo →"; live demo label "Live demo →"; description omitted entirely if null/empty/whitespace)
    - .planning/phases/03-projects/03-CONTEXT.md — D-01 (responsive card grid, anti-template), D-02 (FadeUp wrapping handled in page.tsx Task 3), D-07 (each card: name, description, language, last-pushed date when non-null, repo link, optional live demo)
    - .planning/phases/03-projects/03-REVIEWS.md — H-7 (focus-visible from Wave 1), H-8 (pushedAt may be null — render conditionally), M-13 (whitespace-only description must not render an empty <p>)
    - src/components/cv/WorkEntry.tsx — exact analog for the presentational card; copy structure verbatim, then add card surface treatment and link row
    - src/components/contact/Contact.tsx — section shell pattern
    - src/components/cv/CV.tsx — eyebrow label className pattern (lines 17-19); section shell className pattern (lines 8-12)
  </read_first>
  <action>
    Create src/components/projects/ directory.

    --- src/components/projects/ProjectCard.tsx (Server Component, no 'use client') ---

    Type-only import the Project interface: `import type { Project } from '@/data/projects'`.

    Define ProjectCardProps interface: `{ project: Project }`.

    Default export ProjectCard({ project }: ProjectCardProps).

    Plan 01 scope note: this plan does NOT use the relative-date utility (introduced in Plan 03). The meta line shows `language` only when non-null. Plan 03 adds the relative-date polish to the meta line. The pushedAt-derived <time> element IS rendered conditionally in this plan when pushedAt is non-null (the dateTime attribute proves the type contract end-to-end), but the visible text in this plan is just the raw ISO substring (Plan 03 replaces it with formatRelativeDate output).

    Render <article> with className `p-6 rounded-[var(--radius-card)] border border-[var(--color-muted)]/20`:

    1. <h3> with className `text-[length:var(--text-body)] font-semibold mb-1` containing `{project.name}` verbatim.

    2. Meta line — render only when `project.language` is non-null:
       <p className="text-[length:var(--text-ui)] text-[var(--color-muted)] mb-3">{project.language}</p>

    3. **REVIEW H-8:** PushedAt time element — render only when `project.pushedAt !== null`:
       Inside the meta paragraph above, OR (if language is null) as a standalone tiny meta paragraph, render:
       `{project.pushedAt && <time dateTime={project.pushedAt}>{project.pushedAt.slice(0, 10)}</time>}`
       — Wave-1 visible text is the raw YYYY-MM-DD; Plan 03 replaces it with `formatRelativeDate(project.pushedAt)` and combines with language via the U+00B7 middle dot.
       The `<time>` element's `dateTime` attribute is the full ISO string. If pushedAt is null, no <time> element is emitted at all (no orphan element, no Invalid Date).
       Implementation note for Wave 1: keep this minimal — wrap language + optional time in the same <p> with `{project.language}{project.language && project.pushedAt && ' · '}{project.pushedAt && <time dateTime={project.pushedAt}>{project.pushedAt.slice(0, 10)}</time>}` so Plan 03 only needs to swap `slice(0,10)` for `formatRelativeDate(...)`. If both language and pushedAt are null, omit the <p> entirely.

    4. **REVIEW M-13:** Description — render only when `project.description?.trim()` is truthy (this single character change avoids whitespace-only descriptions producing empty <p> tags with vertical margin):
       `{project.description?.trim() && <p className="text-[length:var(--text-body)] mb-6 max-w-[55ch]">{project.description}</p>}`
       Omit the entire <p> when description is null, empty, or whitespace-only. No placeholder dash, no "No description".

    5. Link row <div className="flex flex-wrap gap-4">:
       - Repo link: anchor with href={project.repoUrl}, target="_blank", rel="noopener noreferrer", aria-label={`View ${project.name} repository on GitHub`}, className=
         **`text-[length:var(--text-ui)] text-[var(--color-text)] underline underline-offset-2 decoration-[var(--color-muted)] hover:decoration-[var(--color-accent)] hover:text-[var(--color-accent)] focus-visible:outline-2 focus-visible:outline-[var(--color-accent)] focus-visible:outline-offset-2 py-2`**
         (REVIEW H-7 — focus-visible from day one; NO plain `focus:outline-*`).
         Inside the link: "Repo " then a separate <span aria-hidden="true">→</span>.
       - Live demo link, rendered conditionally with `{project.homepage && (...)}`: same className (with focus-visible:) and rel/target attributes; href={project.homepage}; aria-label={`View ${project.name} live demo`}; inner content "Live demo " + <span aria-hidden="true">→</span>.

    The `→` glyph is wrapped in `aria-hidden="true"` per UI-SPEC §Accessibility.

    No 'use client' directive. No imports of the relative-date utility (Plan 03). No card-hover edge highlight in this slice (Plan 03 adds the transition + accent border swap).

    --- src/components/projects/Projects.tsx (Server Component, async) ---

    Imports:
    - `import { fetchProjects } from '@/lib/projects'`
    - `import ProjectCard from './ProjectCard'`

    Default export: `export default async function Projects()`.

    Function body: `const projects = await fetchProjects()`.

    Plan 01 scope note: this plan does NOT include an empty-state component (introduced in Plan 03). If projects.length is 0 the grid simply renders no children — Plan 03 adds ProjectsEmptyState and the conditional. Because Wave 1 ships the JSON fallback (REVIEW H-4 path-a), projects.length >= 1 is guaranteed end-to-end.

    Return JSX:
    <section id="projects" aria-labelledby="projects-heading" className="py-[var(--space-section)] px-6 max-w-4xl scroll-mt-16">

    1. Eyebrow label div: className="text-[length:var(--text-ui)] font-semibold text-[var(--color-muted)] uppercase tracking-[0.08em] mb-4" with text "Projects" (verbatim from UI-SPEC §Copywriting).

    2. Visible h2 with id="projects-heading", className="font-[var(--font-heading)] text-[length:var(--text-heading)] mb-8", text "Selected work on GitHub" (verbatim from UI-SPEC §Copywriting).

    3. <ul className="grid grid-cols-1 sm:grid-cols-2 gap-6"> wrapping projects.map. Each item: <li key={p.repoUrl}><ProjectCard project={p} /></li>. The key uses p.repoUrl because it is unique per repo and stable across builds.

    No 'use client'. No useReducedMotion. The async function is valid in Next 16 App Router; FadeUp wraps this section in page.tsx (Task 3).
  </action>
  <verify>
    <automated>cd /Users/axel/code/website &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/projects/ProjectCard.tsx exists
    - ProjectCard.tsx does NOT contain 'use client' (grep -c "use client" returns 0)
    - ProjectCard.tsx imports Project as a type-only import (grep "import type { Project }" returns 1)
    - ProjectCard.tsx renders an &lt;article&gt; element
    - ProjectCard.tsx contains the conditional pattern for homepage (grep "project.homepage &amp;&amp;" returns at least 1 match)
    - **REVIEW M-13:** ProjectCard.tsx contains the trim guard for description (grep -E "project\\.description\\?\\.trim\\(\\)" src/components/projects/ProjectCard.tsx returns at least 1)
    - **REVIEW H-8:** ProjectCard.tsx conditionally renders a &lt;time&gt; element only when project.pushedAt is non-null (grep "project.pushedAt &amp;&amp;" appears at least 1 in JSX position; grep "&lt;time" returns at least 1; grep "dateTime={project.pushedAt}" returns 1)
    - ProjectCard.tsx Repo link sets target="_blank" AND rel="noopener noreferrer" (grep both attributes)
    - ProjectCard.tsx Repo link sets aria-label containing the literal phrase "repository on GitHub" (grep returns 1)
    - ProjectCard.tsx Live demo link sets aria-label containing the literal phrase "live demo" (grep returns 1)
    - ProjectCard.tsx contains aria-hidden="true" on the arrow span (grep returns at least 2 matches: one per link)
    - **REVIEW H-7:** ProjectCard.tsx uses focus-visible: from Wave 1 (grep -c "focus-visible:outline" src/components/projects/ProjectCard.tsx returns at least 2 — once for Repo link, once for Live demo link)
    - **REVIEW H-7:** ProjectCard.tsx contains NO plain `focus:outline-*` class (run: `grep -E "(^|[\"\\s])focus:outline" src/components/projects/ProjectCard.tsx` returns nothing)
    - File src/components/projects/Projects.tsx exists
    - Projects.tsx does NOT contain 'use client' (grep -c "use client" returns 0)
    - Projects.tsx is declared async (grep "async function Projects" returns 1)
    - Projects.tsx imports fetchProjects from '@/lib/projects' (grep returns 1)
    - Projects.tsx imports ProjectCard from './ProjectCard' (grep returns 1)
    - Projects.tsx &lt;section&gt; has id="projects" (grep id=\"projects\" returns 1)
    - Projects.tsx &lt;section&gt; has aria-labelledby="projects-heading" (grep returns 1)
    - Projects.tsx contains scroll-mt-16 in the section className (grep returns 1)
    - Projects.tsx contains the literal text "Selected work on GitHub" (grep returns 1)
    - Projects.tsx contains the literal text "Projects" inside the eyebrow div (grep returns at least 1)
    - Projects.tsx wraps the map output in a &lt;ul&gt; with grid-cols-1 sm:grid-cols-2 gap-6 classes (grep "grid-cols-1 sm:grid-cols-2" returns 1)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>ProjectCard renders name + optional language/pushedAt meta + optional description (with trim guard) + Repo link + optional Live demo link with focus-visible outlines and correct accessibility/external-link attributes; Projects async Server Component fetches and renders the grid; tsc exits 0</done>
</task>

<task type="auto">
  <name>Task 3: Wire Projects into page.tsx between CV and Contact, verify build hardening end-to-end</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - .planning/phases/03-projects/03-PATTERNS.md — section "src/app/page.tsx (modified)"
    - .planning/phases/03-projects/03-CONTEXT.md — D-02 (Projects wrapped in existing FadeUp component, consistent with CV/Contact)
    - .planning/phases/03-projects/03-REVIEWS.md — C-2 (zero token leak in client chunks), H-4 (Wave 1 must build clean without the API)
    - src/app/page.tsx — current composition before modification
    - src/components/header/Header.tsx — confirms nav order is hero → cv → projects → contact, so visible page order must match
  </read_first>
  <action>
    Modify src/app/page.tsx by adding a single new import and a single new wrapped section between CV and Contact.

    1. Add import statement: `import Projects from '@/components/projects/Projects'`. Place it alphabetically near the other component imports, between CV and Contact imports.
    2. Inside <main>, between the existing `<FadeUp><CV /></FadeUp>` and `<FadeUp><Contact /></FadeUp>` wrappers, insert `<FadeUp><Projects /></FadeUp>`.
    3. Remove the `{/* Projects section — Phase 3 */}` comment if present.

    Resulting visible order: Hero → CV → Projects → Contact. Matches the header nav order.

    Do NOT add 'use client' to page.tsx. Do NOT modify FadeUp.tsx in this task (FadeUp was modified in Task 1).

    After saving, the executor MUST run two verifications:

    A) `npm run build` — must exit 0. Because Wave 1 ships the JSON fallback (REVIEW H-4 path-a), this MUST succeed even when the GitHub API is unreachable or rate-limited; the catch block fires and returns the placeholder.

    B) **REVIEW C-2 — token leak surface scan in client chunks:** Run the command in the verify block and assert that NO file under `out/_next/static/` contains the literal substring `GITHUB_TOKEN` or `Bearer `. The grep targets ALL files in that subtree (JS chunks, source maps, anything bundled), not just out/index.html. The scan MUST return zero matches.
  </action>
  <verify>
    <automated>cd /Users/axel/code/website &amp;&amp; npm run build 2>&amp;1 | tail -20 &amp;&amp; echo '--- token leak scan (REVIEW C-2) ---' &amp;&amp; (! grep -rE "GITHUB_TOKEN|Bearer " out/_next/static/ 2&gt;/dev/null) &amp;&amp; echo 'OK: no token strings in client chunks' &amp;&amp; echo '--- index.html token scan ---' &amp;&amp; (! grep -E "GITHUB_TOKEN|Bearer " out/index.html) &amp;&amp; echo 'OK: no token strings in index.html'</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx imports Projects from '@/components/projects/Projects' (grep "import Projects from '@/components/projects/Projects'" returns 1)
    - src/app/page.tsx contains a FadeUp-wrapped Projects element (grep -E "&lt;FadeUp&gt;\\s*&lt;Projects" or its multi-line equivalent matches; verify by visual inspection of the diff)
    - src/app/page.tsx does NOT contain the literal stub comment "Projects section — Phase 3" (grep -c "Projects section — Phase 3" returns 0)
    - src/app/page.tsx does NOT contain 'use client' (grep -c "use client" returns 0)
    - The visible JSX order in page.tsx is: &lt;Hero /&gt; appears before &lt;CV /&gt; appears before &lt;Projects /&gt; appears before &lt;Contact /&gt; (verify by reading the file — line numbers ascending in this order)
    - **REVIEW H-4 path-a:** `npm run build` exits 0 BOTH with network access (real fetch) AND without (the catch path activates and returns projects.json placeholder; the build still produces an out/ directory with the Projects section rendered)
    - The generated out/index.html contains id="projects" (grep -c 'id="projects"' out/index.html returns at least 1)
    - The generated out/index.html contains the literal text "Selected work on GitHub" (grep -c "Selected work on GitHub" out/index.html returns at least 1)
    - The generated out/index.html contains at least one href starting with "https://github.com/axelw/" (grep -E 'href="https://github.com/axelw/[^"]+"' out/index.html returns at least 1 match — real fetched data OR the placeholder fallback both satisfy)
    - **REVIEW C-2 (HARD GATE):** `grep -rE "GITHUB_TOKEN|Bearer " out/_next/static/ 2&gt;/dev/null` returns NOTHING (zero matches across all built JS/CSS/map chunks)
    - The generated out/index.html does NOT contain "Bearer " or "GITHUB_TOKEN" (grep -c each returns 0 — defense in depth)
  </acceptance_criteria>
  <done>page.tsx composes Header + Hero + FadeUp(CV) + FadeUp(Projects) + FadeUp(Contact); npm run build exits 0 even without network (fallback path proven by REVIEW H-4 path-a); out/index.html contains the Projects section anchor; token-leak scan across out/_next/static/ AND out/index.html returns zero matches (REVIEW C-2 hard gate)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build host → GitHub REST API | Untrusted external response crosses into our build pipeline. JSON fields (name, description, homepage) become rendered HTML attributes and link targets. |
| Build environment → committed source | GITHUB_TOKEN env var is read at build time. Must not appear in any committed source file, shipped HTML, or shipped JS chunk. |
| External repo data → page DOM | Repo names/descriptions/homepage URLs from the API are rendered as text and href attributes. JSX escapes text content automatically; URL `href` attributes accept arbitrary schemes — `javascript:` execution on click is NOT prevented by `target="_blank" rel="noopener noreferrer"` and MUST be blocked by source-side scheme allowlisting. |
| Server-only module → client bundle | Without explicit guards, a future `import { fetchProjects } from '@/lib/projects'` from a Client Component would inline `process.env.GITHUB_TOKEN` into the client bundle. The `import 'server-only'` guard structurally blocks this. |
| Build log → CI surface | A single intentional console.warn appears in build logs on the fallback path. Must not contain secrets and must surface visibly via the `::warning::` GitHub Actions prefix. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | src/lib/projects.ts (GITHUB_TOKEN handling) | mitigate | Token is read only via process.env.GITHUB_TOKEN inside the fetcher. The Authorization header is constructed in memory and discarded after fetch. The only console.* statement in the file is the catch-block fallback warn, which references `error.name` only (REVIEW H-5). Never logs `.message`, `.stack`, or any request frame. |
| T-03-01b (REVIEW C-2) | Information Disclosure | src/lib/projects.ts → client bundle | mitigate | First non-comment line is `import 'server-only'`. Importing this module from a Client Component triggers a Next.js build error, structurally preventing `process.env.GITHUB_TOKEN` from being inlined into client chunks. Acceptance criterion `head -1 src/lib/projects.ts \| grep -q "^import 'server-only'"` enforces this. The Task 3 verify runs a recursive grep across `out/_next/static/` for `GITHUB_TOKEN` and `Bearer ` — any match is a hard build failure. |
| T-03-02 | Tampering | src/components/projects/ProjectCard.tsx (rendering API-supplied text) | mitigate | All text fields (name, description, language) are rendered via JSX text-children, which React escapes by default — no dangerouslySetInnerHTML, no innerHTML. The pushedAt value is rendered inside a `<time dateTime={...}>` element — both the attribute and the text-child are escaped. |
| T-03-03 (REVIEW C-3) | Tampering / XSS | src/lib/projects.ts (homepage href scheme) | mitigate | The previous claim that "`javascript:` URLs in `<a href>` do not execute when `target="_blank" rel="noopener noreferrer"` is set" is FALSE. `noopener noreferrer` blocks window.opener tampering after navigation; it does not block javascript-scheme execution on click. Defense: the `isHttpUrl` helper validates `new URL(homepage).protocol` against an allowlist of `'http:'` and `'https:'` — anything else is coerced to null at fetch time, and ProjectCard's `{project.homepage && ...}` guard hides the link entirely. The repo link href is `html_url` from the GitHub API (always `https://github.com/...`) — server-issued, no validation needed. |
| T-03-04 | Denial of Service | src/lib/projects.ts (build success on API outage) | mitigate | REVIEW H-4 path-a: Wave 1 ships try/catch + the JSON fallback inline. If the GitHub API is unreachable (network error, 4xx/5xx, malformed body, MAX_PAGES exceeded), the build still completes by returning `projectsFallback as Project[]`. Wave 2 enriches the fallback dataset and adds the forced-401 verification test. |
| T-03-05 (REVIEW H-5) | Information Disclosure | src/lib/projects.ts catch block (build log warn) | mitigate | The warn message format is `::warning::[projects] GitHub fetch failed, using src/data/projects.json fallback. Reason: <error.name>`. `error.name` is one of a small set of fixed strings (`Error`, `TypeError`, `SyntaxError`, etc.) — it CANNOT include URLs, headers, or request frames. The `error.message` field is intentionally never serialized. Acceptance criterion `grep -E "console\\.[a-z]+\\([^)]*\\.message"` returns zero. The `::warning::` prefix surfaces the fallback in the GitHub Actions job summary UI per REVIEW H-6 D-06 surfacing. |
| T-03-06 | Tampering | src/data/projects.json (committed fallback) | mitigate | The file is committed via normal git review. The same XSS protections apply at render time as for live API data: ProjectCard renders all text via JSX text-children (auto-escaped), and the same `isHttpUrl` validation runs on homepage values when the fetch path returns Project[] (Wave 1's fallback path returns the JSON as-is, so the JSON's homepage values must already be http(s) — Wave 1's seed entry has `homepage: null`, satisfying this trivially). |
| T-03-07 | Pagination integrity | src/lib/projects.ts pagination loop | mitigate | The loop is bounded by `MAX_PAGES = 5` (500 repos). Exceeding this throws a controlled error that lands in the same catch block, falling back to the JSON. Future overrun is loud (a non-zero exit on the success path would fire the warn). Link-header parsing is a one-line regex against `rel="next"`. |
| T-03-SC | Tampering | npm installs | accept | No new packages installed in this plan beyond `server-only`, which is shipped as a Next.js 16 transitive dependency (no version pin needed). If the install requires a fresh `server-only` package addition, the executor checks npmjs.com/package/server-only legitimacy as part of the normal install flow per project policy. |
</threat_model>

<verification>
- npx tsc --noEmit exits 0
- npm run build exits 0 with OR without network access (REVIEW H-4 path-a — the JSON fallback covers any failure mode in the fetcher)
- out/index.html contains id="projects", "Selected work on GitHub", and at least one href to https://github.com/axelw/
- **REVIEW C-2:** `grep -rE "GITHUB_TOKEN|Bearer " out/_next/static/` returns ZERO matches
- out/index.html does NOT contain "Bearer " or "GITHUB_TOKEN" (defense-in-depth token-leak smoke test)
- src/lib/projects.ts contains exactly one `console.warn` (the catch-block fallback warn) and zero `.message` references in any console.* call
- No new files contain 'use client' (this plan adds zero new Client Components — the four new files in src/data, src/lib, and src/components/projects are all Server scope; FadeUp.tsx remains 'use client' but only the JSX return is modified)
- src/components/ui/FadeUp.tsx emits `data-fadein` on its wrapper div (REVIEW C-1 prerequisite for Plan 03 UAT-5)
</verification>

<success_criteria>
- The seven new/modified files exist with the contracts specified above; page.tsx is updated to wire Projects between CV and Contact
- Running `next build` ALWAYS produces an out/index.html — with real fetched data when the GitHub API is reachable, with the placeholder JSON fallback otherwise
- All three Phase 3 requirements (PROJ-01 build-time fetch, PROJ-02 name + description, PROJ-03 conditional homepage link) are observably satisfied in the rendered HTML
- The slice ships hardened per the cross-AI adversarial review: server-only guard (C-2), URL protocol allowlist (C-3), Link-header pagination (H-6), nullable pushedAt (H-8), focus-visible from day one (H-7), sanitized warn (H-5), trim-guarded description (M-13), centralized username constant (L-16), and FadeUp data-fadein attribute (C-1)
- Plan 02 narrows to: enrich the JSON fallback with 4–7 real recent repos AND add the forced-401 verification test
- Plan 03 narrows to: relative-date utility, hover/focus polish on top of existing focus-visible, empty state, Playwright UAT
</success_criteria>

<output>
Create `.planning/phases/03-projects/03-01-SUMMARY.md` when done
</output>
