---
phase: 05-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/projects.ts
  - src/app/layout.tsx
  - src/components/hero/Hero.tsx
  - src/components/contact/Contact.tsx
  - src/components/projects/ProjectsEmptyState.tsx
  - .github/workflows/deploy.yml
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every `axelw` reference in source, workflow, and planning files (except deferred projects.json) is replaced with `axelwaserman` or the correct full URL."
    - "Local builds without `GITHUB_USERNAME` env var still target `axelwaserman` (belt-and-braces per D-21)."
    - "Workflow build step exports `GITHUB_USERNAME=axelwaserman` so CI builds also fetch the correct user (per D-21)."
    - "Hero `<h1>` and metadata title strings align (planner picks `Axel Waserman` for both per UI-SPEC name reconciliation)."
    - "Hero metadata `title` reflects D-15 title (`Senior Engineering Manager | Backend & Data`) so social cards stop showing `Software Engineer`."
    - "Hero CTA cluster keeps four labels per UI-SPEC §Copywriting: GitHub, LinkedIn, Email, Download CV. Email anchor `mailto:` href becomes `mailto:axel.waserman@gmail.com` (D-17 resolved 2026-06-04)."
  artifacts:
    - path: "src/lib/projects.ts"
      provides: "Default GITHUB_USERNAME = 'axelwaserman' on line 7"
      contains: "'axelwaserman'"
    - path: ".github/workflows/deploy.yml"
      provides: "GITHUB_USERNAME env on the build step"
      contains: "GITHUB_USERNAME: axelwaserman"
    - path: "src/app/layout.tsx"
      provides: "metadataBase + openGraph.url on axelwaserman.github.io and aligned title strings"
      contains: "https://axelwaserman.github.io"
    - path: "src/components/hero/Hero.tsx"
      provides: "Hero CTA cluster with four anchors including resolved mailto"
      contains: "mailto:axel.waserman@gmail.com"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/data/cv.ts"
      via: "title (Senior Engineering Manager | Backend & Data) — metadata mirrors hero title"
      pattern: "Senior Engineering Manager"
    - from: ".github/workflows/deploy.yml"
      to: "src/lib/projects.ts"
      via: "process.env.GITHUB_USERNAME injected at build"
      pattern: "GITHUB_USERNAME"
---

<objective>
Sweep every stale `axelw` reference across source, workflow, and planning files, and add the `GITHUB_USERNAME=axelwaserman` env var to the deploy workflow's build step. This unblocks the live GitHub fetch (Plan 05-08) and stops social cards / SEO from leaking the wrong username.

Purpose: Phase 5 Success Criterion #4 — "Projects section renders Axel's real public repos via the GitHub API at build time" — is impossible until the username default and workflow env are correct. Decisions implemented: D-21 (username default + workflow env), D-23 (full stale-username sweep), D-17 (email resolved to `axel.waserman@gmail.com` on 2026-06-04 — Hero email anchor stays). Title alignment piggybacks here so layout metadata stays consistent with D-15.

**Revision note (2026-06-04):** D-17 was resolved during the planner revision pass. Email is now the known string `axel.waserman@gmail.com`. The Hero email anchor is RESTORED with the resolved address; the Contact email row is rewritten to use the resolved address directly (no sentinel handling, since no TBD state remains). The Contact.tsx hard-coded `mailto:axel@example.com` anchor's URL is updated to the real address rather than removed — Plan 05-07 still owns the centering work and converts hard-coded URLs to data-driven imports.

Output: Source + workflow + planning files reference only `axelwaserman`; both `mailto:axel@example.com` placeholders are updated to `mailto:axel.waserman@gmail.com` (excluding `src/data/projects.json` which Plan 05-08 refreshes wholesale).
</objective>

<execution_context>
@/Users/axel/code/website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish/05-CONTEXT.md
@.planning/phases/05-polish/05-PATTERNS.md
@.planning/phases/05-polish/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix `src/lib/projects.ts` default username + add workflow env var</name>
  <files>src/lib/projects.ts, .github/workflows/deploy.yml</files>
  <read_first>
    - src/lib/projects.ts (current state — preserve `import 'server-only'` on line 1, pagination, fork/archive/disabled filter, sort, fallback path; only line 7 changes)
    - .github/workflows/deploy.yml (current build step env block on lines 47-50)
    - .planning/phases/05-polish/05-PATTERNS.md (sections "src/lib/projects.ts (MODIFIED — line 7 only)" and ".github/workflows/deploy.yml (MODIFIED — add username env var)")
    - .planning/phases/05-polish/05-CONTEXT.md (D-21 decision)
  </read_first>
  <action>
    Per D-21 (belt-and-braces): change the default value on `src/lib/projects.ts` line 7 from `'axelw'` to `'axelwaserman'`. Preserve every other line of the file verbatim — `import 'server-only'`, the `GitHubRepo` interface, pagination loop, `MAX_PAGES`, fork/archive/disabled filter, sort by `pushed_at` desc, the `try`/`catch` fallback to `projectsFallback`, the `console.warn` on line 109 (with its eslint-disable comment).

    In `.github/workflows/deploy.yml`, add `GITHUB_USERNAME: axelwaserman` as a second key under the build step's `env:` block (the step named `Build Next.js static export`, currently lines 47-50). Do NOT regress Phase 4 decisions: cron schedule (line 7), permissions (lines 10-13), concurrency (lines 15-17), action versions (`@v4`, `@v5`, `@v3`), `actions/configure-pages@v5`, `actions/cache@v4`, `actions/upload-pages-artifact@v3`, `actions/deploy-pages@v5`. Single addition only.
  </action>
  <verify>
    <automated>grep -n "axelwaserman" src/lib/projects.ts && grep -n "axelw'" src/lib/projects.ts; test $? -ne 0 && grep -A 5 "Build Next.js" .github/workflows/deploy.yml | grep "GITHUB_USERNAME: axelwaserman"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/projects.ts` line 7 contains the literal string `'axelwaserman'` and does NOT contain `'axelw'` as a username default.
    - `grep -c "import 'server-only'" src/lib/projects.ts` returns `1` (preserved).
    - `.github/workflows/deploy.yml` contains a line matching `GITHUB_USERNAME: axelwaserman` inside the `env:` block of the `Build Next.js static export` step.
    - `grep -c "GITHUB_TOKEN: \${{ secrets.GITHUB_TOKEN }}" .github/workflows/deploy.yml` returns `1` (preserved).
    - `grep -c "actions/configure-pages@v5" .github/workflows/deploy.yml` returns `1` (preserved).
    - `next build` exits 0 (use `npm run build`).
  </acceptance_criteria>
  <done>Username default is `axelwaserman`; workflow exports `GITHUB_USERNAME` to the build step; no Phase 4 workflow regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Replace `axelw` URLs in Hero, Contact, ProjectsEmptyState, layout metadata; restore real email</name>
  <files>src/components/hero/Hero.tsx, src/components/contact/Contact.tsx, src/components/projects/ProjectsEmptyState.tsx, src/app/layout.tsx</files>
  <read_first>
    - src/components/hero/Hero.tsx (GitHub on line 24, LinkedIn on line 32, Email on line 40)
    - src/components/contact/Contact.tsx (Email on line 19, LinkedIn on line 25, GitHub on line 33)
    - src/components/projects/ProjectsEmptyState.tsx (line 10 repo URL)
    - src/app/layout.tsx (metadataBase line 22, openGraph.url line 26, title strings on lines 20, 24, 27, 34, 40, 41)
    - .planning/phases/05-polish/05-PATTERNS.md (sections "src/components/hero/Hero.tsx (MODIFIED — embed mandala, fix URLs)", "src/components/contact/Contact.tsx", "src/components/projects/ProjectsEmptyState.tsx", "src/app/layout.tsx")
    - .planning/phases/05-polish/05-UI-SPEC.md (Copywriting Contract — Hero name row + Contact email row resolved)
    - .planning/phases/05-polish/05-CONTEXT.md (D-17 RESOLVED 2026-06-04: email = axel.waserman@gmail.com)
  </read_first>
  <action>
    URL fixes per D-23 + D-17 resolution (planner is responsible for choosing exact replacements; UI-SPEC + CONTEXT.md D-17 update lock them):

    Hero.tsx — change `https://github.com/axelw` to `https://github.com/axelwaserman`; change `https://linkedin.com/in/axelw` to `https://www.linkedin.com/in/axel-waserman-9753221a6/`. **D-17 RESOLVED:** Update the email anchor's `href` from `mailto:axel@example.com` (current line 40) to `mailto:axel.waserman@gmail.com`. The full email anchor block (current lines 39-44) STAYS — preserve every className token, the visible label `Email`, and the surrounding JSX structure. The Hero CTA cluster keeps four anchors per UI-SPEC §Copywriting Contract: GitHub / LinkedIn / Email / Download CV. The mandala embed lands later in Plan 05-06 — leave the rest of Hero.tsx structure untouched here.

    Contact.tsx — same LinkedIn + GitHub rewrites. Update the `mailto:axel@example.com` href on the Email anchor (current line 19) to `mailto:axel.waserman@gmail.com`. Preserve the anchor block's className tokens and visible label. Do NOT change the layout structure (re-centering and conversion to data-driven `contact` import happens in Plan 05-07); only change hrefs in this task.

    ProjectsEmptyState.tsx — change `https://github.com/axelw/axelw.github.io` (line 10) to `https://github.com/axelwaserman/axelwaserman.github.io`.

    layout.tsx — change `metadataBase: new URL('https://axelw.github.io')` (line 22) to `https://axelwaserman.github.io`; change `openGraph.url` (line 26) to the same. **Name + title alignment per UI-SPEC §Copywriting:** change every occurrence of `'Axel W — Software Engineer'` (lines 20, 24, 41) to `'Axel Waserman — Senior Engineering Manager'`. Change `siteName: 'Axel W'` (line 27) to `'Axel Waserman'`. Change `alt: 'Axel W — Software Engineer'` (line 34) to `'Axel Waserman — Senior Engineering Manager'`.

    The Hero `<h1>` rendered text (`Axel W` on line 14) stays as `Axel W` for now — Plan 05-06 reconciles the visible h1 with the metadata `Axel Waserman` when restructuring the hero into a two-column layout; metadata-string alignment in this task is the contract minimum.
  </action>
  <verify>
    <automated>grep -rn "axelw\b\|github\.com/axelw[^a]\|linkedin\.com/in/axelw\b" src/components src/app/layout.tsx; grep -c "https://github.com/axelwaserman" src/components/hero/Hero.tsx src/components/contact/Contact.tsx src/components/projects/ProjectsEmptyState.tsx src/app/layout.tsx; grep -c "mailto:axel.waserman@gmail.com" src/components/hero/Hero.tsx src/components/contact/Contact.tsx; grep -c "mailto:axel@example.com" src/components/hero/Hero.tsx src/components/contact/Contact.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "axelw\b" src/components src/app/layout.tsx` returns zero matches (no whole-word `axelw` left in source).
    - `grep -rn "axelw\.github\.io" src/components src/app/layout.tsx` returns zero matches.
    - `grep -rn "linkedin\.com/in/axelw\b" src/components` returns zero matches.
    - `grep -c "https://github.com/axelwaserman" src/components/hero/Hero.tsx` returns `1`.
    - `grep -c "https://www.linkedin.com/in/axel-waserman-9753221a6/" src/components/hero/Hero.tsx` returns `1`.
    - `grep -c "https://github.com/axelwaserman" src/components/contact/Contact.tsx` returns `1`.
    - `grep -c "https://www.linkedin.com/in/axel-waserman-9753221a6/" src/components/contact/Contact.tsx` returns `1`.
    - `grep -c "https://github.com/axelwaserman/axelwaserman.github.io" src/components/projects/ProjectsEmptyState.tsx` returns `1`.
    - `grep -c "mailto:axel.waserman@gmail.com" src/components/hero/Hero.tsx` returns `1` (Email anchor restored with real address).
    - `grep -c "mailto:axel@example.com" src/components/hero/Hero.tsx` returns `0` (placeholder removed).
    - `grep -c "mailto:axel.waserman@gmail.com" src/components/contact/Contact.tsx` returns `1` (placeholder updated to real address; Plan 05-07 later swaps to data-driven `contact.email`).
    - `grep -c "mailto:axel@example.com" src/components/contact/Contact.tsx` returns `0` (placeholder removed).
    - `grep -c "Email" src/components/hero/Hero.tsx` returns at least `1` (visible label preserved).
    - `grep -c "https://axelwaserman.github.io" src/app/layout.tsx` returns at least `2` (metadataBase + openGraph.url).
    - `grep -c "Senior Engineering Manager" src/app/layout.tsx` returns at least `2` (title + openGraph.title or twitter.title).
    - `grep -c "Axel W —" src/app/layout.tsx` returns `0` (old title strings replaced).
    - `next build` exits 0.
  </acceptance_criteria>
  <done>All four source/component files use `axelwaserman` URLs; Hero CTA cluster keeps four anchors with the resolved email; both `mailto:axel@example.com` placeholders are updated to `mailto:axel.waserman@gmail.com`; layout metadata title aligns with D-15 (`Senior Engineering Manager`); no Phase 2 layout structure regressions.</done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md to reference axelwaserman.github.io</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 1, 32 — stale `axelw.github.io` references in the title heading and INFRA-07 narrative)
    - .planning/phases/05-polish/05-CONTEXT.md (D-23 sweep list)
  </read_first>
  <action>
    Per D-23 sweep: in `.planning/REQUIREMENTS.md`, replace `axelw.github.io` with `axelwaserman.github.io` on line 1 (title heading) and line 32 (INFRA-07 narrative). No other content changes — preserve the requirement IDs, statuses, traceability table.

    Also scan `.planning/PROJECT.md` for any stale `axelw` references and update them too (verify-step grep determines whether changes are needed; if the file is already clean, no edit is required).
  </action>
  <verify>
    <automated>grep -c "axelw\.github\.io\b" .planning/REQUIREMENTS.md; grep -c "axelwaserman.github.io" .planning/REQUIREMENTS.md; grep -rn "axelw[^a]\|axelw\b" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "axelw\.github\.io\b" .planning/REQUIREMENTS.md` returns `0`.
    - `grep -c "axelwaserman\.github\.io" .planning/REQUIREMENTS.md` returns at least `2` (heading + INFRA-07).
    - `grep -c "INFRA-07" .planning/REQUIREMENTS.md` returns at least `2` (preserved).
    - `grep -rn "axelw\b\|axelw\.github\.io" .planning/PROJECT.md` returns no matches (or, if matches existed, they were updated to `axelwaserman`).
  </acceptance_criteria>
  <done>Planning docs no longer reference the stale `axelw.github.io` host; requirement traceability table is intact.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0.
- `grep -rn "axelw\b\|axelw\.github\.io\|linkedin\.com/in/axelw\b\|github\.com/axelw[^a]" src .github/workflows .planning/REQUIREMENTS.md .planning/PROJECT.md` returns zero matches (excluding `src/data/projects.json` which Plan 05-08 refreshes).
- `grep -rn "mailto:axel@example.com" src` returns zero matches.
- `git diff` against `HEAD` is limited to the seven files in `files_modified` plus possibly `.planning/PROJECT.md` if it had stale references.
</verification>

<success_criteria>
- D-21 implemented: `src/lib/projects.ts` default + workflow env both target `axelwaserman`.
- D-23 implemented: every stale `axelw` URL/host in source, components, layout metadata, workflow, REQUIREMENTS, and PROJECT is fixed.
- D-17 partial closure: both `mailto:axel@example.com` placeholders replaced with `mailto:axel.waserman@gmail.com`. Hero email anchor stays in place (four-link CTA cluster preserved). Plan 05-08 no longer needs to gate on email TBD; Plan 05-02 sets `contact.email` to the real value directly.
- Hero metadata title aligned with D-15 so SEO / social cards reflect the new title once deployed.
- No regressions in Phase 4 deploy workflow (cron, permissions, action versions preserved).
</success_criteria>

<output>
Create `.planning/phases/05-polish/05-01-SUMMARY.md` per the summary template when done.
</output>
