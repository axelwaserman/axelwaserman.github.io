---
phase: 05-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/cv.ts
  - src/components/cv/CV.tsx
  - justfile
  - public/cv.pdf
autonomous: false
requirements: []
user_setup:
  - service: typst
    why: "Compile assets/CV.typ -> public/cv.pdf locally; PDF is committed (D-11 — no CI Typst dependency)."
    install_check: "command -v typst (install via `brew install typst` if missing)."
  - service: just
    why: "Run the `cv` recipe."
    install_check: "command -v just (install via `brew install just` if missing)."
must_haves:
  truths:
    - "src/data/cv.ts mirrors assets/CV.typ verbatim: bio, title, work experience (with bullets), education, three skill groups (D-10)."
    - "public/cv.pdf exists and was produced from `just cv` (typst compile assets/CV.typ public/cv.pdf) (D-11)."
    - "WorkEntry interface gains `bullets: string[]` so CV.typ rich bullets render (D-13)."
    - "title export equals the literal string `Senior Engineering Manager | Backend & Data` (D-15)."
    - "skills shape becomes `SkillGroup[]` with the three CV.typ groups verbatim (D-16)."
    - "contact.email is the real string `axel.waserman@gmail.com` (D-17 RESOLVED 2026-06-04) — not nullable, not a sentinel."
    - "CV.tsx switches its imports from `skills` to `skillGroups` so the build stays green within Plan 05-02. Plan 05-05 then handles centering / bullets / DownloadCVButton on top of the new data shape (Strategy A — chosen during 2026-06-04 revision; no deprecated stub left behind)."
  artifacts:
    - path: "src/data/cv.ts"
      provides: "Real CV data + extended interfaces"
      contains: "skillGroups"
    - path: "src/data/cv.ts"
      provides: "title export equal to D-15 literal"
      contains: "Senior Engineering Manager | Backend & Data"
    - path: "src/data/cv.ts"
      provides: "Resolved contact.email string (D-17)"
      contains: "axel.waserman@gmail.com"
    - path: "src/components/cv/CV.tsx"
      provides: "Imports skillGroups (not skills); minimal data-shape switch only — centering + DownloadCVButton + bullets land in Plan 05-05"
      contains: "skillGroups"
    - path: "justfile"
      provides: "`just cv` recipe compiling assets/CV.typ to public/cv.pdf"
      contains: "typst compile assets/CV.typ public/cv.pdf"
    - path: "public/cv.pdf"
      provides: "Committed PDF artifact"
      min_bytes: 1000
  key_links:
    - from: "justfile"
      to: "assets/CV.typ"
      via: "typst compile recipe"
      pattern: "typst compile assets/CV.typ"
    - from: "src/data/cv.ts"
      to: "assets/CV.typ"
      via: "verbatim mirror — same work entries, education entries, skill groups"
      pattern: "Contentsquare\\|PwC France\\|Panodyssey\\|ESILV\\|Griffith"
    - from: "src/components/cv/CV.tsx"
      to: "src/data/cv.ts"
      via: "imports skillGroups instead of skills"
      pattern: "skillGroups"
---

<objective>
Lift Axel's real CV out of `assets/CV.typ` into `src/data/cv.ts`, stand up the local Typst → PDF pipeline so the website's CV section and `Download CV` link both reflect his real history, and switch `CV.tsx` to consume the new `skillGroups` export so the build stays green at the end of Wave 1. Email is the resolved string `axel.waserman@gmail.com` (D-17 resolved 2026-06-04 during planner revision).

Purpose: Phase 5 Success Criterion #3 — "CV section content reflects Axel's real history with working email, LinkedIn, and GitHub URLs — no Lorem Ipsum, placeholder names, or example.com links". This plan delivers the data layer + minimal render contract switch + PDF artifact; Plan 05-05 layers on centering, DownloadCVButton, and WorkEntry bullets on top of the new data shape.

**Revision note (2026-06-04):** Strategy A chosen during planner revision pass — the temporary `// @deprecated export const skills: string[] = []` stub from the original plan has been DROPPED. CV.tsx switches to `skillGroups` here in Plan 05-02 (Wave 1), keeping `npm run build` green at the end of Wave 1 without a deprecated bridge. Plan 05-05 (Wave 2) takes only the centering / DownloadCVButton / WorkEntry-bullets render work. Both plans modify CV.tsx, so Plan 05-05's `depends_on` already includes 05-02 and waves are correctly assigned.

Output: Updated `src/data/cv.ts` with extended interfaces and verbatim CV.typ content (including the resolved email), updated `src/components/cv/CV.tsx` consuming `skillGroups`, a root `justfile` with the `cv` recipe, and a committed `public/cv.pdf`.
</objective>

<execution_context>
@/Users/axel/code/website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-polish/05-CONTEXT.md
@.planning/phases/05-polish/05-PATTERNS.md
@.planning/phases/05-polish/05-UI-SPEC.md
@assets/CV.typ
@src/data/cv.ts
@src/components/cv/CV.tsx
@src/components/cv/SkillsList.tsx

<interfaces>
Current `src/data/cv.ts` exports (file is small — read in full above):
  - interface WorkEntry { role: string; company: string; dates: string; description: string }
  - interface EducationEntry { degree: string; institution: string; years: string }
  - export const bio: string
  - export const title: string
  - export const workEntries: WorkEntry[]
  - export const educationEntries: EducationEntry[]
  - export const skills: string[]

Phase 5 changes per D-13 / D-14 / D-15 / D-16 / D-17:
  - WorkEntry gains `bullets: string[]` (default to `[]` when a role has none)
  - New `interface SkillGroup { category: string; items: string[] }`
  - REPLACE `skills: string[]` with `skillGroups: SkillGroup[]` (no deprecated stub left behind)
  - Add a `contact` export: `interface Contact { github: string; linkedin: string; email: string }` — `email` is a non-null string `axel.waserman@gmail.com` (D-17 resolved 2026-06-04)
  - bio + title strings replaced verbatim from CV.typ

Current `src/components/cv/CV.tsx` (read the file before editing):
  - imports `{ workEntries, educationEntries, skills } from '@/data/cv'`
  - imports `SkillsList from './SkillsList'`
  - renders three blocks with `<SkillsList items={skills} />`

Plan 05-02 minimal CV.tsx switch:
  - Change `skills` import to `skillGroups`.
  - Change `<SkillsList items={skills} />` to render the three groups in a way that compiles AND keeps the page rendering. Two acceptable shapes (executor picks the simpler):
    a) Inline a quick `groups.map(g => <SkillsList items={g.items} />)` rendering each group's items as the existing SkillsList. This preserves the SkillsList component for now; Plan 05-05 deletes SkillsList when introducing SkillGroupList.
    b) Inline a minimal `groups.map(g => <div key={g.category}><h4>{g.category}</h4><p>{g.items.join(', ')}</p></div>)` with a sensible className. Plan 05-05 will replace this with the SkillGroupList component + UI-SPEC styling.

  Either shape is fine — the goal is "compile + render real skill data" at end of Wave 1, not the final styled layout. Plan 05-05 owns the final SkillGroupList component, the centering, the eyebrow rework, and SkillsList deletion.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend cv.ts interfaces + replace placeholders with CV.typ content (incl. resolved email)</name>
  <files>src/data/cv.ts</files>
  <read_first>
    - assets/CV.typ (full file — every work entry, every bullet, education entries, professional summary, three competency groups)
    - src/data/cv.ts (current state — preserve named-exports style, em-dash date format, interface-not-type per coding-style.md)
    - .planning/phases/05-polish/05-PATTERNS.md (section "src/data/cv.ts (MODIFIED — extend interfaces, replace placeholder data)")
    - .planning/phases/05-polish/05-CONTEXT.md (D-10, D-13, D-14, D-15, D-16, D-17 RESOLVED 2026-06-04)
    - .planning/phases/05-polish/05-UI-SPEC.md (Copywriting Contract + skill group rendering rules)
    - /Users/axel/.claude/rules/typescript/coding-style.md (interface vs type, named exports, public APIs require explicit types)
  </read_first>
  <action>
    Rewrite `src/data/cv.ts` per the rules below. Preserve named-exports-only style; preserve em-dash convention in dates; use `interface` for object shapes; explicit types on every export.

    Interfaces:
    - Extend `WorkEntry` with `bullets: string[]`. Optionality is FALSE — always pass an array (empty `[]` if a role has no bullets in CV.typ).
    - Keep `EducationEntry` unchanged.
    - Add `interface SkillGroup { category: string; items: string[] }`.
    - Add `interface Contact { github: string; linkedin: string; email: string }`. Email is a non-null string — D-17 resolved.

    Constants:
    - `export const bio` — D-14 trim of CV.typ Professional Summary. Use the locked recommended draft from UI-SPEC §Copywriting Contract: `'Senior Engineering Manager working at the intersection of product engineering, high-availability data architecture, and distributed team operations — building resilient, async-first engineering cultures across time zones.'` (Max 220 chars; verify with `string.length`.) Axel reviews on PR.
    - `export const title = 'Senior Engineering Manager | Backend & Data'` (D-15 — exact pipe + single spaces, verbatim from CV.typ contact header line 49).
    - `export const contact: Contact = { github: 'https://github.com/axelwaserman', linkedin: 'https://www.linkedin.com/in/axel-waserman-9753221a6/', email: 'axel.waserman@gmail.com' }` — D-17 resolved 2026-06-04. The literal email string MUST appear in the source so the rendered HTML surfaces it for ATS / SEO crawlers.

    Work entries (verbatim from CV.typ §"Professional Experience" — preserve every bullet's wording, including the bold-prefix pattern like `*Post-Acquisition Integration:*`; store bullets as plain strings WITH the leading `Post-Acquisition Integration:` prefix intact — the WorkEntry component splits on first colon at render time per UI-SPEC):
    1. `Senior Engineering Manager` — Contentsquare — `May 2025 — Present` — description: top-line context (CV.typ line 75 first sentence about Hotjar acquisition + 5 distributed engineers + merging cultures); bullets: the five `*…:*` bullets on lines 76-80 of CV.typ verbatim.
    2. `Engineering Manager` — Contentsquare — `Jan 2024 — Apr 2025` — same description as Senior EM (consolidated per CV.typ which lists both roles under one bullet block); bullets EMPTY array `[]` (the bullet list belongs to the Senior EM entry per CV.typ structure). Planner's call: render the EM role as a stacked sub-heading inside the Senior EM article in Plan 05-05; storing it as a separate WorkEntry with `bullets: []` keeps the data shape simple and lets the component decide presentation.
    3. `Data Engineer II` — Contentsquare — `Jan 2023 — Dec 2023` — description: short opener from CV.typ line 85; bullets: lines 86-89 verbatim (`Designed and implemented…`, `Transitioned the engineering function…`, `Optimized legacy infrastructure…`, `Established robust backend engineering standards…`).
    4. `Data Engineer I` — Contentsquare — `May 2022 — Dec 2022` — same description + bullets as Data Engineer II per CV.typ structure (the bullet block covers both); bullets EMPTY `[]` with the same rationale as #2.
    5. `Data Engineer` — PwC France — `Feb 2021 — Apr 2022` — description: opening sentence from CV.typ line 92; bullets: lines 93-94 verbatim.
    6. `Software Engineer` — Panodyssey — `Mar 2020 — Aug 2020` — description: empty string OR a single-sentence trim of the bullet (planner's call — recommend empty `''` and put the full sentence as the single bullet); bullets: line 99 verbatim.

    Education entries (verbatim from CV.typ §Education):
    1. `Master of Engineering — Mathematics and Computer Science` — `ESILV (Ecole Supérieure d'Ingénieurs Léonard de Vinci)` — `2017 — 2021`.
    2. `Computer Science (Study Abroad Semester)` — `Griffith College Dublin` — `2020 — 2021`.

    Skill groups (verbatim from CV.typ §"Core Competencies & Technologies", lines 64-66; preserve the three category names `Engineering Leadership`, `Backend & Systems Architecture`, `Hands-on Tech Stack`; split each group's items by `, ` — UI-SPEC §"Skill groups (D-16)" renders them comma-separated, so storing each individual item as an array element is the right shape):
    - Engineering Leadership: `['Remote & Async Team Management', 'Empathetic Leadership & Mentorship', 'High-Bar Technical Hiring', 'Cross-Border Team Integration', 'Agile Delivery', 'Product-Engineering Alignment']`
    - Backend & Systems Architecture: `['Distributed Systems', 'Microservices', 'Internal Developer Platforms', 'High-Availability Data Pipelines', 'Cloud Architecture', 'Self-Serve Data Systems']`
    - Hands-on Tech Stack: `['Python', 'FastAPI', 'PydanticAI (AI Agents/Data Validation)', 'SQL', 'Cloud Data Warehouses (Snowflake, BigQuery, Redshift)', 'ELT (dbt, Airflow)', 'CI/CD Tooling', 'Git', 'REST & GraphQL APIs']`

    Remove the old `skills: string[]` export entirely. Replace with `export const skillGroups: SkillGroup[]`. Do NOT leave a deprecated stub — Strategy A applies.

    Em-dash convention: dates use the EN DASH (`—`, U+2014) per the existing pattern in `cv.ts`. CV.typ uses an EN DASH too; replicate exactly. Do NOT use a hyphen `-`.

    Do NOT export an `EMAIL_TBD_SENTINEL` constant — D-17 is resolved; no consumer needs the sentinel anymore.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "skillGroups" src/data/cv.ts && grep -c "Senior Engineering Manager | Backend & Data" src/data/cv.ts && grep -c "axel.waserman@gmail.com" src/data/cv.ts && grep -c "Contentsquare" src/data/cv.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (no type errors).
    - `grep -c "interface WorkEntry" src/data/cv.ts` returns `1` and the interface block contains `bullets:` followed by `string[]`.
    - `grep -c "interface SkillGroup" src/data/cv.ts` returns `1`.
    - `grep -c "interface Contact" src/data/cv.ts` returns `1`.
    - `grep -c "email: string" src/data/cv.ts` returns at least `1` (Contact interface has non-null email).
    - `grep -c "string | null" src/data/cv.ts` returns `0` (no nullable email type).
    - `grep -c "axel.waserman@gmail.com" src/data/cv.ts` returns at least `1` (resolved email present as plain text).
    - `grep -c "<<TBD: email>>" src/data/cv.ts` returns `0` (sentinel removed).
    - `grep -c "EMAIL_TBD_SENTINEL" src/data/cv.ts` returns `0` (constant removed).
    - `grep -c "Senior Engineering Manager | Backend & Data" src/data/cv.ts` returns `1` (D-15 verbatim).
    - `grep -c "Contentsquare" src/data/cv.ts` returns at least `4` (Senior EM, EM, Data Eng II, Data Eng I).
    - `grep -c "PwC France" src/data/cv.ts` returns `1`.
    - `grep -c "Panodyssey" src/data/cv.ts` returns `1`.
    - `grep -c "ESILV" src/data/cv.ts` returns `1`.
    - `grep -c "Griffith College Dublin" src/data/cv.ts` returns `1`.
    - `grep -c "Engineering Leadership" src/data/cv.ts` returns `1`.
    - `grep -c "Backend & Systems Architecture" src/data/cv.ts` returns `1`.
    - `grep -c "Hands-on Tech Stack" src/data/cv.ts` returns `1`.
    - `grep -c "Placeholder Role\\|Placeholder Company\\|Placeholder Degree\\|Placeholder University" src/data/cv.ts` returns `0`.
    - `grep -c "skills: string\\[\\]" src/data/cv.ts` returns `0` (old export removed; no deprecated stub).
    - `grep -c "skillGroups" src/data/cv.ts` returns at least `2` (interface + export).
  </acceptance_criteria>
  <done>cv.ts mirrors CV.typ verbatim across bio, title, work entries (with bullets), education, skill groups; email is the real `axel.waserman@gmail.com` string (no sentinel, no nullable type); tsc clean; no deprecated stubs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Switch CV.tsx imports from skills to skillGroups (minimal data-shape switch)</name>
  <files>src/components/cv/CV.tsx</files>
  <read_first>
    - src/components/cv/CV.tsx (current state — three two-column grid blocks, imports `skills`, renders `<SkillsList items={skills} />`)
    - src/components/cv/SkillsList.tsx (current shape; preserved here, deleted in Plan 05-05)
    - src/data/cv.ts (post-Task-1 — confirms `skillGroups` export available, `skills` removed)
    - .planning/phases/05-polish/05-PATTERNS.md (section "src/components/cv/CV.tsx (MODIFIED — re-center, render groups, add CTA)" — but only the data-import switch happens here; centering + CTA are Plan 05-05)
    - /Users/axel/.claude/rules/typescript/coding-style.md
  </read_first>
  <action>
    Make the MINIMAL change required to keep the build green now that `skills` no longer exists in `cv.ts`. Two acceptable shapes — executor picks shape (a) for the smallest diff:

    Shape (a) — preferred (smallest diff):
      - Change the import from `import { workEntries, educationEntries, skills } from '@/data/cv'` to `import { workEntries, educationEntries, skillGroups } from '@/data/cv'`.
      - Replace the single `<SkillsList items={skills} />` JSX with a `skillGroups.map(group => (<SkillsList key={group.category} items={group.items} />))` block. This renders three SkillsList instances, one per group. The category headings are NOT rendered here — Plan 05-05 introduces SkillGroupList with proper category headings + UI-SPEC styling.
      - Preserve every other token in the file: max-w-4xl, the three two-column grid wrappers, eyebrow labels, Phase 2 layout. Plan 05-05 owns those changes.

    Shape (b) — acceptable if executor prefers a structurally cleaner intermediate:
      - Same import change as (a).
      - Replace `<SkillsList items={skills} />` with a quick inline `skillGroups.map(group => (<div key={group.category}><h4 className="font-semibold text-[length:var(--text-body)] text-[var(--color-text)] mb-2">{group.category}</h4><p className="text-[length:var(--text-ui)] text-[var(--color-text)] max-w-[60ch] leading-[1.5]">{group.items.join(', ')}</p></div>))`. Plan 05-05 will swap this for the proper SkillGroupList component.
      - Still no centering / CTA / WorkEntry bullets here.

    DO NOT delete SkillsList.tsx. DO NOT add max-w-3xl or DownloadCVButton. DO NOT touch WorkEntry. DO NOT touch EducationEntry. Plan 05-05 owns all of that.

    Anti-pattern guard: keep this commit small. The goal is "Wave 1 ends with `npm run build` green and the rendered page showing real skill group data".
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && grep -c "skillGroups" src/components/cv/CV.tsx && grep -c "from '@/data/cv'" src/components/cv/CV.tsx && grep -c "skills" src/components/cv/CV.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm run build` exits 0 — proves Wave 1 ends green even though Plan 05-05 hasn't run yet.
    - `grep -c "skillGroups" src/components/cv/CV.tsx` returns at least `2` (import + map usage).
    - `grep -c "from '@/data/cv' import\\|, skills }\\|skills }" src/components/cv/CV.tsx` returns `0` checked another way: `grep -c "skills:" src/components/cv/CV.tsx` returns `0` AND `grep -c " skills " src/components/cv/CV.tsx` returns `0` AND the import line does NOT contain a bare `skills` token (use `grep -E "import \\{[^}]*\\bskills\\b[^}]*\\}" src/components/cv/CV.tsx` returns `0`).
    - The rendered HTML in `out/index.html` (after `npm run build`) contains the literal text `Engineering Leadership` AND `Backend & Systems Architecture` AND `Hands-on Tech Stack` (proves real skill data reaches the page even before Plan 05-05's restyle).
    - File `src/components/cv/SkillsList.tsx` still exists (Plan 05-05 deletes it).
  </acceptance_criteria>
  <done>CV.tsx imports `skillGroups` and renders all three groups; `npm run build` is green at end of Wave 1; SkillsList.tsx still in place for Plan 05-05 to delete.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add justfile + compile public/cv.pdf</name>
  <files>justfile, public/cv.pdf</files>
  <read_first>
    - assets/CV.typ (the input the recipe compiles)
    - .planning/phases/05-polish/05-PATTERNS.md (section "justfile (NEW — repo root)")
    - .planning/phases/05-polish/05-CONTEXT.md (D-11)
  </read_first>
  <action>
    Create `/Users/axel/code/website/justfile` at repo root (no extension; lowercase). Single recipe per D-11:
    - Recipe name: `cv`
    - Body: `typst compile assets/CV.typ public/cv.pdf`
    - Add a single `default` recipe at the top that lists recipes (`just --list`) so `just` with no argument is helpful, OR omit `default` — planner picks. Keep the file under 20 lines.

    Then run `just cv` from the repo root. This produces `public/cv.pdf`. Both files (`justfile` + `public/cv.pdf`) are committed.

    If `typst` is not on PATH, this is a `checkpoint:human-action` situation — but D-11 explicitly says PDF is committed (no CI dependency on Typst), so the executor is responsible for installing Typst locally (`brew install typst` on macOS) and running the recipe. The PDF is binary-committed; CI never runs Typst.

    If the build emits warnings (e.g. font fallbacks for "Liberation Sans"), they're acceptable — Typst will fall back to a similar sans-serif. Do NOT modify `assets/CV.typ` to silence them — D-domain says "Editing the wording or structure of `assets/CV.typ` itself" is OUT OF SCOPE.

    **D-17 / SEO / ATS note:** Confirm the email address `axel.waserman@gmail.com` is present in `assets/CV.typ` before running `just cv` so it surfaces in `public/cv.pdf` as well as the rendered HTML. The email MUST remain in CV.typ — do NOT scrub it from the Typst source.
  </action>
  <verify>
    <automated>command -v typst && command -v just && just cv && test -s public/cv.pdf && file public/cv.pdf | grep -i "PDF document" && grep -c "axel.waserman@gmail.com" assets/CV.typ</automated>
  </verify>
  <acceptance_criteria>
    - `justfile` exists at repo root.
    - `grep -c "typst compile assets/CV.typ public/cv.pdf" justfile` returns `1`.
    - `wc -l < justfile` returns less than `20`.
    - `just cv` exits 0 from a clean working directory (re-runnable; idempotent).
    - `public/cv.pdf` exists.
    - `test -s public/cv.pdf` succeeds (file size > 0).
    - `file public/cv.pdf` output contains `PDF document` (proves it's a real PDF, not a stub).
    - `wc -c < public/cv.pdf` returns at least `1000` bytes (sanity floor — empty/error PDFs are far smaller).
    - `grep -c "axel.waserman@gmail.com" assets/CV.typ` returns at least `1` (email preserved in CV.typ source — Plan 05-02 must NOT scrub it during data extraction work).
  </acceptance_criteria>
  <done>`just cv` is reproducible; `public/cv.pdf` is committed and is a real, non-trivial PDF; email remains in CV.typ source.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Axel reviews bio + title + CV mirror before commit</name>
  <what-built>
    - `src/data/cv.ts` rewritten from CV.typ: bio (1-2 sentences), title (Senior Engineering Manager | Backend & Data), full work history with bullets, education, three skill groups, contact with `email: 'axel.waserman@gmail.com'` (D-17 resolved).
    - `src/components/cv/CV.tsx` minimal switch: imports `skillGroups`, renders three groups (no styling rework yet — Plan 05-05 owns that).
    - `justfile` with `cv` recipe; `public/cv.pdf` compiled from assets/CV.typ.
  </what-built>
  <how-to-verify>
    1. Open `src/data/cv.ts` and skim the data alongside `assets/CV.typ`. Confirm:
       - `bio` reads naturally and reflects the Professional Summary's tone (Axel may edit wording before commit; the file is the canonical source going forward).
       - `title` is exactly `Senior Engineering Manager | Backend & Data`.
       - `contact.email` is exactly `axel.waserman@gmail.com`.
       - Every work entry has the right `role`, `company`, `dates`, `description`, `bullets` per CV.typ.
       - Education entries are correct.
       - Three skill groups carry the right items in order.
    2. Open `public/cv.pdf` in a viewer; confirm it's the real CV (not a Typst error stub), that fonts look acceptable, and that `axel.waserman@gmail.com` appears on the rendered page.
    3. Confirm `npm run build` is green (Plan 05-05's restyle hasn't run yet; rendered CV will look unstyled in this intermediate state — that's expected).
    4. Type `approved` to continue, or describe edits to make.
  </how-to-verify>
  <resume-signal>Type `approved` once cv.ts content is correct and the build is green, OR describe specific text edits.</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm run build` exits 0 — Wave 1 ends green; Plan 05-05's centering / DownloadCVButton / WorkEntry bullets layer onto this in Wave 2.
- `just cv` is re-runnable and produces the same PDF byte-for-byte (modulo Typst metadata timestamps).
- `grep -rn "skills" src/components/cv/CV.tsx` does NOT contain a bare `skills` token in the import or JSX (only `skillGroups`).
- `grep -c "axel.waserman@gmail.com" src/data/cv.ts` returns at least `1`.
- `grep -c "<<TBD: email>>" src/data/cv.ts src/components` returns `0` (sentinel scrubbed).
</verification>

<success_criteria>
- D-10 implemented: cv.ts mirrors CV.typ verbatim.
- D-11 implemented: `justfile` + committed `public/cv.pdf` produced via `typst compile`; no CI Typst dependency.
- D-13 implemented: WorkEntry has `bullets: string[]`.
- D-14 implemented: bio = locked UI-SPEC trim of Professional Summary.
- D-15 implemented: title = exact `Senior Engineering Manager | Backend & Data`.
- D-16 implemented: skillGroups carries the three CV.typ groups verbatim; `skills` export removed entirely (no deprecated stub — Strategy A from 2026-06-04 revision).
- D-17 implemented: contact.email is the resolved string `axel.waserman@gmail.com`; no sentinel constant; type is non-nullable.
- CV.tsx switched to `skillGroups` so Wave 1 ends with `npm run build` green; Plan 05-05 layers the final styling on top.
</success_criteria>

<output>
Create `.planning/phases/05-polish/05-02-SUMMARY.md` per the summary template when done.
</output>
