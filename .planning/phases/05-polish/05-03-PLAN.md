---
phase: 05-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/icon.tsx
  - src/app/instrument-serif-400.ttf
autonomous: true
requirements: []
must_haves:
  truths:
    - "Browser tab for the deployed site shows a favicon (no /favicon.ico 404 in DevTools console) — Phase 5 SC-1."
    - "Favicon is the AW monogram in Instrument Serif on parchment (D-18)."
    - "Favicon ships via the Next.js `app/icon.tsx` ImageResponse convention only — no `public/favicon.ico` legacy fallback (D-19)."
    - "Favicon background = --color-surface oklch literal; text = --color-text oklch literal (D-20)."
    - "After `next build`, the static export contains a built icon asset that the deployed HTML references via a generated `<link rel=\"icon\" …>` tag."
    - "Path B chosen (revision 2026-06-04): Instrument Serif 400 TTF binary committed at `src/app/instrument-serif-400.ttf` and loaded synchronously at build time. No network fetch during build — deterministic, offline-safe."
  artifacts:
    - path: "src/app/icon.tsx"
      provides: "ImageResponse-based favicon generator"
      contains: "ImageResponse"
    - path: "src/app/icon.tsx"
      provides: "Inline oklch literal for surface (--color-surface)"
      contains: "oklch(97% 0.01 75)"
    - path: "src/app/icon.tsx"
      provides: "Inline oklch literal for text (--color-text)"
      contains: "oklch(18% 0.01 75)"
    - path: "src/app/instrument-serif-400.ttf"
      provides: "Instrument Serif 400 weight TTF binary used by ImageResponse fonts option"
      min_bytes: 20000
  key_links:
    - from: "src/app/icon.tsx"
      to: "src/app/instrument-serif-400.ttf"
      via: "readFile + ImageResponse fonts option"
      pattern: "instrument-serif-400.ttf"
    - from: "src/app/icon.tsx"
      to: "out/icon-*.png (or equivalent)"
      via: "Next.js build generates the icon asset"
      pattern: "<link rel=\"icon\""
---

<objective>
Ship a typographic AW favicon via the Next.js `app/icon.tsx` convention so axelwaserman.github.io displays a real browser-tab icon — closing Phase 5 Success Criterion #1.

Purpose: Implements D-18 (AW monogram in Instrument Serif), D-19 (single source via `app/icon.tsx`, no legacy `favicon.ico`), D-20 (parchment surface background, dark text).

**Revision note (2026-06-04):** The original plan offered Path A (fetch font at build) and Path B (commit TTF binary). Path B is now pre-committed for deterministic, offline-safe builds. Plan no longer evaluates Path A.

Output: One new file `src/app/icon.tsx` and one binary asset `src/app/instrument-serif-400.ttf`. At `next build` time the generator emits an icon asset Next.js auto-links into the HTML head.
</objective>

<execution_context>
@/Users/axel/code/website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-polish/05-CONTEXT.md
@.planning/phases/05-polish/05-PATTERNS.md
@.planning/phases/05-polish/05-UI-SPEC.md
@src/styles/tokens.css
@src/app/layout.tsx

<interfaces>
Existing tokens (verify in src/styles/tokens.css):
  --color-surface: oklch(97% 0.01 75)   -- parchment background
  --color-text:    oklch(18% 0.01 75)   -- near-black warm
  --font-heading:  'Instrument Serif', Georgia, serif

Existing font import in src/app/layout.tsx (use the same family inside ImageResponse):
  import { Instrument_Serif } from 'next/font/google'

Next.js convention reference (Next.js 16, App Router):
  - `src/app/icon.tsx` exports a default async function returning an `ImageResponse`.
  - Sibling exports `export const size = { width, height }` and `export const contentType` configure metadata.
  - Compatible with `output: 'export'` in Next.js 15+; emits a static asset at build time and Next auto-injects the `<link rel="icon">` tag into every page's HTML.

UI-SPEC §"Favicon Visual Contract" locked values:
  - 32 × 32 px
  - Font: Instrument Serif 400, ~22pt, letter-spacing -0.02em
  - Glyphs: "AW"
  - Background: oklch(97% 0.01 75) (inlined — CSS vars don't resolve inside ImageResponse)
  - Text color: oklch(18% 0.01 75) (inlined)
  - Centered both axes via flex
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Commit Instrument Serif 400 TTF binary at src/app/instrument-serif-400.ttf</name>
  <files>src/app/instrument-serif-400.ttf</files>
  <read_first>
    - .planning/phases/05-polish/05-PATTERNS.md (Path B reference)
    - .planning/phases/05-polish/05-UI-SPEC.md (Favicon Visual Contract — confirms Instrument Serif 400 weight)
  </read_first>
  <action>
    Download the Instrument Serif 400-weight TTF and commit it at `src/app/instrument-serif-400.ttf` (binary, ~80kb).

    Two acceptable acquisition paths (executor picks whichever is fastest and reliable):

    Acquisition path 1 — Google Fonts CSS resolver:
    - `curl -s -A "Mozilla/5.0" "https://fonts.googleapis.com/css2?family=Instrument+Serif&display=swap"` returns CSS with a `url(...)` pointing at `https://fonts.gstatic.com/s/instrumentserif/...`. Extract the URL and `curl` it to `src/app/instrument-serif-400.ttf`. Confirm the response Content-Type is `font/ttf` (or the file size > 20kb).

    Acquisition path 2 — direct GitHub release:
    - Instrument Serif lives at `https://github.com/Instrument/instrument-serif`. Download the regular weight TTF from the repo's `fonts/ttf/` directory (or a release artifact) and place it at the target path.

    Do NOT use Path A (build-time fetch from Google Fonts) — that approach was rejected during the 2026-06-04 revision in favor of deterministic offline-safe builds.

    The file is a binary asset; commit it as-is. License confirmation: Instrument Serif is SIL Open Font License 1.1 — redistribution is allowed; ensure no surprise LICENSE / OFL.txt requirement is missed (if Google Fonts attribution is required, document it in the SUMMARY.md but no other file changes are needed for SIL OFL compliance — the OFL allows bundling without copyleft restrictions on the consuming project).
  </action>
  <verify>
    <automated>test -f src/app/instrument-serif-400.ttf && test -s src/app/instrument-serif-400.ttf && file src/app/instrument-serif-400.ttf | grep -iE "TrueType|OpenType|font" && wc -c < src/app/instrument-serif-400.ttf</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/app/instrument-serif-400.ttf` succeeds.
    - `test -s src/app/instrument-serif-400.ttf` succeeds (non-zero file size).
    - `wc -c < src/app/instrument-serif-400.ttf` returns at least `20000` (sanity floor; full Instrument Serif 400 TTF is ~80–120kb).
    - `file src/app/instrument-serif-400.ttf` output contains `TrueType` OR `OpenType` OR `font` (file command's font detection — any of these is acceptable since some TTFs are reported as OpenType depending on table layout).
  </acceptance_criteria>
  <done>Instrument Serif 400 TTF is committed at the locked path; ready for icon.tsx to load synchronously.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create src/app/icon.tsx with Instrument Serif "AW" ImageResponse (Path B — load TTF from disk)</name>
  <files>src/app/icon.tsx</files>
  <read_first>
    - src/app/layout.tsx (font loading pattern — Instrument_Serif from next/font/google, weight ['400'])
    - src/styles/tokens.css (oklch values to inline)
    - src/app/instrument-serif-400.ttf (committed in Task 1; loaded synchronously here)
    - .planning/phases/05-polish/05-PATTERNS.md (section "src/app/icon.tsx (NEW — Next.js convention file)")
    - .planning/phases/05-polish/05-UI-SPEC.md (Favicon Visual Contract — full table)
    - .planning/phases/05-polish/05-CONTEXT.md (D-18, D-19, D-20)
  </read_first>
  <action>
    Create `src/app/icon.tsx`. Pattern per Next.js docs + UI-SPEC §Favicon Visual Contract:
    - Import `ImageResponse` from `next/og`.
    - Import `readFile` from `node:fs/promises` and `join` from `node:path`.
    - Export `size = { width: 32, height: 32 } as const` and `contentType = 'image/png' as const`.
    - Default-export an `async function Icon()` that:
      1. Reads the TTF binary: `const fontPath = join(process.cwd(), 'src/app/instrument-serif-400.ttf'); const fontData = await readFile(fontPath);`
      2. Constructs the ImageResponse with `fonts: [{ name: 'Instrument Serif', data: fontData, weight: 400, style: 'normal' }]` in the options (second argument), alongside the spread of `size`.
      3. Returns an ImageResponse whose JSX is a single `<div>` with style:
         - `width: '100%'`, `height: '100%'`
         - `display: 'flex'`, `alignItems: 'center'`, `justifyContent: 'center'`
         - `background: 'oklch(97% 0.01 75)'` (inline literal — no CSS vars)
         - `color: 'oklch(18% 0.01 75)'` (inline literal)
         - `fontSize: 22` (px — Satori font sizing inside ImageResponse uses px-equivalent, NOT pt; 22 maps to UI-SPEC's "~22pt rendering ~24px height in 32×32")
         - `letterSpacing: '-0.02em'`
         - `fontFamily: 'Instrument Serif'`
      - Children: literal string `AW`.

    Path B rationale (Revision 2026-06-04): Loading the TTF from disk via `readFile` makes the build deterministic — no external network call to fonts.gstatic.com during `next build`, no flake risk, no sandbox issues. The ~80kb TTF binary is a one-time commit.

    Do NOT also ship `public/favicon.ico` — D-19 explicitly says "Do not also ship `public/favicon.ico` unless legacy support proves necessary during implementation — keep it simple". Single source of truth = `src/app/icon.tsx`.

    Static-export compatibility: per CONTEXT.md D-19, `ImageResponse` IS supported under `output: 'export'` in Next.js 15+ (project is on 16.2.6). Verify by inspecting `out/` after build — Next emits a `_next/static/media/icon.<hash>.png` (or similar path) and injects `<link rel="icon">` into every generated HTML page.
  </action>
  <verify>
    <automated>npm run build && find out -name "icon*.png" -o -name "icon*.svg" | head -5 && grep -r 'rel="icon"' out/index.html | head -3 && grep -c "instrument-serif-400.ttf" src/app/icon.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/icon.tsx` exists.
    - `grep -c "ImageResponse" src/app/icon.tsx` returns at least `1`.
    - `grep -c "from 'next/og'" src/app/icon.tsx` returns `1`.
    - `grep -c "node:fs/promises" src/app/icon.tsx` returns `1` (Path B file read).
    - `grep -c "instrument-serif-400.ttf" src/app/icon.tsx` returns `1` (Path B file path reference).
    - `grep -c "oklch(97% 0.01 75)" src/app/icon.tsx` returns `1` (D-20 surface inlined).
    - `grep -c "oklch(18% 0.01 75)" src/app/icon.tsx` returns `1` (D-20 text inlined).
    - `grep -c "'Instrument Serif'" src/app/icon.tsx` returns at least `1` (font family).
    - `grep -c "letterSpacing: '-0.02em'" src/app/icon.tsx` returns `1` (UI-SPEC tracking).
    - `grep -E ">\\s*AW\\s*<" src/app/icon.tsx` returns at least `1` match (literal "AW" glyphs in JSX).
    - `grep -c "size = " src/app/icon.tsx` returns `1` and `grep -c "32" src/app/icon.tsx` confirms the 32×32 default.
    - `npm run build` exits 0.
    - After build, `find out -type f \( -name "icon*.png" -o -name "icon*.svg" -o -name "icon*.jpg" \)` returns at least one file (Next emits the icon asset into the static export).
    - `grep -l 'rel="icon"' out/*.html out/**/*.html 2>/dev/null` returns at least `out/index.html` (Next.js auto-injected the link).
    - The repo does NOT contain `public/favicon.ico` (D-19): `test ! -f public/favicon.ico`.
  </acceptance_criteria>
  <done>`next build` succeeds offline, an icon asset is in `out/`, every page's HTML head includes `<link rel="icon">`, and the favicon visually renders "AW" in Instrument Serif on parchment (verified at deploy time by Plan 05-08, NOT in this plan — this plan only verifies the build pipeline produces the asset).</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 with no network access required (Path B determinism check).
- The static export `out/` directory contains exactly one favicon asset emitted by Next.js (path varies by Next.js version: typically `_next/static/media/icon.*.png` or `icon.png` at the root depending on convention handler version).
- Every generated `out/*.html` includes a `<link rel="icon"` tag.
- No `public/favicon.ico` exists.
- `src/app/instrument-serif-400.ttf` exists and is at least 20kb.
- Visual verification of glyph rendering happens at deploy verification time (Plan 05-08); this plan provides the asset, not the live URL check.
</verification>

<success_criteria>
- D-18 implemented: AW monogram in Instrument Serif.
- D-19 implemented: single source via `app/icon.tsx`; no `public/favicon.ico`; static-export compatible.
- D-20 implemented: parchment surface background + dark text, both inlined as oklch literals.
- Path B (deterministic offline-safe build) implemented per 2026-06-04 revision; no Google Fonts network dependency at build time.
- Phase 5 SC-1 verifiable: build emits an icon asset and HTML references it via `<link rel="icon">`.
</success_criteria>

<output>
Create `.planning/phases/05-polish/05-03-SUMMARY.md` per the summary template when done. Note that Path B was used (committed TTF binary; no build-time network fetch).
</output>
