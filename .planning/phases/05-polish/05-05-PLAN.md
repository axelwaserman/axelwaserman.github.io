---
phase: 05-polish
plan: 05
type: execute
wave: 2
depends_on:
  - 05-02
files_modified:
  - src/components/cv/CV.tsx
  - src/components/cv/WorkEntry.tsx
  - src/components/cv/SkillGroupList.tsx
  - src/components/cv/DownloadCVButton.tsx
  - src/components/cv/SkillsList.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "CV section is centered (max-w-3xl mx-auto, eyebrow labels centered above each block) per D-02 — no more 20%/1fr two-column grid."
    - "WorkEntry renders bullets per D-13: ul with marker:text-[var(--color-muted)], list-disc list-outside pl-5; bold-prefix detected by splitting on first colon within first 60 chars."
    - "Skills section renders three SkillGroup blocks (D-16), each = category heading (Sora 600) + comma-separated items (Sora 400) via the new SkillGroupList component."
    - "A new 'Download CV (PDF)' button (DownloadCVButton.tsx) sits between Work and Education blocks per D-12 + UI-SPEC Download Button Visual Contract (transparent bg, accent-color bottom hairline border, inline SVG download icon, download attribute on the anchor)."
    - "SkillsList.tsx is deleted from the codebase (deprecated per UI-SPEC inventory) once SkillGroupList replaces it in CV.tsx."
  artifacts:
    - path: "src/components/cv/CV.tsx"
      provides: "Centered CV layout consuming skillGroups via SkillGroupList + DownloadCVButton"
      contains: "max-w-3xl mx-auto"
    - path: "src/components/cv/WorkEntry.tsx"
      provides: "Bullet rendering with first-colon bold prefix split"
      contains: "list-disc list-outside pl-5"
    - path: "src/components/cv/SkillGroupList.tsx"
      provides: "Group-by-category renderer for SkillGroup[]"
      contains: "groups.map"
    - path: "src/components/cv/DownloadCVButton.tsx"
      provides: "Anchor-with-download semantic + UI-SPEC styling"
      contains: 'href="/cv.pdf"'
  key_links:
    - from: "src/components/cv/CV.tsx"
      to: "src/data/cv.ts"
      via: "imports skillGroups + workEntries + educationEntries"
      pattern: "skillGroups"
    - from: "src/components/cv/DownloadCVButton.tsx"
      to: "public/cv.pdf"
      via: "anchor href + download attribute"
      pattern: 'href="/cv.pdf"'
---

<objective>
Layer the final CV section UI on top of Plan 05-02's data shape: introduce the SkillGroupList component (replacing the inline `skillGroups.map` Plan 05-02 left behind), add the DownloadCVButton between Work and Education, render WorkEntry bullets, re-center the entire CV section per D-02, and delete the now-unused SkillsList component. This delivers Phase 5 SC-3 ("CV section content reflects Axel's real history") at the visual layer.

Purpose: D-02 (re-center CV), D-12 (secondary download CTA at end of experience block), D-13 (render WorkEntry bullets), D-16 (render skill groups). Email TBD UX is no longer a concern (D-17 resolved 2026-06-04 — Plan 05-02 hard-codes the real email).

**Revision note (2026-06-04):** Strategy A applies — Plan 05-02 already switched CV.tsx to import `skillGroups` and removed the `skills` export entirely (no deprecated stub). This plan now starts from "CV.tsx imports skillGroups but renders an unstyled / SkillsList-based fallback". The work here is: (a) introduce SkillGroupList, (b) re-center, (c) add DownloadCVButton, (d) render WorkEntry bullets, (e) delete SkillsList.tsx. No `skills`-stub-removal step needed — Plan 05-02 already handled it.

Output: Updated CV section components rendering Axel's real CV with bullets, skill groups, secondary download button, and centered layout.
</objective>

<execution_context>
@/Users/axel/code/website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-polish/05-CONTEXT.md
@.planning/phases/05-polish/05-PATTERNS.md
@.planning/phases/05-polish/05-UI-SPEC.md
@.planning/phases/05-polish/05-02-SUMMARY.md
@src/data/cv.ts
@src/components/cv/CV.tsx
@src/components/cv/WorkEntry.tsx
@src/components/cv/EducationEntry.tsx
@src/components/cv/SkillsList.tsx
@src/components/hero/Hero.tsx

<interfaces>
After Plan 05-02 lands, src/data/cv.ts exports:
  - interface WorkEntry { role: string; company: string; dates: string; description: string; bullets: string[] }
  - interface SkillGroup { category: string; items: string[] }
  - interface Contact { github: string; linkedin: string; email: string }   // non-null email
  - export const bio: string
  - export const title: string  // 'Senior Engineering Manager | Backend & Data'
  - export const workEntries: WorkEntry[]
  - export const educationEntries: EducationEntry[]
  - export const skillGroups: SkillGroup[]
  - export const contact: Contact   // contact.email = 'axel.waserman@gmail.com'
  // NOTE: no `skills` export, no `EMAIL_TBD_SENTINEL` — both removed in Plan 05-02 per Strategy A + D-17 resolution.

After Plan 05-02 lands, src/components/cv/CV.tsx is in an INTERMEDIATE state:
  - imports `skillGroups` (not `skills`) from `@/data/cv`
  - renders three blocks; the Skills block is either:
    (a) `skillGroups.map(g => <SkillsList key={g.category} items={g.items} />)`, OR
    (b) an inline `skillGroups.map(g => <div><h4>{g.category}</h4><p>{g.items.join(', ')}</p></div>)` block
  - Phase 2 layout is otherwise unchanged: max-w-4xl, three two-column grid wrappers, no DownloadCVButton

This plan replaces whichever intermediate Plan 05-02 left with the proper SkillGroupList + centered layout + DownloadCVButton.

Hero.tsx already has the 'Download CV' anchor (lines 45-51 — base styling pattern). DownloadCVButton.tsx clones that semantic but rebrands the visuals per UI-SPEC §"Download Button Visual Contract".

UI-SPEC inline SVG path data for the download icon is locked verbatim in 05-UI-SPEC.md §"Download Button Visual Contract":
  - viewBox 0 0 24 24, fill none, stroke currentColor, stroke-width 1.5, round caps + joins
  - paths: M12 3v13 / m7 11 5 5 5-5 / M5 21h14
  - Render at width=16 height=16 inside the button

UI-SPEC layout values for the centered CV section (locked):
  - Outer container max-w-3xl mx-auto
  - Block separation mb-12
  - Entry separation within a block space-y-8
  - Eyebrow-to-first-entry mt-6
  - Eyebrow text-center, Sora 600, --text-ui, --color-muted, uppercase, tracking-[0.08em]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WorkEntry bullets + SkillGroupList + DownloadCVButton (presentational components)</name>
  <files>src/components/cv/WorkEntry.tsx, src/components/cv/SkillGroupList.tsx, src/components/cv/DownloadCVButton.tsx</files>
  <read_first>
    - src/components/cv/WorkEntry.tsx (current shape — preserve role h3 + company-dates p + description p; add bullets ul AFTER description)
    - src/components/cv/SkillsList.tsx (component shape analog — interface ComponentNameProps, default export, named function)
    - src/components/cv/EducationEntry.tsx (presentational pattern analog)
    - src/components/hero/Hero.tsx lines 45-51 (existing Download CV anchor — DownloadCVButton starts from this semantic)
    - .planning/phases/05-polish/05-PATTERNS.md (sections "src/components/cv/WorkEntry.tsx (MODIFIED — render bullets)", "src/components/cv/SkillGroupList.tsx (NEW)", "src/components/cv/DownloadCVButton.tsx (NEW)", and Pattern 5 "Token consumption in Tailwind v4 arbitrary values")
    - .planning/phases/05-polish/05-UI-SPEC.md (CV section / Work entry rendering / Skill groups / Download Button Visual Contract — every locked value)
    - /Users/axel/.claude/rules/typescript/coding-style.md (interface for props; no React.FC; explicit prop types)
  </read_first>
  <behavior>
    WorkEntry.tsx:
    - When entry.bullets.length === 0, render the same JSX as today (no bullet list element added).
    - When entry.bullets.length > 0, render a ul directly below the description.
    - For each bullet string: if bullet.indexOf(':') returns an index greater than 0 and less than 60, split into prefix = bullet.slice(0, colonIndex + 1) and rest = bullet.slice(colonIndex + 1); render strong containing prefix, then rest as plain text.
    - Otherwise render the bullet as plain text inside the li.

    SkillGroupList.tsx:
    - Accepts groups: SkillGroup[].
    - Renders one block per group: category heading (h4, Sora 600, --color-text, --text-body, mb-2), then a single p containing group.items.join(', ') at --text-ui Sora 400 --color-text leading-1.5 max-w-[60ch].
    - Wrapper div uses space-y-6 between groups.

    DownloadCVButton.tsx:
    - Renders an anchor with href="/cv.pdf" + download (boolean attribute).
    - Inline SVG icon (UI-SPEC locked path data) on the left, label "Download CV (PDF)" on the right.
    - inline-flex items-center gap-2, py-3 px-5, transparent background, border-b 1px var(--color-accent) at rest, hover:border-b-2 with 150ms ease-out, focus outline 2px var(--color-accent) outline-offset-4, rounded-none.
    - Label text: Sora 600 (font-semibold), --text-ui, --color-text.
    - Mobile (less than 640px): w-full justify-center; sm:w-auto sm:justify-start.
  </behavior>
  <action>
    WorkEntry.tsx (modify): Keep the existing article element, role h3, company-dates p, description p. After the description p, conditionally render a ul element when entry.bullets.length is greater than 0. The ul className per UI-SPEC §"CV section — Work entry rendering": "mt-3 space-y-2 list-disc list-outside pl-5 marker:text-[var(--color-muted)]". Each li className: "text-[length:var(--text-body)] text-[var(--color-text)] max-w-[65ch] leading-[1.6]". Inside each li, apply the first-colon split per UI-SPEC's "colonIndex greater than 0 and less than 60" rule. Use a stable React key (index inside map is acceptable here because bullet order is data-driven and immutable).

    SkillGroupList.tsx (NEW): Accepts groups: SkillGroup[]. Outer div className="space-y-6". Per group, render a div with key=group.category containing an h4 (font-semibold, text-[length:var(--text-body)], text-[var(--color-text)], mb-2) and a p (text-[length:var(--text-ui)] text-[var(--color-text)] max-w-[60ch] leading-[1.5]) with content group.items.join(", "). Pure presentational. Default export. Import type SkillGroup from '@/data/cv'.

    DownloadCVButton.tsx (NEW): Anchor element with href="/cv.pdf", boolean download attribute, and aria-label="Download CV (PDF)". className="inline-flex items-center gap-2 py-3 px-5 bg-transparent text-[length:var(--text-ui)] font-semibold text-[var(--color-text)] border-0 border-b border-[var(--color-accent)] rounded-none hover:border-b-2 transition-[border-bottom-width] duration-150 ease-out focus:outline-2 focus:outline-[var(--color-accent)] focus:outline-offset-4 w-full justify-center sm:w-auto sm:justify-start". Children: inline SVG icon followed by a span with text "Download CV (PDF)". The SVG must use JSX-cased attributes (strokeWidth, strokeLinecap, strokeLinejoin, aria-hidden="true"), viewBox="0 0 24 24", fill="none", stroke="currentColor", strokeWidth="1.5", width="16", height="16", and the three path d values from UI-SPEC verbatim: "M12 3v13", "m7 11 5 5 5-5", "M5 21h14". Default export, named function.
  </action>
  <verify>
    <automated>npx tsc --noEmit; grep -c "list-disc list-outside pl-5" src/components/cv/WorkEntry.tsx; grep -c "groups.map" src/components/cv/SkillGroupList.tsx; grep -c 'href="/cv.pdf"' src/components/cv/DownloadCVButton.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist: src/components/cv/WorkEntry.tsx, src/components/cv/SkillGroupList.tsx, src/components/cv/DownloadCVButton.tsx.
    - npx tsc --noEmit exits 0.
    - grep -c "list-disc list-outside pl-5" src/components/cv/WorkEntry.tsx returns 1.
    - grep -c "marker:text-\[var(--color-muted)\]" src/components/cv/WorkEntry.tsx returns 1.
    - grep -c "max-w-\[65ch\]" src/components/cv/WorkEntry.tsx returns at least 1 (preserved or in new bullets li).
    - grep -c "entry.bullets" src/components/cv/WorkEntry.tsx returns at least 2 (length check + map).
    - grep -c "groups.map" src/components/cv/SkillGroupList.tsx returns 1.
    - grep -c "items.join" src/components/cv/SkillGroupList.tsx returns 1.
    - grep -c "space-y-6" src/components/cv/SkillGroupList.tsx returns 1.
    - grep -c 'href="/cv.pdf"' src/components/cv/DownloadCVButton.tsx returns 1.
    - grep -c "download" src/components/cv/DownloadCVButton.tsx returns at least 1 (boolean attribute on the anchor).
    - grep -c "Download CV (PDF)" src/components/cv/DownloadCVButton.tsx returns at least 1.
    - grep -c "border-\[var(--color-accent)\]" src/components/cv/DownloadCVButton.tsx returns at least 1.
    - grep -c 'M12 3v13' src/components/cv/DownloadCVButton.tsx returns 1 (icon path data verbatim).
  </acceptance_criteria>
  <done>Three presentational components exist, type-check, and follow UI-SPEC styling tokens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-wire CV.tsx with centered layout, SkillGroupList, and DownloadCVButton; delete SkillsList.tsx</name>
  <files>src/components/cv/CV.tsx, src/components/cv/SkillsList.tsx</files>
  <read_first>
    - src/components/cv/CV.tsx (post-Plan-05-02 INTERMEDIATE state — imports skillGroups already; Skills block is either an inline map or three SkillsList renders; Phase 2 grid still in place; no DownloadCVButton)
    - src/data/cv.ts (post-Plan-05-02 — confirms skillGroups, no skills, no sentinel)
    - .planning/phases/05-polish/05-PATTERNS.md (section "src/components/cv/CV.tsx (MODIFIED — re-center, render groups, add CTA)")
    - .planning/phases/05-polish/05-UI-SPEC.md (CV section visual contract — every locked value)
    - /Users/axel/.claude/rules/typescript/coding-style.md
  </read_first>
  <behavior>
    CV.tsx:
    - Outer section preserves id="cv", aria-labelledby="cv-heading", py-[var(--space-section)], px-6, scroll-mt-16 (UI-SPEC Pattern 2 "Section container shape").
    - Section's max-w changes from max-w-4xl to max-w-3xl mx-auto.
    - The Phase 2 grid grid-cols-1 sm:grid-cols-[20%_1fr] gap-y-8 sm:gap-x-12 wrapper is REMOVED on all three blocks.
    - Each block (Work / Skills / Education): eyebrow label centered above the entries, then entries below.
    - Eyebrow className stays the existing tokens but adds text-center: "text-center text-[length:var(--text-ui)] font-semibold text-[var(--color-muted)] uppercase tracking-[0.08em]".
    - Vertical rhythm: mb-12 between blocks; entries div uses mt-6 (gap from eyebrow to first entry); existing space-y-12 between work entries and space-y-8 between education entries preserved (UI-SPEC says space-y-8 between entries within a block; planner aligns work to space-y-8 too unless preserving 12 better matches the bullets-heavy density — pick space-y-8 for consistency with UI-SPEC).
    - Block ORDER per UI-SPEC: Work → DownloadCVButton (centered, mt-12 mb-12) → Education → Skills.
    - DownloadCVButton wrapper: a div with className="flex justify-center mt-12 mb-12".
    - Skills block uses SkillGroupList groups={skillGroups}; the Plan-05-02 intermediate (either inline map or three SkillsList renders) is REPLACED with a single <SkillGroupList groups={skillGroups} /> render.
    - SkillsList import removed (Plan 05-02 may have left it in place if shape (a) was used).

    src/components/cv/SkillsList.tsx:
    - Delete the file. Nothing else imports it (verify with grep).
  </behavior>
  <action>
    CV.tsx (rewrite the three-block body): Drop all three grid grid-cols-1 sm:grid-cols-[20%_1fr] wrappers. Keep the outer section element (preserve every existing className token EXCEPT change max-w-4xl to max-w-3xl mx-auto). Keep the sr-only h2#cv-heading. Replace the Work block with a div className="mb-12" containing a div className="text-center text-[length:var(--text-ui)] font-semibold text-[var(--color-muted)] uppercase tracking-[0.08em]" with text "Work", then a div className="mt-6 space-y-8" mapping over workEntries with WorkEntry. After the Work block, insert a div className="flex justify-center mt-12 mb-12" containing DownloadCVButton. Then the Education block with the same eyebrow + entries pattern (eyebrow text "Education", entries map educationEntries to EducationEntry, mb-12 on the wrapper). Then the Skills block using SkillGroupList groups={skillGroups} (no eyebrow wrapper layout change — same eyebrow above + SkillGroupList below).

    Imports: ensure the file imports SkillGroupList from './SkillGroupList' (added) and DownloadCVButton from './DownloadCVButton' (added). REMOVE any `import SkillsList from './SkillsList'` line if Plan 05-02 left it (Plan 05-02 shape (a) keeps SkillsList; shape (b) doesn't). The data import line should be `import { workEntries, educationEntries, skillGroups } from '@/data/cv'` — already correct from Plan 05-02; do not regress.

    src/components/cv/SkillsList.tsx: delete the file (use rm). Confirm with grep that no other file imports it.

    Anti-pattern guard: do NOT introduce a centered paragraph for entry text. Each entry's internal layout stays left-aligned per UI-SPEC: "the block sits inside the centered container; entries hang off the container's left edge." The CV centering is BLOCK-level, not text-level.
  </action>
  <verify>
    <automated>npx tsc --noEmit; npm run build; grep -c "max-w-3xl mx-auto" src/components/cv/CV.tsx; grep -c "skillGroups" src/components/cv/CV.tsx; grep -c "DownloadCVButton" src/components/cv/CV.tsx; test ! -f src/components/cv/SkillsList.tsx; grep -rn "from './SkillsList'\|from '@/components/cv/SkillsList'" src/</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0.
    - npm run build exits 0.
    - grep -c "max-w-3xl mx-auto" src/components/cv/CV.tsx returns 1.
    - grep -c "max-w-4xl" src/components/cv/CV.tsx returns 0 (old constraint removed).
    - grep -c "grid-cols-\[20%_1fr\]" src/components/cv/CV.tsx returns 0 (Phase 2 grid removed).
    - grep -c "skillGroups" src/components/cv/CV.tsx returns at least 1 (import + usage).
    - grep -c "import SkillsList" src/components/cv/CV.tsx returns 0.
    - grep -c "import SkillGroupList" src/components/cv/CV.tsx returns 1.
    - grep -c "<SkillGroupList" src/components/cv/CV.tsx returns 1.
    - grep -c "DownloadCVButton" src/components/cv/CV.tsx returns at least 2 (import + render).
    - grep -c "text-center" src/components/cv/CV.tsx returns at least 3 (one per eyebrow: Work, Education, Skills).
    - test ! -f src/components/cv/SkillsList.tsx (file deleted).
    - grep -rn "from './SkillsList'" src returns no matches.
    - grep -rn "from '@/components/cv/SkillsList'" src returns no matches.
    - grep -c "export const skills" src/data/cv.ts returns 0 (Plan 05-02 already removed; this is a regression sentry — must still be 0).
    - grep -c "export const skillGroups" src/data/cv.ts returns 1 (preserved from Plan 05-02).
    - The rendered HTML in out/index.html includes the literal text "Senior Engineering Manager" and "Engineering Leadership" and "Backend & Systems Architecture" and "Hands-on Tech Stack" after npm run build (proves real CV content reaches the page).
  </acceptance_criteria>
  <done>CV section renders Axel's real CV in centered layout with bullets, skill groups, and the secondary Download CV (PDF) button; SkillsList.tsx is gone; cv.ts has only the canonical skillGroups export.</done>
</task>

</tasks>

<verification>
- npm run build exits 0.
- npx tsc --noEmit exits 0.
- out/index.html (after build) contains: "Senior Engineering Manager", "Engineering Leadership", "Backend & Systems Architecture", "Hands-on Tech Stack", "Contentsquare", "PwC France", "Panodyssey", "ESILV", "Griffith".
- grep -rn "Placeholder " src/data/cv.ts returns no matches.
- grep -rn "Lorem\|placeholder name" src/components/cv/ returns no matches.
- File src/components/cv/SkillsList.tsx no longer exists.
</verification>

<success_criteria>
- D-02 implemented for CV section: max-w-3xl mx-auto, eyebrow-centered, entries left-aligned within centered block.
- D-12 implemented: Download CV (PDF) button between Work and Education, anchor with download attribute, UI-SPEC Download Button Visual Contract styling.
- D-13 implemented: WorkEntry bullets render with first-colon bold-prefix split.
- D-16 implemented: SkillGroupList renders three CV.typ groups; SkillsList.tsx deleted.
- No regressions on Plan 05-02's data layer (cv.ts still has only the canonical skillGroups export, real email, no sentinel).
</success_criteria>

<output>
Create `.planning/phases/05-polish/05-05-SUMMARY.md` per the summary template when done.
</output>
