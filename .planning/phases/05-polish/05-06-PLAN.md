---
phase: 05-polish
plan: 06
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-04
files_modified:
  - src/components/hero/HeroMandala.tsx
  - src/components/hero/MandalaSVG.tsx
  - src/components/hero/HeroMandalaControls.tsx
  - src/components/hero/Hero.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Hero section is two-column on desktop (≥1024px): left = existing text + CTAs (preserved Phase 2 D-01 left-aligned), right = mandala SVG + controls (D-01)."
    - "Hero is two-column on tablet (640–1023px) at 60/40, and stacked on mobile (<640px) with mandala below the CTAs."
    - "HeroMandala is a Client Component ('use client') that owns (n,k) state, picks a random pair from CURATED_PAIRS on mount, owns scroll-driven rotation gated by useReducedMotion, and resets the pair via IntersectionObserver when the hero scrolls out of view (D-06, D-07, D-08, D-09)."
    - "MandalaSVG is a presentational component (no 'use client' of its own) consuming generateLines(n, k) from src/lib/mandala (Plan 05-04)."
    - "HeroMandalaControls renders n + k number inputs (clamp-on-blur, min/max per UI-SPEC), a refresh button (UI-SPEC SVG path), and the (n,k) caption."
    - "Mandala uses transform: rotate(...) only — compositor-friendly per web/performance.md."
    - "Scroll handler is passive: true and writes only latestScrollY; rAF loop reads latestScrollY and writes the transform — NO layout reads inside the rAF (UI-SPEC Performance Lock)."
    - "will-change: transform is set on the SVG ONLY while the hero is in viewport (IntersectionObserver-driven), removed when hero scrolls out — avoids permanent compositor layer."
    - "When prefers-reduced-motion is true, the rAF loop NEVER starts and the SVG renders static at 0deg rotation; controls remain functional."
    - "Hero h1 plus h1-adjacent metadata in layout.tsx remain consistent (planner finalizes 'Axel W' or 'Axel Waserman' in the h1 — UI-SPEC accepts either; layout.tsx already chose 'Axel Waserman' in Plan 05-01, so the h1 should align here)."
  artifacts:
    - path: "src/components/hero/HeroMandala.tsx"
      provides: "Client Component owning state, scroll loop, IntersectionObserver, controls"
      contains: "'use client'"
    - path: "src/components/hero/MandalaSVG.tsx"
      provides: "Pure presentational SVG renderer"
      contains: "generateLines"
    - path: "src/components/hero/HeroMandalaControls.tsx"
      provides: "n input, k input, refresh button, caption"
      contains: "aria-label"
    - path: "src/components/hero/Hero.tsx"
      provides: "Two-column layout embedding HeroMandala in right column"
      contains: "HeroMandala"
  key_links:
    - from: "src/components/hero/HeroMandala.tsx"
      to: "src/lib/mandala.ts"
      via: "imports CURATED_PAIRS, pickRandomPair"
      pattern: "from '@/lib/mandala'"
    - from: "src/components/hero/MandalaSVG.tsx"
      to: "src/lib/mandala.ts"
      via: "imports generateLines"
      pattern: "generateLines"
    - from: "src/components/hero/HeroMandala.tsx"
      to: "src/hooks/useReducedMotion.ts"
      via: "imports useReducedMotion hook"
      pattern: "useReducedMotion"
    - from: "src/components/hero/Hero.tsx"
      to: "src/components/hero/HeroMandala.tsx"
      via: "renders the client component in the right column"
      pattern: "<HeroMandala"
---

<objective>
Build the interactive Times-Tables-on-a-Circle mandala — the centerpiece anti-template visual of Phase 5 — and embed it in the hero's right column. Pure SVG, scroll-driven rotation, deterministic algorithm (Plan 05-04), interactive (n,k) controls, refresh button, scroll-out reset, useReducedMotion gate. No animation library; no math library.

Purpose: Phase 5 Success Criterion #2 — "The page features a deliberate mandala-style decorative pattern integrated into the design". Decisions: D-01 (placement), D-03 (algorithm — already shipped via Plan 05-04), D-04 (curated set), D-05 (controls), D-06 (scroll reset), D-07 (scroll-driven rotation), D-08 (reduced-motion), D-09 (Client Component).

Output: Three new hero components + Hero.tsx restructured into a two-column layout with the mandala on the right.
</objective>

<execution_context>
@/Users/axel/code/website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-polish/05-CONTEXT.md
@.planning/phases/05-polish/05-PATTERNS.md
@.planning/phases/05-polish/05-UI-SPEC.md
@.planning/phases/05-polish/05-01-SUMMARY.md
@.planning/phases/05-polish/05-04-SUMMARY.md
@src/components/hero/Hero.tsx
@src/components/ui/FadeUp.tsx
@src/components/header/Header.tsx
@src/hooks/useReducedMotion.ts
@src/lib/mandala.ts

<interfaces>
From src/lib/mandala.ts (Plan 05-04, already verified):
  - export interface ChordLine { x1: number; y1: number; x2: number; y2: number }
  - export interface MandalaPair { n: number; k: number }
  - export const CURATED_PAIRS: readonly MandalaPair[]
  - export function generateLines(n: number, k: number): ChordLine[]
  - export function pickRandomPair(exclude?: MandalaPair, rng?: () => number): MandalaPair

From src/hooks/useReducedMotion.ts:
  - export function useReducedMotion(): boolean

Existing Client Component analogs (DO read these before writing):
  - src/components/ui/FadeUp.tsx: 'use client', useEffect with reduced check + early return, IntersectionObserver pattern
  - src/components/header/Header.tsx: passive scroll listener pattern with cleanup

UI-SPEC locked values (consult 05-UI-SPEC.md §"Mandala Visual Contract"):
  - viewBox 0 0 1000 1000, center (500, 500), radius 460
  - stroke = var(--color-text), stroke-opacity 0.35, stroke-width 1, stroke-linecap round, fill none
  - container aspect-ratio 1/1; max width on desktop min(50vw, 600px); mobile min(70vw, 360px)
  - rotation rate: 1 deg per 8px scrolled (rotation = scrollY * 0.125 deg)
  - n input min=3 max=500 step=1; k input min=1 max=(n-1) step=1; clamp on blur; live re-render on change
  - Refresh button: 44×44 touch target, circular bg, icon stroke var(--color-muted) at rest -> var(--color-accent) on hover, transform scale(0.96) for 80ms on active

Inline SVG path data for the refresh icon (UI-SPEC verbatim):
  - viewBox 0 0 24 24, fill none, stroke currentColor, stroke-width 1.5, round caps + joins, aria-hidden true
  - paths: M3 12a9 9 0 0 1 15.5-6.3L21 8 / M21 3v5h-5 / M21 12a9 9 0 0 1-15.5 6.3L3 16 / M3 21v-5h5
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: MandalaSVG presentational component</name>
  <files>src/components/hero/MandalaSVG.tsx</files>
  <read_first>
    - src/lib/mandala.ts (the contract from Plan 05-04 — generateLines + ChordLine)
    - src/components/cv/EducationEntry.tsx (presentational analog)
    - .planning/phases/05-polish/05-PATTERNS.md (section "src/components/hero/MandalaSVG.tsx (NEW — presentational)")
    - .planning/phases/05-polish/05-UI-SPEC.md (Mandala Visual Contract — Stroke + Geometry tables)
  </read_first>
  <behavior>
    - Pure presentational. NO 'use client' directive.
    - Props: { n: number; k: number }.
    - Renders an svg element with viewBox="0 0 1000 1000", preserveAspectRatio="xMidYMid meet", role="img", and an aria-label that includes the dynamic n + k values per UI-SPEC §Copywriting Contract: "Decorative pattern: chords drawn between {n} points on a circle, connecting each point to its multiple of {k}".
    - Computes lines via generateLines(n, k) and maps to one line element per entry.
    - Each line: x1, y1, x2, y2 from the ChordLine record; stroke="var(--color-text)" strokeOpacity={0.35} strokeWidth={1} strokeLinecap="round" fill="none".
    - The svg element has no fill, no drop-shadow, no filter, no glow (anti-template gate from UI-SPEC).
    - Container styling is the parent's responsibility; this component renders ONLY the svg element with className="w-full h-full" so the parent's aspect-ratio container governs size.
  </behavior>
  <action>
    Create src/components/hero/MandalaSVG.tsx. Import type ChordLine and the function generateLines from '@/lib/mandala'. Define interface MandalaSVGProps { n: number; k: number }. Default-export a named function MandalaSVG that destructures { n, k }, calls generateLines(n, k), and returns the svg element described in behavior. Use a key on each line element (index from map is acceptable — line order is data-driven). Spread x1/y1/x2/y2 from each ChordLine record onto each line element. Sora-styling not applicable (this file has no text). No state. No effects.
  </action>
  <verify>
    <automated>npx tsc --noEmit; grep -c "generateLines" src/components/hero/MandalaSVG.tsx; grep -c "viewBox=\"0 0 1000 1000\"" src/components/hero/MandalaSVG.tsx; grep -c 'role="img"' src/components/hero/MandalaSVG.tsx; grep -c "'use client'" src/components/hero/MandalaSVG.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/hero/MandalaSVG.tsx exists.
    - npx tsc --noEmit exits 0.
    - grep -c "generateLines" src/components/hero/MandalaSVG.tsx returns at least 2 (import + call).
    - grep -c 'viewBox="0 0 1000 1000"' src/components/hero/MandalaSVG.tsx returns 1.
    - grep -c 'role="img"' src/components/hero/MandalaSVG.tsx returns 1.
    - grep -c 'aria-label' src/components/hero/MandalaSVG.tsx returns at least 1.
    - grep -c "'use client'" src/components/hero/MandalaSVG.tsx returns 0 (presentational, NOT a client directive).
    - grep -c 'stroke="var(--color-text)"' src/components/hero/MandalaSVG.tsx returns 1.
    - grep -c 'strokeOpacity={0.35}' src/components/hero/MandalaSVG.tsx returns 1.
    - grep -c 'strokeLinecap="round"' src/components/hero/MandalaSVG.tsx returns 1.
    - grep -c 'fill="none"' src/components/hero/MandalaSVG.tsx returns at least 1.
    - File length under 60 lines: wc -l src/components/hero/MandalaSVG.tsx returns < 60.
  </acceptance_criteria>
  <done>Pure SVG renderer of n*k chord lines with the locked stroke contract; no state, no client directive.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: HeroMandalaControls presentational sub-component</name>
  <files>src/components/hero/HeroMandalaControls.tsx</files>
  <read_first>
    - src/components/cv/SkillsList.tsx (small list+control cluster shape — to be deleted in Plan 05-05; if missing at runtime, use SkillGroupList.tsx as analog)
    - src/components/hero/Hero.tsx lines 25-29 (CTA focus-ring pattern)
    - .planning/phases/05-polish/05-PATTERNS.md (section "src/components/hero/HeroMandalaControls.tsx (NEW)")
    - .planning/phases/05-polish/05-UI-SPEC.md (Number input contract + Refresh button contract — every value)
  </read_first>
  <behavior>
    - Presentational; NO 'use client' (parent HeroMandala is the client boundary).
    - Props: { n: number; k: number; onChangeN: (next: number) => void; onChangeK: (next: number) => void; onRefresh: () => void }.
    - Renders three children stacked vertically with gap-3:
      1. A row containing a label "n" + an input type="number" (min=3, max=500, step=1, value={n}, onChange writes parsed integer to onChangeN, onBlur clamps the value to [3, 500] and calls onChangeN with the clamped value), and similarly a label "k" + input (min=1, max=n-1, step=1, value={k}, clamp on blur to [1, n-1]); plus a refresh button (button type="button") next to k input.
      2. A caption span with content "n = {n}, k = {k}" at --text-ui Sora 400 --color-muted, font-variant-numeric tabular-nums, id="mandala-caption" so MandalaSVG's aria-describedby can target it.
    - Number input className per UI-SPEC: "w-[6ch] px-2 py-1.5 rounded-[var(--radius-card)] bg-[var(--color-surface)] border border-[var(--color-muted)]/30 text-[length:var(--text-ui)] text-[var(--color-text)] tabular-nums hover:border-[var(--color-muted)]/50 focus:border-[var(--color-accent)] focus:outline-2 focus:outline-[var(--color-accent)] focus:outline-offset-2".
    - Each input has inputMode="numeric" so mobile shows the numeric keypad.
    - Each input has aria-label per UI-SPEC: "Number of points on circle" for n, "Multiplier" for k. Visible labels are single chars "n" / "k".
    - Refresh button per UI-SPEC §Refresh button contract: type="button", aria-label="Refresh mandala — pick a new random pattern", className="w-11 h-11 rounded-full flex items-center justify-center border-0 bg-transparent text-[var(--color-muted)] hover:text-[var(--color-accent)] hover:bg-[color-mix(in_oklch,var(--color-accent)_8%,transparent)] focus:outline-2 focus:outline-[var(--color-accent)] focus:outline-offset-2 active:scale-[0.96] transition-[color,background-color,transform] duration-150 ease-out". Tailwind v4 supports color-mix() inside arbitrary-value brackets (use underscores for spaces in arbitrary values).
    - Refresh button's child is the inline SVG icon with paths from UI-SPEC verbatim: M3 12a9 9 0 0 1 15.5-6.3L21 8 / M21 3v5h-5 / M21 12a9 9 0 0 1-15.5 6.3L3 16 / M3 21v-5h5; viewBox 0 0 24 24, fill none, stroke currentColor, strokeWidth 1.5, strokeLinecap round, strokeLinejoin round, aria-hidden true, width=20 height=20.
  </behavior>
  <action>
    Create src/components/hero/HeroMandalaControls.tsx. Define interface HeroMandalaControlsProps with the five props from behavior. Default-export a named function HeroMandalaControls that destructures props and returns a div with className="flex flex-col gap-3 mt-4" containing the inputs row and caption span. Inputs row className="flex items-center gap-3 flex-wrap". Each label is a label element with className="flex items-center gap-2 text-[length:var(--text-ui)] text-[var(--color-muted)]" containing the single-char visible text and the input. Convert the user's typed value through Number(e.target.value) on change; on blur, call Math.min(Math.max(parsed, MIN), MAX) and pass to onChange. For the k input's max, use n - 1. For the caption span, use font-variant-numeric tabular-nums via the Tailwind utility tabular-nums. The refresh button click handler calls onRefresh(). All five inline SVG path d values for the refresh icon must appear verbatim in the file.
  </action>
  <verify>
    <automated>npx tsc --noEmit; grep -c "type=\"number\"" src/components/hero/HeroMandalaControls.tsx; grep -c "Refresh mandala" src/components/hero/HeroMandalaControls.tsx; grep -c "id=\"mandala-caption\"" src/components/hero/HeroMandalaControls.tsx; grep -c "M3 12a9" src/components/hero/HeroMandalaControls.tsx; grep -c "'use client'" src/components/hero/HeroMandalaControls.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/hero/HeroMandalaControls.tsx exists.
    - npx tsc --noEmit exits 0.
    - grep -c 'type="number"' src/components/hero/HeroMandalaControls.tsx returns 2 (n input, k input).
    - grep -c 'inputMode="numeric"' src/components/hero/HeroMandalaControls.tsx returns 2.
    - grep -c 'aria-label="Number of points on circle"' src/components/hero/HeroMandalaControls.tsx returns 1.
    - grep -c 'aria-label="Multiplier"' src/components/hero/HeroMandalaControls.tsx returns 1.
    - grep -c 'aria-label="Refresh mandala' src/components/hero/HeroMandalaControls.tsx returns 1.
    - grep -c 'id="mandala-caption"' src/components/hero/HeroMandalaControls.tsx returns 1.
    - grep -c "M3 12a9" src/components/hero/HeroMandalaControls.tsx returns 1 (refresh icon path data verbatim).
    - grep -c "'use client'" src/components/hero/HeroMandalaControls.tsx returns 0 (parent owns client boundary).
    - grep -c "onBlur" src/components/hero/HeroMandalaControls.tsx returns 2 (clamp on blur for both inputs).
  </acceptance_criteria>
  <done>Controls cluster renders with locked styling; clamping happens on blur; ARIA labels are correct.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: HeroMandala Client Component (state + scroll loop + IntersectionObserver) and Hero.tsx embed</name>
  <files>src/components/hero/HeroMandala.tsx, src/components/hero/Hero.tsx</files>
  <read_first>
    - src/components/ui/FadeUp.tsx (closest analog: 'use client', useEffect, useReducedMotion early return, IntersectionObserver, returned cleanup)
    - src/components/header/Header.tsx (passive scroll listener pattern, no-deps useEffect, addEventListener cleanup)
    - src/hooks/useReducedMotion.ts (signature)
    - src/lib/mandala.ts (CURATED_PAIRS + pickRandomPair signatures from Plan 05-04)
    - src/components/hero/Hero.tsx (current state — Server Component; the embed point; max-w-4xl will widen)
    - .planning/phases/05-polish/05-PATTERNS.md (sections "src/components/hero/HeroMandala.tsx (NEW — Client Component)" and "src/components/hero/Hero.tsx (MODIFIED — embed mandala, fix URLs)")
    - .planning/phases/05-polish/05-UI-SPEC.md (Hero composition desktop/tablet/mobile + Scroll-driven rotation + Hero scroll-out reset + Performance Lock — every constraint)
  </read_first>
  <behavior>
    HeroMandala.tsx:
    - First line: 'use client'.
    - Hooks used: useState, useEffect, useRef, useReducedMotion (existing).
    - On mount: pick a random pair from CURATED_PAIRS via pickRandomPair() and store in state as { n, k }.
    - State: n: number, k: number, both initialized from the picked pair.
    - Refs: containerRef (HTMLDivElement) for the outer container (used by IntersectionObserver), svgWrapperRef (HTMLDivElement) wrapping the MandalaSVG (the element receiving transform: rotate).
    - Effect 1 (scroll-driven rotation, deps [reduced]):
      * If reduced is true: write svgWrapperRef.current.style.transform = 'rotate(0deg)' and 'will-change' = ''. Return early — no rAF, no listener.
      * Otherwise: install a passive scroll listener that writes only latestScrollY = window.scrollY (closure variable). Start a rAF loop that on each frame computes degrees = latestScrollY * 0.125 and writes svgWrapperRef.current.style.transform = `rotate(${degrees}deg)`. Return a cleanup that removes the listener and cancels the rAF.
      * NEVER read layout properties (offsetTop, getBoundingClientRect, etc.) inside the rAF — Performance Lock.
    - Effect 2 (IntersectionObserver gating + scroll-out reset, deps [reduced, currentPair]):
      * Create an IntersectionObserver(entries => { ... }, { threshold: 0 }) observing containerRef.current.
      * On entry.isIntersecting === true: set svgWrapperRef.current.style.willChange = 'transform' (only while in viewport).
      * On entry.isIntersecting === false: set svgWrapperRef.current.style.willChange = '' (clear the compositor promotion). Then call pickRandomPair(currentPair) and set state to the new pair (D-06 scroll-out reset).
      * Return cleanup that disconnects the observer.
    - Refresh button handler (passed to HeroMandalaControls.onRefresh): calls pickRandomPair({ n, k }) and setState to the result.
    - Input handlers: onChangeN sets state.n (and may need to clamp k to [1, n-1] if user reduces n below current k); onChangeK sets state.k.
    - Render: a div ref={containerRef} with className="w-full" containing a div ref={svgWrapperRef} with className="aspect-square mx-auto" style={{ transformOrigin: '50% 50%', maxWidth: 'min(50vw, 600px)' }} (mobile is handled in Hero.tsx via container width — the inner svg-wrapper still respects aspect-square). Inside svgWrapperRef render <MandalaSVG n={n} k={k} />. After the SVG wrapper, render <HeroMandalaControls n={n} k={k} onChangeN={...} onChangeK={...} onRefresh={...} />.

    Hero.tsx (modify):
    - Stays a Server Component (no 'use client').
    - Imports HeroMandala default export.
    - The outer section className changes max-w-4xl to max-w-6xl (UI-SPEC says planner's call to widen for two-col; 6xl = 1152px is comfortable for the 50/50 split).
    - Wrap the existing left-column block (h1 / title p / bio p / CTA div) in a new flex/grid container: className="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center". Mobile (default) stacks: left content first, mandala below. Tablet (640-1023px) per UI-SPEC: 60/40 split — implement via sm:grid-cols-[60%_40%] or skip the tablet override (leaving mobile single-col + desktop two-col is acceptable; UI-SPEC accepts 60/40 OR mobile-stacked). Recommend implement: className="grid grid-cols-1 sm:grid-cols-[60%_40%] lg:grid-cols-2 lg:gap-12 lg:items-center gap-8".
    - Left column: a div containing the existing h1, title p, bio p, CTAs div (preserved verbatim; URL fixes from Plan 05-01 already in place).
    - Right column: a div containing <HeroMandala />.
    - On mobile (single col), the mandala stacks below the CTAs because grid-row-1 vs row-2 in source order means HeroMandala lives second in the JSX — confirms.
    - Hero h1 text: planner reconciles with layout.tsx. Plan 05-01 set layout.tsx metadata to "Axel Waserman". Update the h1 from "Axel W" to "Axel Waserman" so the visible h1 matches metadata. Keep all other left-column content (title via {title} import, bio via {bio} import, three CTA links) untouched.
  </behavior>
  <action>
    Create src/components/hero/HeroMandala.tsx with first line "'use client'". Imports: useState, useEffect, useRef from react; useReducedMotion from '@/hooks/useReducedMotion'; CURATED_PAIRS, pickRandomPair, type MandalaPair from '@/lib/mandala'; default imports for MandalaSVG and HeroMandalaControls (siblings). Define interface (no props — self-contained). Default-export named function HeroMandala. Inside: useReducedMotion(), state for currentPair as MandalaPair (initialize via lazy initializer that calls pickRandomPair() so each mount produces a new pair), derive n + k from currentPair. Two refs: containerRef (HTMLDivElement, observed by IntersectionObserver), svgWrapperRef (HTMLDivElement, receives transform). Effect 1 deps [reduced]: implement the rAF + passive scroll loop per behavior; cancel and cleanup on unmount or when reduced flips. Effect 2 deps [currentPair]: implement IntersectionObserver per behavior; the threshold-0 observer toggles willChange and triggers scroll-out reset. Render the container/svg-wrapper/controls per behavior. Pass onChangeN as a function that takes a candidate next n, clamps it to [3, 500], and if the resulting n - 1 is less than current k, also clamp k to n - 1; setState accordingly. Pass onChangeK as a function that clamps to [1, currentN - 1] and setState. Pass onRefresh as a closure that calls pickRandomPair(currentPair) and setState.

    Modify src/components/hero/Hero.tsx: add "import HeroMandala from './HeroMandala'" alongside the existing data imports. Change the outer section className: replace "max-w-4xl" with "max-w-6xl". Replace the existing flat content (h1, title p, bio p, CTAs div) with a grid wrapper as described in behavior. Move the existing children INTO the left-column div verbatim (preserving Phase 2 D-01 left-aligned content + Plan 05-01 URL fixes). Add a right-column div that renders <HeroMandala />. Update the h1 visible text from "Axel W" to "Axel Waserman" (matches Plan 05-01 metadata reconciliation).
  </action>
  <verify>
    <automated>npx tsc --noEmit; npm run build; grep -c "'use client'" src/components/hero/HeroMandala.tsx; grep -c "useReducedMotion" src/components/hero/HeroMandala.tsx; grep -c "pickRandomPair" src/components/hero/HeroMandala.tsx; grep -c "IntersectionObserver" src/components/hero/HeroMandala.tsx; grep -c "requestAnimationFrame" src/components/hero/HeroMandala.tsx; grep -c "passive: true" src/components/hero/HeroMandala.tsx; grep -c "transform: rotate" src/components/hero/HeroMandala.tsx; grep -c "willChange" src/components/hero/HeroMandala.tsx; grep -c "<HeroMandala" src/components/hero/Hero.tsx; grep -c "Axel Waserman" src/components/hero/Hero.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist: HeroMandala.tsx, MandalaSVG.tsx, HeroMandalaControls.tsx, modified Hero.tsx.
    - npx tsc --noEmit exits 0.
    - npm run build exits 0.
    - grep -c "'use client'" src/components/hero/HeroMandala.tsx returns 1 (line 1).
    - grep -c "useReducedMotion" src/components/hero/HeroMandala.tsx returns at least 2 (import + invocation).
    - grep -c "pickRandomPair" src/components/hero/HeroMandala.tsx returns at least 3 (import + initial + refresh + scroll-out reset).
    - grep -c "IntersectionObserver" src/components/hero/HeroMandala.tsx returns at least 1.
    - grep -c "requestAnimationFrame" src/components/hero/HeroMandala.tsx returns at least 1.
    - grep -c "passive: true" src/components/hero/HeroMandala.tsx returns at least 1.
    - grep -c "rotate(" src/components/hero/HeroMandala.tsx returns at least 1.
    - grep -c "willChange" src/components/hero/HeroMandala.tsx returns at least 2 (set + clear OR per-effect references).
    - grep -c "if (reduced)" src/components/hero/HeroMandala.tsx returns at least 1 (early return path mirrors FadeUp pattern).
    - grep -c "removeEventListener" src/components/hero/HeroMandala.tsx returns at least 1 (cleanup function).
    - grep -c "cancelAnimationFrame" src/components/hero/HeroMandala.tsx returns at least 1 (cleanup function).
    - grep -c "<HeroMandala" src/components/hero/Hero.tsx returns 1.
    - grep -c "import HeroMandala" src/components/hero/Hero.tsx returns 1.
    - grep -c "max-w-6xl" src/components/hero/Hero.tsx returns 1.
    - grep -c "max-w-4xl" src/components/hero/Hero.tsx returns 0 (old constraint removed).
    - grep -c "lg:grid-cols-2" src/components/hero/Hero.tsx returns 1 (two-col on desktop).
    - grep -c "Axel Waserman" src/components/hero/Hero.tsx returns 1 (h1 reconciled with metadata).
    - grep -c ">Axel W<" src/components/hero/Hero.tsx returns 0 (old monogram-style h1 removed).
    - The rendered HTML in out/index.html (after npm run build) contains the literal text "Axel Waserman" and at least one `<svg` tag inside the hero section (mandala static-renders at server-render time before client hydration takes over).
  </acceptance_criteria>
  <done>HeroMandala renders, hydrates, and runs the rAF scroll loop on hover/scroll; reduced-motion users see a static SVG and can still use the controls; Hero is a two-column desktop layout with mandala on the right and stacks vertically on mobile.</done>
</task>

</tasks>

<verification>
- npm run build exits 0.
- npx tsc --noEmit exits 0.
- out/index.html (after build) contains an svg tag inside the hero section AND the text "Axel Waserman".
- A devtools-confirmed Performance Lock check during execute-phase: scrolling the hero region produces transform changes only (no layout / paint storms); will-change is "transform" while hero is in view and clears when hero leaves; rAF is gated on visibility.
- Reduced-motion check (DevTools "Emulate prefers-reduced-motion: reduce"): mandala renders static at 0deg, scroll has no effect, inputs and refresh button still respond.
</verification>

<success_criteria>
- D-01 implemented: hero is two-column with mandala in the right column on desktop; mobile stacks the mandala below the CTAs.
- D-04 implemented (consumer side): random pair from CURATED_PAIRS shows on every page load.
- D-05 implemented: number inputs + refresh button work; clamp-on-blur per UI-SPEC.
- D-06 implemented: scroll-out triggers a fresh random pair (different from current).
- D-07 implemented: rotation = scrollY * 0.125 deg via rAF + passive scroll listener writing only latestScrollY.
- D-08 implemented: useReducedMotion early-returns the rotation effect; static SVG.
- D-09 implemented: HeroMandala is the only Client Component in the hero subtree; MandalaSVG and HeroMandalaControls remain pure presentational; Hero stays Server.
- Performance Lock satisfied: compositor-only transform animation, will-change gated by visibility, no layout reads in rAF.
</success_criteria>

<output>
Create `.planning/phases/05-polish/05-06-SUMMARY.md` per the summary template when done.
</output>
