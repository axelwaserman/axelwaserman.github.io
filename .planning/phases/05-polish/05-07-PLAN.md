---
phase: 05-polish
plan: 07
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/components/projects/Projects.tsx
  - src/components/contact/Contact.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Projects section is centered: max-w-5xl mx-auto, eyebrow + heading text-center; card grid stays grid-cols-1 sm:grid-cols-2 gap-6 with cards internally left-aligned (D-02)."
    - "Contact section is centered: max-w-2xl mx-auto, heading + body text-center, link cluster justify-center (D-02)."
    - "Contact section ALWAYS renders the email row using `contact.email` (D-17 RESOLVED 2026-06-04 — no more typed-conditional, no sentinel handling)."
    - "Contact section renders the email as VISIBLE PLAIN TEXT in the rendered HTML (not only as a `mailto:` href attribute) so ATS / SEO crawlers can pick it up. The email anchor's text content is the literal email string (e.g. `axel.waserman@gmail.com`) rather than the word `Email`."
    - "All link styling preserved verbatim — no regression on the Phase 2 underline + hover + focus pattern."
  artifacts:
    - path: "src/components/projects/Projects.tsx"
      provides: "Centered Projects container + centered eyebrow/heading"
      contains: "max-w-5xl mx-auto"
    - path: "src/components/contact/Contact.tsx"
      provides: "Centered Contact container; email row always renders, label is the literal email string for SEO/ATS visibility"
      contains: "max-w-2xl mx-auto"
  key_links:
    - from: "src/components/contact/Contact.tsx"
      to: "src/data/cv.ts"
      via: "imports contact (LinkedIn / GitHub / email all data-driven)"
      pattern: "from '@/data/cv'"
---

<objective>
Re-center the Projects and Contact sections per D-02, and convert the Contact section to consume the typed `contact` export from cv.ts (Plan 05-02). Email is no longer TBD (D-17 resolved 2026-06-04 — `contact.email` is the literal string `axel.waserman@gmail.com`), so the email row ALWAYS renders. The email anchor's visible text content is the email address itself (rather than the word "Email") so the address surfaces in the rendered HTML as plain text — discoverable by ATS / SEO crawlers.

Purpose: D-02 (re-center) and D-17 (email visible in rendered HTML for crawler discoverability). Both sections inherit the parchment palette, FadeUp wrapper, and link styling from earlier phases — no token changes here.

**Revision note (2026-06-04):** The original plan implemented a typed-conditional that omitted the email row while `contact.email` was null. With D-17 resolved, the conditional is unnecessary scope. The email row always renders. `EMAIL_TBD_SENTINEL` no longer exists in cv.ts (Plan 05-02 removed it), so no sentinel reference appears here. The plan's `depends_on` list now correctly includes `05-02` (this plan imports `contact` from cv.ts, which Plan 05-02 creates).

Output: Updated Projects + Contact section components.
</objective>

<execution_context>
@/Users/axel/code/website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-polish/05-CONTEXT.md
@.planning/phases/05-polish/05-PATTERNS.md
@.planning/phases/05-polish/05-UI-SPEC.md
@.planning/phases/05-polish/05-02-SUMMARY.md
@src/components/projects/Projects.tsx
@src/components/contact/Contact.tsx
@src/data/cv.ts

<interfaces>
After Plans 05-01 and 05-02 land, src/data/cv.ts exports include:
  - export const contact: Contact   // { github: string; linkedin: string; email: string }   email is NON-NULL
  - contact.email = 'axel.waserman@gmail.com' (literal)

Plan 05-01 already updated Contact.tsx's hard-coded `mailto:axel@example.com` to `mailto:axel.waserman@gmail.com` and updated the LinkedIn + GitHub URLs to axelwaserman canonical values. This plan re-centers the section AND converts the hard-coded URLs to data-driven imports from `contact`. The email URL is still data-driven (sourced from `contact.email`) so future email changes are a one-line edit in cv.ts.

UI-SPEC layout values (locked):
  - Projects: max-w-5xl mx-auto; eyebrow text-center; heading text-center; cards internally unchanged.
  - Contact: max-w-2xl mx-auto; heading text-center; body text-center mx-auto preserves max-w-[55ch]; link cluster flex flex-wrap gap-4 justify-center.

Email row rendering rules (Revision 2026-06-04, derived from CONTEXT.md D-17 resolution):
  - The email anchor MUST be the FIRST link in the cluster (UI-SPEC §Copywriting Contract reading order: Email -> LinkedIn -> GitHub -> Download CV).
  - The anchor's `href` is `mailto:${contact.email}`.
  - The anchor's TEXT CONTENT is the literal email string `{contact.email}` (the email address itself, not the word "Email"). This makes the address visible plain text in the rendered HTML — a strict requirement for ATS / SEO crawler discoverability per CONTEXT.md D-17 update note: "Email lives in `assets/CV.typ` AND surfaces in the rendered HTML so it is discoverable by recruiter scrapers and search engines."
  - The Hero CTA cluster (Plan 05-01) keeps `Email` as the visible label since the rendered visible-text email in Contact already covers the SEO/ATS concern. Only Contact uses the email string as visible text.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-center Projects.tsx</name>
  <files>src/components/projects/Projects.tsx</files>
  <read_first>
    - src/components/projects/Projects.tsx (current state — section + eyebrow + heading + grid)
    - .planning/phases/05-polish/05-PATTERNS.md (section "src/components/projects/Projects.tsx (MODIFIED — re-center)" and Pattern 2 "Section container shape")
    - .planning/phases/05-polish/05-UI-SPEC.md (Re-Centered Sections — Projects section)
  </read_first>
  <behavior>
    - Outer section preserves id="projects", aria-labelledby="projects-heading", py-[var(--space-section)], px-6, scroll-mt-16 (Pattern 2).
    - Section's max-w changes from max-w-4xl to max-w-5xl mx-auto.
    - Eyebrow div className gets text-center added.
    - Heading h2 className gets text-center added.
    - Card grid (grid grid-cols-1 sm:grid-cols-2 gap-6) is preserved verbatim — cards stay internally left-aligned per UI-SPEC.
    - Empty state and ProjectsEmptyState consumption preserved verbatim.
    - No new imports.
  </behavior>
  <action>
    Modify src/components/projects/Projects.tsx. On the section element, replace "max-w-4xl" with "max-w-5xl mx-auto" (preserve the other classes verbatim). On the eyebrow div, add "text-center" to the existing className. On the h2 element, add "text-center" to the existing className. Do NOT touch the ul card grid — cards remain internally left-aligned. Do NOT touch the ProjectsEmptyState consumption.
  </action>
  <verify>
    <automated>npx tsc --noEmit; grep -c "max-w-5xl mx-auto" src/components/projects/Projects.tsx; grep -c "max-w-4xl" src/components/projects/Projects.tsx; grep -c "text-center" src/components/projects/Projects.tsx; grep -c "grid-cols-1 sm:grid-cols-2" src/components/projects/Projects.tsx</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0.
    - grep -c "max-w-5xl mx-auto" src/components/projects/Projects.tsx returns 1.
    - grep -c "max-w-4xl" src/components/projects/Projects.tsx returns 0.
    - grep -c "text-center" src/components/projects/Projects.tsx returns at least 2 (eyebrow + heading).
    - grep -c "grid grid-cols-1 sm:grid-cols-2 gap-6" src/components/projects/Projects.tsx returns 1 (card grid preserved verbatim).
    - grep -c "py-\[var(--space-section)\]" src/components/projects/Projects.tsx returns 1 (section padding preserved).
    - grep -c "scroll-mt-16" src/components/projects/Projects.tsx returns 1.
  </acceptance_criteria>
  <done>Projects section renders with centered container, centered eyebrow + heading, and an unchanged card grid.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-center Contact.tsx + drive email/LinkedIn/GitHub via the typed `contact` import; render the email as visible plain text</name>
  <files>src/components/contact/Contact.tsx</files>
  <read_first>
    - src/components/contact/Contact.tsx (post-Plan-05-01 state — `mailto:axel.waserman@gmail.com` already in place; LinkedIn + GitHub URLs already axelwaserman; structure still flex flex-wrap gap-4 not yet centered)
    - src/data/cv.ts (post-Plan-05-02 — `contact` export with non-null `email` string)
    - .planning/phases/05-polish/05-PATTERNS.md (section "src/components/contact/Contact.tsx (MODIFIED — re-center + URL fixes + email TBD)" — note: TBD section is now obsolete; centering + data-driven URLs still apply)
    - .planning/phases/05-polish/05-UI-SPEC.md (Re-Centered Sections — Contact section)
    - .planning/phases/05-polish/05-CONTEXT.md (D-17 RESOLVED 2026-06-04 — email visible in rendered HTML for ATS/SEO)
  </read_first>
  <behavior>
    - Outer section preserves id="contact", aria-labelledby="contact-heading", py-[var(--space-section)], px-6, scroll-mt-16.
    - Section's max-w changes from max-w-4xl to max-w-2xl mx-auto.
    - Heading h2 gets text-center.
    - Body p gets text-center mx-auto; max-w-[55ch] preserved.
    - Link cluster div className changes from "flex flex-wrap gap-4" to "flex flex-wrap gap-4 justify-center".
    - Component imports the typed contact record from '@/data/cv' (rather than hard-coding URLs).
    - LinkedIn anchor href becomes {contact.linkedin}; GitHub anchor href becomes {contact.github}.
    - Email anchor: ALWAYS rendered (D-17 resolved). Anchor href is `mailto:${contact.email}`. Anchor text content is `{contact.email}` (the literal email string itself — visible plain text in rendered HTML). The email anchor is the FIRST link in the cluster (Email -> LinkedIn -> GitHub -> Download CV).
    - Download CV anchor href="/cv.pdf" download stays at the end of the cluster, styling preserved.
    - All link className tokens (underline underline-offset-2 decoration-[var(--color-muted)] hover:decoration-[var(--color-accent)] hover:text-[var(--color-accent)] focus:outline-2 focus:outline-[var(--color-accent)] focus:outline-offset-2 py-2 + text-[length:var(--text-ui)] text-[var(--color-text)]) preserved verbatim across all surviving anchors.
    - No `EMAIL_TBD_SENTINEL` reference (the constant no longer exists in cv.ts).
    - No typed-conditional rendering branch — every render path emits the email row.
  </behavior>
  <action>
    Modify src/components/contact/Contact.tsx. Add `import { contact } from '@/data/cv'`. Replace the section className: change "max-w-4xl" to "max-w-2xl mx-auto", preserve the other tokens verbatim. On the h2 element, add "text-center" to the existing className. On the body p element, add "text-center mx-auto" to the existing className (keep max-w-[55ch]). On the link cluster div, change "flex flex-wrap gap-4" to "flex flex-wrap gap-4 justify-center".

    Replace the LinkedIn anchor's hard-coded href with `{contact.linkedin}`; replace the GitHub anchor's hard-coded href with `{contact.github}`. Preserve the LinkedIn / GitHub visible text content (`LinkedIn`, `GitHub`).

    Update the Email anchor at the START of the link cluster:
    - href becomes `mailto:${contact.email}` (template literal — was hard-coded `mailto:axel.waserman@gmail.com` after Plan 05-01).
    - Visible text content becomes `{contact.email}` (the literal email string) instead of the word `Email`. This is the SEO/ATS visibility requirement from D-17.
    - The anchor stays the FIRST link in the cluster (Email -> LinkedIn -> GitHub -> Download CV).

    Preserve the Download CV anchor at the end verbatim.

    Do NOT add any `if (contact.email)` / `&&` short-circuit — the email row is unconditional. Do NOT reference `EMAIL_TBD_SENTINEL` (it no longer exists).
  </action>
  <verify>
    <automated>npx tsc --noEmit; npm run build; grep -c "max-w-2xl mx-auto" src/components/contact/Contact.tsx; grep -c "max-w-4xl" src/components/contact/Contact.tsx; grep -c "text-center" src/components/contact/Contact.tsx; grep -c "justify-center" src/components/contact/Contact.tsx; grep -c "EMAIL_TBD_SENTINEL" src/components/contact/Contact.tsx; grep -c "contact.linkedin" src/components/contact/Contact.tsx; grep -c "contact.github" src/components/contact/Contact.tsx; grep -c "contact.email" src/components/contact/Contact.tsx; grep -c "mailto:axel@example.com" src/components/contact/Contact.tsx</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0.
    - npm run build exits 0.
    - grep -c "max-w-2xl mx-auto" src/components/contact/Contact.tsx returns 1.
    - grep -c "max-w-4xl" src/components/contact/Contact.tsx returns 0.
    - grep -c "text-center" src/components/contact/Contact.tsx returns at least 2 (heading + body).
    - grep -c "justify-center" src/components/contact/Contact.tsx returns 1 (link cluster).
    - grep -c "from '@/data/cv'" src/components/contact/Contact.tsx returns 1.
    - grep -c "EMAIL_TBD_SENTINEL" src/components/contact/Contact.tsx returns 0 (sentinel handling removed).
    - grep -c "contact\.linkedin" src/components/contact/Contact.tsx returns 1.
    - grep -c "contact\.github" src/components/contact/Contact.tsx returns 1.
    - grep -c "contact\.email" src/components/contact/Contact.tsx returns at least 2 (mailto href + visible text content).
    - grep -c "mailto:axel@example.com" src/components/contact/Contact.tsx returns 0 (placeholder gone).
    - grep -c "axelw[^a]" src/components/contact/Contact.tsx returns 0 (URLs now data-driven).
    - The rendered HTML in `out/index.html` (after `npm run build`) contains a `mailto:axel.waserman@gmail.com` anchor inside the contact section.
    - `cat out/index.html | grep -c "axel.waserman@gmail.com"` returns at least 2 (one for the mailto: href attribute, one for the visible text content of the anchor).
    - The rendered HTML contains "https://github.com/axelwaserman" and "https://www.linkedin.com/in/axel-waserman-9753221a6/" inside the contact section.
  </acceptance_criteria>
  <done>Contact renders centered, with email row always present; email URL + visible text both sourced from `contact.email`; LinkedIn + GitHub hrefs sourced from typed contact record; rendered HTML surfaces the email as both attribute and plain text for ATS/SEO crawlers.</done>
</task>

</tasks>

<verification>
- npm run build exits 0.
- npx tsc --noEmit exits 0.
- The Contact section in `out/index.html` shows four links (Email, LinkedIn, GitHub, Download CV) with the Email anchor's text content being the literal email string.
- `cat out/index.html | grep -c "axel.waserman@gmail.com"` returns at least 2 (mailto href + visible text in the Contact section, possibly more if Hero also surfaces the email).
- Switching `contact.email` in cv.ts to a different address and re-running `npm run build` updates BOTH the mailto href and the visible text in one line of source — proves the rendering is data-driven.
</verification>

<success_criteria>
- D-02 implemented for Projects + Contact (CV is covered by Plan 05-05).
- D-17 implemented at the render layer: email row is unconditional; email anchor's text content is the literal email string so ATS/SEO crawlers can pick up the address; mailto href works; future email changes require only a cv.ts edit.
- depends_on now correctly includes 05-02 (this plan imports `contact` from cv.ts).
- No regressions on link styling, FadeUp wrapping, semantic structure, or sticky-header anchor scrolling.
</success_criteria>

<output>
Create `.planning/phases/05-polish/05-07-SUMMARY.md` per the summary template when done.
</output>
