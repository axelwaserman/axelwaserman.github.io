---
phase: 05-polish
plan: 08
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
  - 05-03
  - 05-05
  - 05-06
  - 05-07
files_modified:
  - src/data/projects.json
autonomous: false
requirements: []
must_haves:
  truths:
    - "src/data/projects.json contains a snapshot of axelwaserman's live public non-archived non-fork repos (D-24) — every entry's repoUrl starts with https://github.com/axelwaserman/."
    - "Live deployed HTML (https://axelwaserman.github.io/) renders repos that match `gh repo list axelwaserman --visibility public --no-archived` (Phase 5 SC-4 + D-22)."
    - "Live deployed HTML does NOT contain the stale fallback repo names (axelw.github.io, node-sublime-build, gun, project-template, project-template-support)."
    - "Live deployed HTML head includes a <link rel=\"icon\"> tag and the icon asset returns 200 (Phase 5 SC-1)."
    - "Live deployed page text contains Axel's real CV: 'Senior Engineering Manager | Backend & Data', 'Contentsquare', 'PwC France', 'Panodyssey', 'ESILV', 'Engineering Leadership', 'Backend & Systems Architecture', 'Hands-on Tech Stack' (Phase 5 SC-3)."
    - "Live deployed page hero contains the mandala SVG (Phase 5 SC-2)."
    - "Live deployed HTML surfaces the email `axel.waserman@gmail.com` at least twice — once as the visible text of the Contact email anchor, and once as the mailto: href attribute. ATS / SEO crawlers can therefore discover the address (D-17 visibility requirement)."
  artifacts:
    - path: "src/data/projects.json"
      provides: "Refreshed fallback snapshot of axelwaserman repos (D-24)"
      contains: "axelwaserman"
  key_links:
    - from: "deployed HTML"
      to: "github.com/axelwaserman public repos"
      via: "build-time fetchProjects fed by GITHUB_USERNAME=axelwaserman"
      pattern: "https://github.com/axelwaserman/"
---

<objective>
Close Phase 5 by capturing the live GitHub fetch as the new committed fallback (D-24) and verifying every Phase 5 Success Criterion is satisfied on the deployed site.

Purpose: This plan is the phase exit gate. It refreshes `src/data/projects.json` from the live GitHub REST API, then validates the live URL against all four Phase 5 success criteria plus the D-17 ATS/SEO visibility check.

**Revision note (2026-06-04):** D-17 was resolved during the planner revision pass — `contact.email` is the literal string `axel.waserman@gmail.com` set in Plan 05-02. This plan no longer includes the email TBD closure checkpoint or the cv.ts email-update task. The remaining work is the projects.json refresh + live verification. Approach B (direct GitHub REST API call via `gh api`) is now the only refresh approach — Approach A (re-running fetchProjects via tsx) was blocked by the `import 'server-only'` barrier in `src/lib/projects.ts` and is no longer viable as written.

Output: Refreshed projects.json and a verified deployment that satisfies SC-1 through SC-4 plus the D-17 visibility check.
</objective>

<execution_context>
@/Users/axel/code/website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-polish/05-CONTEXT.md
@.planning/phases/05-polish/05-PATTERNS.md
@.planning/phases/05-polish/05-UI-SPEC.md
@.planning/phases/05-polish/05-01-SUMMARY.md
@.planning/phases/05-polish/05-02-SUMMARY.md
@src/lib/projects.ts
@src/data/projects.json
@src/data/projects.ts
@src/data/cv.ts

<interfaces>
src/lib/projects.ts (Plan 05-01) defaults GITHUB_USERNAME to axelwaserman. fetchProjects() returns a Project[] of non-archived non-fork non-disabled public repos sorted by pushed_at desc. Project shape from src/data/projects.ts:
  { name: string; description: string | null; language: string | null; pushedAt: string | null; repoUrl: string; homepage: string | null }

projects.json must match this shape exactly (it is consumed via projectsFallback as Project[] in the catch branch of fetchProjects). The `import 'server-only'` directive on `src/lib/projects.ts` makes it impossible to invoke `fetchProjects` from a one-shot Node script — the import barrier rejects non-server runtimes. This plan therefore uses the GitHub REST API directly via `gh api` to produce the same shape.

src/data/cv.ts (Plan 05-02) exports:
  contact: Contact = { github: '...', linkedin: '...', email: 'axel.waserman@gmail.com' }
This plan does NOT modify cv.ts. (D-17 already resolved; no email closure task.)

Live deploy URL per Phase 4 + Plan 05-01 fixes: https://axelwaserman.github.io/

Phase 5 Success Criteria (ROADMAP.md §"Phase 5"):
  SC-1: Browser tab shows favicon (no /favicon.ico 404).
  SC-2: Page features a deliberate mandala-style decorative pattern.
  SC-3: CV section reflects real history with working email/LinkedIn/GitHub URLs. Email visible in rendered HTML for ATS/SEO crawlers (D-17 visibility requirement).
  SC-4: Projects section renders real repos via GitHub API at build time, verified against gh repo list axelwaserman --visibility public --no-archived.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refresh src/data/projects.json with the live axelwaserman fetch via gh REST API (D-24)</name>
  <files>src/data/projects.json</files>
  <read_first>
    - src/lib/projects.ts (the fetcher whose output we mirror — note the `import 'server-only'` barrier on line 1; this is why we use the gh API path)
    - src/data/projects.ts (Project interface)
    - src/data/projects.json (current placeholder content — five axelw repos that must be replaced)
    - .planning/phases/05-polish/05-CONTEXT.md (D-24)
    - .planning/phases/05-polish/05-PATTERNS.md (section "src/data/projects.json (MODIFIED — refresh fallback)")
  </read_first>
  <action>
    Use the GitHub REST API directly via `gh api` to fetch the live axelwaserman public, non-archived, non-fork repo list. Approach B from the original plan; Approach A (calling `fetchProjects` programmatically) is INFEASIBLE because `src/lib/projects.ts` has `import 'server-only'`, which throws when imported from any non-server runtime including a one-shot Node script.

    Step 1 — Fetch raw repo data:

      gh api -H "Accept: application/vnd.github+json" \
        "/users/axelwaserman/repos?per_page=100&type=owner&sort=pushed&direction=desc" \
        > /tmp/axelwaserman-repos.json

    Confirm the file is a JSON array (`jq 'type' /tmp/axelwaserman-repos.json` returns `"array"`) and has at least 1 entry.

    Step 2 — Filter and project the data into the Project[] shape that `src/data/projects.json` requires. Use `jq` (executor's choice — equivalent to the in-source filter+projection in `src/lib/projects.ts`):

      jq '[
        .[]
        | select(.archived == false)
        | select(.fork == false)
        | select(.disabled == false)
        | {
            name: .name,
            description: .description,
            language: .language,
            pushedAt: .pushed_at,
            repoUrl: .html_url,
            homepage: (.homepage | if . == "" then null else . end)
          }
      ]' /tmp/axelwaserman-repos.json > src/data/projects.json

    The resulting file MUST conform to Project[] from src/data/projects.ts: every entry has name (string), description (string | null), language (string | null), pushedAt (ISO8601 string | null), repoUrl (string), homepage (string | null). repoUrl values must all start with `https://github.com/axelwaserman/`. None of the entries' name should be `axelw.github.io`, `node-sublime-build`, `gun`, `project-template`, or `project-template-support` (those were the original axelw user's repos and aren't owned by axelwaserman).

    Step 3 — Format for diff-friendliness: `jq '.' src/data/projects.json > /tmp/formatted.json && mv /tmp/formatted.json src/data/projects.json` (re-emit with two-space indentation matching the existing file's style; preserve trailing newline).

    Step 4 — Validate by re-running `npm run build` and confirming the build succeeds and the `out/` HTML renders the new repo names.

    Edge cases: if `gh api` returns a 401 (auth failure), the executor should run `gh auth status` and re-authenticate as needed. If the response is paginated (>100 repos — unlikely for an individual user), use `gh api --paginate` instead. If `axelwaserman` has zero non-archived non-fork repos at refresh time, surface this immediately — Phase 5 SC-4 cannot be satisfied with an empty list, and the user must be informed before continuing.

    Do NOT delete or modify any other file. Do NOT create scripts/refresh-fallback.ts (that script is not needed under Approach B).
  </action>
  <verify>
    <automated>npm run build; grep -c '"axelw/' src/data/projects.json; grep -c '"axelwaserman/' src/data/projects.json; grep -c '"name": "axelw\.github\.io"' src/data/projects.json; node -e "const j = require('./src/data/projects.json'); console.log('count:', j.length); j.forEach(p => { if (typeof p.name !== 'string' || typeof p.repoUrl !== 'string') process.exit(1); if (!p.repoUrl.startsWith('https://github.com/axelwaserman/')) { console.error('Bad repoUrl:', p.repoUrl); process.exit(1); } })"</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0.
    - `git diff src/data/projects.json` shows at least 5 entries with non-`axelw` repo names (proves a real refresh happened).
    - `grep -c '"axelw/' src/data/projects.json` returns 0 (no stale axelw URLs).
    - `grep -c 'https://github.com/axelwaserman/' src/data/projects.json` returns at least 1.
    - `grep -c '"name": "axelw\.github\.io"' src/data/projects.json` returns 0 (old fallback name removed).
    - `grep -c '"name": "node-sublime-build"' src/data/projects.json` returns 0.
    - `grep -c '"name": "gun"' src/data/projects.json` returns 0.
    - `grep -c '"name": "project-template"' src/data/projects.json` returns 0.
    - The Node validation script in <verify> exits 0 (every entry's repoUrl starts with `https://github.com/axelwaserman/` and the count is non-zero).
    - The output of `gh repo list axelwaserman --visibility public --no-archived --no-fork --json name --jq '.[].name'` matches the names in projects.json (any reasonable difference, e.g. timing-skew between gh CLI and gh api, is acceptable as long as the projects.json names are a subset of the gh repo list names).
  </acceptance_criteria>
  <done>projects.json mirrors the live axelwaserman public non-archived non-fork repo list; no stale axelw entries remain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Push, deploy, and verify all four Phase 5 Success Criteria + D-17 visibility on the live URL</name>
  <what-built>
    A complete Phase 5 deliverable, ready for production verification:
    - Plans 05-01..05-07 implemented and committed.
    - Plan 05-08 Task 1 refreshed projects.json from the live gh REST API.
    - cv.ts already carries the resolved email `axel.waserman@gmail.com` (Plan 05-02; no edit needed in this plan).
    - Local `npm run build` succeeds with the live GitHub fetch (no fallback warning).

    What this task does:
    1. Push the phase-04-deploy branch (or whichever branch holds the Phase 5 work) to origin.
    2. The Phase 4 deploy workflow runs and deploys to https://axelwaserman.github.io/.
    3. Once the workflow completes, run the success-criterion checks below.
  </what-built>
  <how-to-verify>
    Wait for the GitHub Actions deployment to finish (check Actions tab on github.com/axelwaserman/axelwaserman.github.io). Then run these checks from your terminal:

    SC-1 (favicon):
      `curl -sI https://axelwaserman.github.io/ | grep -i 'content-type'` -> 200 OK page.
      `curl -s https://axelwaserman.github.io/ | grep -E 'rel="icon"|rel="shortcut icon"'` -> at least one match.
      Open https://axelwaserman.github.io/ in a browser; the tab favicon visible (AW monogram on parchment); DevTools Network tab shows zero 404s for /favicon.ico (the request may not even be made — Next.js auto-injects a different path).

    SC-2 (mandala):
      `curl -s https://axelwaserman.github.io/ | grep -c "<svg"` -> at least 1 (mandala SVG present in static HTML).
      `curl -s https://axelwaserman.github.io/ | grep -E 'viewBox="0 0 1000 1000"'` -> at least 1 match.
      In a browser: scroll the hero. Mandala rotates smoothly. Set DevTools "Emulate prefers-reduced-motion: reduce" -> mandala stops rotating, controls still work.

    SC-3 (real CV + D-17 ATS/SEO email visibility):
      `curl -s https://axelwaserman.github.io/ | grep -c "Senior Engineering Manager | Backend & Data"` -> 1 or more.
      `curl -s https://axelwaserman.github.io/ | grep -c "Contentsquare"` -> 1 or more.
      `curl -s https://axelwaserman.github.io/ | grep -c "PwC France"` -> 1.
      `curl -s https://axelwaserman.github.io/ | grep -c "Engineering Leadership"` -> 1.
      `curl -s https://axelwaserman.github.io/ | grep -c "Backend & Systems Architecture"` -> 1.
      `curl -s https://axelwaserman.github.io/ | grep -c "Hands-on Tech Stack"` -> 1.
      `curl -s https://axelwaserman.github.io/ | grep -E 'href="mailto:[^"]+@[^"]+"'` -> at least 1 (real email anchor present).
      `curl -s https://axelwaserman.github.io/ | grep -c "axel.waserman@gmail.com"` -> at least 2 (D-17 visibility: one for the visible text content of the Contact email anchor, one for the mailto: href; possibly higher if the Hero email anchor also surfaces the address). This is the load-bearing ATS/SEO check.
      `curl -s https://axelwaserman.github.io/ | grep -c "https://github.com/axelwaserman"` -> 2 or more (Hero CTA + Contact).
      `curl -s https://axelwaserman.github.io/ | grep -c "https://www.linkedin.com/in/axel-waserman-9753221a6/"` -> 2 or more.
      `curl -s https://axelwaserman.github.io/cv.pdf -o /tmp/cv.pdf -w '%{http_code}\\n'` -> 200; `file /tmp/cv.pdf` says PDF document.
      `curl -s https://axelwaserman.github.io/ | grep -c "axelw[^a]"` -> 0 (no stale username leaks).

    SC-4 (live GitHub repos):
      `LIVE_REPOS=$(curl -s https://axelwaserman.github.io/ | grep -oE 'github\.com/axelwaserman/[a-zA-Z0-9._-]+' | sort -u)`
      `GH_REPOS=$(gh repo list axelwaserman --visibility public --no-archived --json name,isFork --limit 200 --jq '.[] | select(.isFork == false) | .name' | sort -u)`
      Confirm: every name in LIVE_REPOS (after stripping the `github.com/axelwaserman/` prefix) is in GH_REPOS — proves the deployed page is showing live data, not the old fallback.
      `curl -s https://axelwaserman.github.io/ | grep -c "axelw\.github\.io"` -> 0 (old placeholder repo name absent).
      `curl -s https://axelwaserman.github.io/ | grep -c "node-sublime-build\\|/gun\"\\|project-template\\|project-template-support"` -> 0 (old axelw repos absent unless coincidentally also present in axelwaserman, which they are not).

    Report back: paste the curl command outputs, paste the LIVE_REPOS / GH_REPOS comparison, OR describe any check that failed. Include a screenshot of the hero (favicon + mandala visible) for the visual checks.
  </how-to-verify>
  <resume-signal>Type "approved" if every check passed, OR describe the failing check(s) so the executor can decide whether the gap is in plans 05-01..05-07 (re-execute) or in this plan (fix here).</resume-signal>
</task>

</tasks>

<verification>
- All four Phase 5 Success Criteria pass against the live https://axelwaserman.github.io/ URL per Task 2 checks.
- `curl -s https://axelwaserman.github.io/ | grep -c "axel.waserman@gmail.com"` returns at least 2 (D-17 ATS/SEO visibility check).
- src/data/projects.json contains only axelwaserman repos.
- No stale axelw references remain anywhere in the repo (excluded from D-23 sweep targets except git history): `grep -rn "axelw[^a]" src .planning .github | grep -v '\.planning/phases/0[1-4]'` returns no source matches.
</verification>

<success_criteria>
- D-17 visibility verified: the live HTML surfaces the email at least twice (visible text + mailto href), proving ATS / SEO crawlers can pick up the address.
- D-22 verified: live HTML check confirms repos match `gh repo list axelwaserman --visibility public --no-archived` and the placeholder axelw.github.io repo name is absent.
- D-24 implemented: src/data/projects.json refreshed with the live snapshot via gh REST API (Approach B); future API-outage builds will render real (slightly stale) Axel data instead of axelw placeholders.
- Phase 5 SC-1 (favicon), SC-2 (mandala), SC-3 (real CV + visible email), SC-4 (live GitHub repos) all pass on the production URL.
</success_criteria>

<output>
Create `.planning/phases/05-polish/05-08-SUMMARY.md` per the summary template when done. Include the live-verification curl outputs / repo-list comparison as appendix evidence.

After the SUMMARY is committed, the phase is complete. Open a PR per the project's phase PR workflow (memory: `feedback_phase_pr_workflow.md` — push + open PR after subagent validates each phase; human merges manually).
</output>
