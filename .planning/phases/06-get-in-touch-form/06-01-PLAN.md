---
phase: 06-get-in-touch-form
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/data/site.ts
autonomous: true
requirements: [SC-1]
# SC-1: form posts to Formspree | SC-2: JSON-LD + plain-text email visible | SC-3: anchors removed/swapped | SC-4: validation/states/reduced-motion
must_haves:
  truths:
    - "react-hook-form, zod, and @hookform/resolvers are present in package.json dependencies"
    - "package-lock.json is in sync with package.json (npm ci would succeed)"
    - "src/data/site.ts exports a FORMSPREE_ENDPOINT named constant typed as string"
    - "next build succeeds with the new deps and the site.ts module imported"
  artifacts:
    - path: "src/data/site.ts"
      provides: "FORMSPREE_ENDPOINT named export consumed by ContactForm"
      contains: "export const FORMSPREE_ENDPOINT"
    - path: "package.json"
      provides: "react-hook-form, zod, @hookform/resolvers dependencies"
    - path: "package-lock.json"
      provides: "Lockfile fully resolved against new deps (regenerated from scratch)"
  key_links:
    - from: "src/components/contact/ContactForm.tsx (created in plan 03)"
      to: "src/data/site.ts"
      via: "import { FORMSPREE_ENDPOINT } from '@/data/site'"
      pattern: "FORMSPREE_ENDPOINT"
---

<objective>
Foundation slice for Phase 6: install form-handling dependencies (react-hook-form + zod + @hookform/resolvers per D-07), regenerate package-lock from scratch (per memory `feedback_npm_ci_lock_sync.md`), and create the centralised `src/data/site.ts` module that exports `FORMSPREE_ENDPOINT` (D-01, D-04).

Purpose: Without these deps and the endpoint constant, no other plan can build. This is the smallest possible vertical slice — after it, the project compiles with the new dependency surface and the Formspree endpoint has a single source of truth.
Output: Updated `package.json` + freshly regenerated `package-lock.json`; new `src/data/site.ts` with `FORMSPREE_ENDPOINT` constant; a documented placeholder ID and an explicit user task to paste the real Formspree form ID before phase exit.
</objective>

<execution_context>
@/Users/axel/code/website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-get-in-touch-form/06-CONTEXT.md
@CLAUDE.md
@package.json
@/Users/axel/.claude/projects/-Users-axel-code-website/memory/feedback_npm_ci_lock_sync.md

<interfaces>
<!-- Existing data module convention (named exports only, mirroring src/data/cv.ts) -->

From src/data/cv.ts (style reference for new src/data/site.ts):
- Named exports only, no default export
- Each export typed explicitly (e.g., `export const title: string = ...`)
- Plain string/object literals; no runtime computation

Target shape for src/data/site.ts (to be created):
- `export const FORMSPREE_ENDPOINT: string`
- Value format per D-01: `https://formspree.io/f/<id>` (URL-string, public — not a secret)
- Optional inline comment marking the placeholder ID for the user-action task
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add form deps and regenerate package-lock from scratch</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current dependency surface)
    - /Users/axel/.claude/projects/-Users-axel-code-website/memory/feedback_npm_ci_lock_sync.md (regenerate-from-scratch protocol)
    - .planning/phases/06-get-in-touch-form/06-CONTEXT.md (D-07 names the three deps)
  </read_first>
  <action>
    Add three runtime dependencies to package.json `dependencies` (D-07): `react-hook-form`, `zod`, `@hookform/resolvers`. Use the latest stable majors that are compatible with React 19.2.x and Next 16.2.6 (resolve at install time; do not pin to outdated majors). After editing package.json, regenerate `package-lock.json` from scratch per memory `feedback_npm_ci_lock_sync.md` to avoid the EUSAGE failure mode in CI: delete the existing `package-lock.json` (and `node_modules` if necessary), then run `npm install` to produce a fully resolved lockfile in one pass. Do NOT use `npm install <pkg>` against an out-of-sync lockfile — this is the pattern memory explicitly warns against. Verify deps land in `dependencies` (not `devDependencies`) since they ship to the client bundle.
  </action>
  <verify>
    <automated>npm ci &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '"(react-hook-form|zod|@hookform/resolvers)"' package.json` finds all three under `"dependencies"`
    - `node -e "const p=require('./package-lock.json'); for (const k of ['react-hook-form','zod','@hookform/resolvers']) { if (!p.packages['node_modules/'+k]) { console.error('missing in lockfile:', k); process.exit(1); } }"` exits 0
    - `npm ci` (clean install from lockfile) completes without EUSAGE / ELOCKVERIFY errors
    - `npm run build` exits 0 (no broken imports introduced)
  </acceptance_criteria>
  <done>All three deps present in package.json + package-lock.json; `npm ci` succeeds in a clean tree; `next build` still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/data/site.ts with FORMSPREE_ENDPOINT</name>
  <files>src/data/site.ts</files>
  <read_first>
    - src/data/cv.ts (named-export style to mirror)
    - .planning/phases/06-get-in-touch-form/06-CONTEXT.md (D-01 endpoint storage, D-04 user-action task)
  </read_first>
  <action>
    Create `src/data/site.ts` with a single named export `FORMSPREE_ENDPOINT` typed as `string` (D-01). Initial value: a placeholder URL of the form `https://formspree.io/f/PLACEHOLDER_FORM_ID`. Place a TS comment directly above the constant noting the placeholder must be replaced with the real form ID Axel provisions on Formspree (D-04 — handled in plan 06-06's user-action checkpoint). Keep the file minimal: no other exports, no default export, no logic. Style must match `src/data/cv.ts` (named-export-only convention from Phase 2 D-09 / 02-01 SUMMARY).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; node -e "const fs=require('fs'); const s=fs.readFileSync('src/data/site.ts','utf8'); if (!/export const FORMSPREE_ENDPOINT\s*:\s*string\s*=/.test(s)) { console.error('FORMSPREE_ENDPOINT missing or untyped'); process.exit(1); } if (!/PLACEHOLDER/i.test(s)) { console.error('placeholder marker missing'); process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/site.ts` exists and exports `FORMSPREE_ENDPOINT: string` (regex `export const FORMSPREE_ENDPOINT\s*:\s*string\s*=` matches)
    - File contains a comment with the substring `PLACEHOLDER` (or equivalent) flagging the value to be replaced
    - `npx tsc --noEmit` exits 0 with the new module in tree
    - File has no default export and no imports beyond what is strictly needed (mirrors `cv.ts` shape)
  </acceptance_criteria>
  <done>`src/data/site.ts` committed with typed `FORMSPREE_ENDPOINT` placeholder + comment; type-check passes.</done>
</task>

</tasks>

<verification>
- `npm ci` completes cleanly (deps + lockfile in sync — guards memory `feedback_npm_ci_lock_sync.md` failure mode)
- `npm run lint`, `npx tsc --noEmit`, `npm run build` all pass
- `src/data/site.ts` is the single source of truth for `FORMSPREE_ENDPOINT`; no other file hardcodes the URL
</verification>

<success_criteria>
- react-hook-form, zod, @hookform/resolvers added under `dependencies` in package.json
- `package-lock.json` regenerated from scratch and `npm ci` succeeds
- `src/data/site.ts` exists, exports `FORMSPREE_ENDPOINT`, contains placeholder marker for the user-action task
- Build still passes
</success_criteria>

<output>
Create `.planning/phases/06-get-in-touch-form/06-01-SUMMARY.md` when done.
</output>
</content>
</invoke>