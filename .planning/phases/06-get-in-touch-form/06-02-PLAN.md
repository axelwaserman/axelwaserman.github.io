---
phase: 06-get-in-touch-form
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
autonomous: true
requirements: [SC-2]
# SC-1: form posts to Formspree | SC-2: JSON-LD + plain-text email visible | SC-3: anchors removed/swapped | SC-4: validation/states/reduced-motion
must_haves:
  truths:
    - "A single <script type=\"application/ld+json\"> block is rendered on every page (in <head> or <body>) containing the Person schema"
    - "The JSON-LD payload contains the literal string 'axel.waserman@gmail.com' under the email key"
    - "Schema fields name, email, url, jobTitle, sameAs are all populated from cv.ts (no drift between Person schema and CV data)"
    - "Build still produces static HTML (output: 'export') and the JSON-LD appears in the prerendered out/index.html"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Person JSON-LD <script> tag rendered into the document"
      contains: "application/ld+json"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/data/cv.ts"
      via: "import { contact, title } from '@/data/cv'"
      pattern: "from ['\\\"]@/data/cv['\\\"]"
---

<objective>
Inject a Schema.org `Person` JSON-LD block into the root layout (D-22 — script lives inside `<body>`, NOT `<head>`; SC-2's "in `<head>`" wording is satisfied in spirit by site-wide presence in the prerendered HTML) so the address `axel.waserman@gmail.com` is parseable by ATS / search-engine crawlers — fulfilling half of SC-2 (the JSON-LD half; the rendered-HTML half is delivered in plan 06-03).

Purpose: Site-wide structured data that mirrors the CV without duplicating it. Values are imported from `src/data/cv.ts` so the JSON-LD never drifts from displayed content.
Output: Updated `src/app/layout.tsx` rendering `<script type="application/ld+json">` with the Person schema; identical SSR + first-client render (deterministic JSON.stringify of build-time-known data, no Math.random / Date.now).
</objective>

<execution_context>
@/Users/axel/code/website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-get-in-touch-form/06-CONTEXT.md
@CLAUDE.md
@src/app/layout.tsx
@src/data/cv.ts
@/Users/axel/.claude/projects/-Users-axel-code-website/memory/feedback_ssr_hydration_determinism.md

<interfaces>
<!-- Existing root layout exports + cv.ts named exports -->

From src/app/layout.tsx (current shape):
- `export const metadata: Metadata` — Open Graph + Twitter card (preserved unchanged)
- `export default function RootLayout({ children }: { children: React.ReactNode })`
- Renders `<html lang="en" className={...}><body className="antialiased">{children}</body></html>`
- Two next/font/google instances (Sora, Instrument Serif) with `--font-body` / `--font-heading` variables

From src/data/cv.ts (values consumed by JSON-LD per D-22):
- `export const title: string = 'Senior Engineering Manager | Backend & Data'`  (D-22: jobTitle uses the leading phrase before `|`, e.g., 'Senior Engineering Manager')
- `export const contact: Contact = { github: 'https://github.com/axelwaserman', linkedin: 'https://www.linkedin.com/in/axel-waserman-9753221a6/', email: 'axel.waserman@gmail.com' }`

Person schema target shape (D-22):
- `@context`: 'https://schema.org'
- `@type`: 'Person'
- `name`: 'Axel Waserman'
- `email`: contact.email
- `url`: 'https://axelwaserman.github.io'
- `jobTitle`: derived from cv.ts `title` — D-22 specifies 'Senior Engineering Manager' (the leading clause before ' | '); split on ' | ' and take `[0]` rather than hardcoding
- `sameAs`: [contact.github, contact.linkedin]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inject Person JSON-LD into root layout</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx (current root layout — preserve metadata + font setup)
    - src/data/cv.ts (source-of-truth values)
    - .planning/phases/06-get-in-touch-form/06-CONTEXT.md (D-22 schema definition, D-18 noindex precedent)
    - /Users/axel/.claude/projects/-Users-axel-code-website/memory/feedback_ssr_hydration_determinism.md (deterministic render contract)
  </read_first>
  <behavior>
    - Page HTML contains exactly one `<script type="application/ld+json">` element when rendered through RootLayout
    - Parsed JSON has `@type === 'Person'`, `name === 'Axel Waserman'`, `email === 'axel.waserman@gmail.com'`, `url === 'https://axelwaserman.github.io'`, `jobTitle === 'Senior Engineering Manager'`, `sameAs` array containing both the GitHub and LinkedIn URLs from `cv.ts`
    - Email value comes from `contact.email` import (not a hardcoded string literal in layout.tsx)
    - Render output is identical between SSR and client first render (no time / random values; JSON.stringify of a stable object literal)
  </behavior>
  <action>
    Edit `src/app/layout.tsx` to import `contact` and `title` from `@/data/cv`. Build a `personSchema` object literal at module scope (not inside the component body — keeps it deterministic and avoids per-render allocation) shaped per D-22. Derive `jobTitle` from `title` by splitting on the literal ` | ` separator and taking the first segment (matches D-22's stated value 'Senior Engineering Manager' while staying single-source-of-truth with cv.ts). Render a `<script>` element with `type="application/ld+json"` inside `<body>` (place it before `{children}` so it sits high in the DOM but does not affect layout) using `dangerouslySetInnerHTML={{ __html: JSON.stringify(personSchema) }}`. Do NOT use `JSON.stringify(personSchema, null, 2)` — minified output is preferred for transport. Preserve the existing `metadata` export, font setup, and `<html lang="en" className=...>` exactly. Do not introduce any non-deterministic values (Math.random, Date.now, new Date(), etc.) per memory `feedback_ssr_hydration_determinism.md`.
  </action>
  <verify>
    <!-- Simple, robust presence checks against the built HTML. Full JSON.parse correctness is asserted by the Playwright DOM eval test in plan 06-06 task 1, which uses a real DOM parser instead of regex. -->
    <automated>npm run build &amp;&amp; grep -F 'application/ld+json' out/index.html &amp;&amp; grep -F '"@type":"Person"' out/index.html &amp;&amp; grep -F 'axel.waserman@gmail.com' out/index.html &amp;&amp; grep -F 'Axel Waserman' out/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `grep -F 'application/ld+json' out/index.html` exits 0 (JSON-LD script tag present)
    - `grep -F '"@type":"Person"' out/index.html` exits 0 (Person schema type emitted by minified JSON.stringify)
    - `grep -F 'axel.waserman@gmail.com' out/index.html` exits 0 (email present in rendered HTML)
    - `grep -F 'https://github.com/axelwaserman' out/index.html` exits 0 (sameAs[0] present)
    - `grep -F 'https://www.linkedin.com/in/axel-waserman-9753221a6/' out/index.html` exits 0 (sameAs[1] present)
    - `grep -n 'contact.email' src/app/layout.tsx` shows the email is imported, not hardcoded
    - `grep -nE 'Math\.random|Date\.now|new Date\(' src/app/layout.tsx` returns no matches inside the JSON-LD construction
    - `npm run lint` and `npx tsc --noEmit` pass
    - Full JSON-LD payload correctness (all six keys, exact values) is asserted by Playwright DOM eval in plan 06-06 task 1 (`document.querySelector('script[type="application/ld+json"]')` + `JSON.parse(...)`).
  </acceptance_criteria>
  <done>Person JSON-LD rendered into every prerendered page; values sourced from cv.ts; SSR-deterministic; build green.</done>
</task>

</tasks>

<verification>
- Build the site (`npm run build`) and inspect `out/index.html` for the JSON-LD block
- Plan 06-06 Playwright DOM eval confirms parsed JSON matches the D-22 schema exactly (string-literal greps here are presence-only)
- Lint + type-check pass
</verification>

<success_criteria>
- SC-2 (JSON-LD half): `<script type="application/ld+json">` Person block present in `<body>` (D-22 locks placement to body) with axel.waserman@gmail.com
- No drift between JSON-LD and `cv.ts` (email + sameAs + jobTitle all imported / derived)
- No SSR / client mismatch risk
</success_criteria>

<output>
Create `.planning/phases/06-get-in-touch-form/06-02-SUMMARY.md` when done.
</output>
</content>
</invoke>