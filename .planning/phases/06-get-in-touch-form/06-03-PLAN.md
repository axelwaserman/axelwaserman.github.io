---
phase: 06-get-in-touch-form
plan: 03
type: execute
wave: 2
depends_on: ['06-01']
files_modified:
  - src/components/contact/contact-schema.ts
  - src/components/contact/ContactForm.tsx
  - src/components/contact/Contact.tsx
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]
# SC-1: form posts to Formspree | SC-2: JSON-LD + plain-text email visible | SC-3: anchors removed/swapped | SC-4: validation/states/reduced-motion
must_haves:
  truths:
    - "Visitor sees a four-field form (name, email, company optional, message) inside the Contact section"
    - "Submitting with invalid input shows inline errors below each invalid field after blur (D-10) and on submit attempts"
    - "Submitting valid input POSTs JSON to FORMSPREE_ENDPOINT with Accept + Content-Type application/json (D-13)"
    - "On non-2xx response, an inline error block above the submit button shows the email-fallback message (D-16, D-03)"
    - "Submit button is disabled with the label 'Sending…' while in flight (D-14)"
    - "Honeypot input named '_gotcha' is rendered but visually hidden and skipped by tab order (D-02)"
    - "Plain-text email 'axel.waserman@gmail.com' appears as non-link text inside the Contact section (D-21, SC-2 rendered-HTML half)"
    - "Contact section no longer renders the mailto / LinkedIn / GitHub / Download CV anchors (D-23 — Download CV also removed per CONTEXT discretion recommendation)"
    - "Contact section heading 'Get in touch' and subtext are preserved verbatim (D-20)"
  artifacts:
    - path: "src/components/contact/contact-schema.ts"
      provides: "Zod schema + z.infer type — single source of truth for client validation and length caps"
      contains: "z.object"
    - path: "src/components/contact/ContactForm.tsx"
      provides: "'use client' form Client Component (RHF + zodResolver, honeypot, submit handler, error block)"
      contains: "'use client'"
    - path: "src/components/contact/Contact.tsx"
      provides: "Server Component shell that embeds <ContactForm /> + plain-text email caption"
  key_links:
    - from: "src/components/contact/ContactForm.tsx"
      to: "src/data/site.ts"
      via: "import { FORMSPREE_ENDPOINT } from '@/data/site'"
      pattern: "FORMSPREE_ENDPOINT"
    - from: "src/components/contact/ContactForm.tsx"
      to: "src/components/contact/contact-schema.ts"
      via: "import { contactSchema, type ContactFormValues } from './contact-schema'"
      pattern: "contactSchema"
    - from: "src/components/contact/Contact.tsx"
      to: "src/components/contact/ContactForm.tsx"
      via: "import ContactForm from './ContactForm'"
      pattern: "ContactForm"
    - from: "src/components/contact/ContactForm.tsx onSubmit"
      to: "FORMSPREE_ENDPOINT (Formspree)"
      via: "fetch(FORMSPREE_ENDPOINT, { method: 'POST', headers: { Accept: 'application/json', 'Content-Type': 'application/json' }, body: JSON.stringify({ ...values, _gotcha: gotchaRef.current?.value ?? '' }) })"
      pattern: "fetch\\(FORMSPREE_ENDPOINT"
    - from: "ContactForm onSubmit success branch"
      to: "/thanks (created in plan 06-04)"
      via: "router.push('/thanks') from useRouter() of next/navigation"
      pattern: "router\\.push\\(['\\\"]/thanks['\\\"]"
---

<objective>
Vertical slice that delivers the working form end-to-end inside the existing Contact section: Zod schema, RHF + zodResolver Client Component, AJAX submit to Formspree, success → `/thanks` navigation, error → email-fallback block, plain-text email caption (D-21), and removal of the legacy direct-link cluster (D-23). After this plan ships, a visitor can fill the form, submit it, and (assuming Formspree ID is configured in plan 06-06) reach the inbox.

Purpose: This is the heart of Phase 6. It satisfies SC-1 (form posts to Formspree), the rendered-HTML half of SC-2 (plain-text email visible), part of SC-3 (Contact-section anchors removed), and SC-4 (validation + states + reduced-motion respected by inheriting FadeUp + token-driven CSS only).
Output: Two new files (schema + ContactForm Client Component) and a refactored `Contact.tsx` that becomes a Server Component shell embedding the Client Component plus the plain-text email caption.
</objective>

<execution_context>
@/Users/axel/code/website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-get-in-touch-form/06-CONTEXT.md
@.planning/phases/06-get-in-touch-form/06-01-PLAN.md
@CLAUDE.md
@src/components/contact/Contact.tsx
@src/data/cv.ts
@src/data/site.ts
@src/styles/tokens.css
@src/components/ui/FadeUp.tsx
@/Users/axel/.claude/projects/-Users-axel-code-website/memory/feedback_ssr_hydration_determinism.md
@/Users/axel/.claude/rules/typescript/coding-style.md

<interfaces>
<!-- Source-of-truth imports for form values + endpoint -->

From src/data/site.ts (created in plan 06-01):
- `export const FORMSPREE_ENDPOINT: string`

From src/data/cv.ts (used for plain-text email caption):
- `export const contact: Contact = { email: 'axel.waserman@gmail.com', ... }`

From next/navigation (Next 16 App Router):
- `useRouter(): { push(url: string): void; ... }` — used in the form's onSubmit success branch (D-15)

From react-hook-form (added in plan 06-01):
- `useForm<TFieldValues>({ resolver, mode, shouldFocusError, ... })`
- Returns `{ register, handleSubmit, formState: { errors, isSubmitting }, ... }`

From @hookform/resolvers/zod:
- `zodResolver(schema)`

Target shape for src/components/contact/contact-schema.ts (D-07, D-08, D-09):
- `import { z } from 'zod'`
- `export const contactSchema = z.object({ name: z.string().trim().min(1, ...).max(100, ...), email: z.string().trim().email(...).max(254, ...), company: z.string().trim().max(200, ...).optional().or(z.literal('')), message: z.string().trim().min(1, ...).max(5000, ...) })`
- `export type ContactFormValues = z.infer<typeof contactSchema>`
- Single source of truth for both runtime validation and TypeScript form-values type
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Zod contact schema and inferred type</name>
  <files>src/components/contact/contact-schema.ts</files>
  <read_first>
    - .planning/phases/06-get-in-touch-form/06-CONTEXT.md (D-05 fields, D-06 company label, D-07 lib choice, D-08 length caps, D-09 email format)
    - /Users/axel/.claude/rules/typescript/coding-style.md (Zod + z.infer pattern)
    - src/data/cv.ts (style reference for module-level exports)
  </read_first>
  <behavior>
    <!-- Behaviour described here is exercised at runtime by the Playwright e2e test in plan 06-06 task 1 (real form input traverses the schema via RHF + zodResolver). This task verifies the schema compiles and exports correctly; runtime parse cases are not asserted here because Node cannot natively `require` a `.ts` file and adding `tsx` solely for one assertion is overkill. -->
    - `contactSchema.parse({ name: 'A', email: 'a@b.co', message: 'hi' })` succeeds (company is optional)
    - `contactSchema.parse({ name: '', email: 'a@b.co', message: 'hi' })` throws (empty name rejected)
    - `contactSchema.parse({ name: 'A', email: 'not-an-email', message: 'hi' })` throws
    - `contactSchema.parse({ name: 'A'.repeat(101), email: 'a@b.co', message: 'hi' })` throws (name > 100)
    - `contactSchema.parse({ name: 'A', email: 'a@b.co', message: 'x'.repeat(5001) })` throws (message > 5000)
    - Email length cap: rejects `('a'.repeat(255)) + '@b.co'`
    - `ContactFormValues` type compiles with the four expected keys
  </behavior>
  <action>
    Create `src/components/contact/contact-schema.ts` exporting `contactSchema` (a `z.object` with the four fields per D-05/D-08/D-09) and `ContactFormValues` as `z.infer<typeof contactSchema>`. Use `.trim()` before `.min()` / `.max()` so leading/trailing whitespace does not bypass length caps. Provide human-readable Zod messages for each rule (e.g., 'Please enter your name', 'Please enter a valid email', 'Message can be at most 5000 characters') — these will surface verbatim under each field per D-11. The optional `company` field handles both 'absent' and 'present-but-empty' inputs (use `.optional().or(z.literal(''))` or `.transform(v => v ?? '')` — choose the form that keeps the inferred type as `string | undefined` to play nicely with RHF's defaultValues). Keep the file pure data (no React imports, no fetch, no side effects) so it can be imported by both ContactForm and unit tests.
  </action>
  <verify>
    <!-- Type-check covers the schema export shape and `z.infer<typeof contactSchema>`. Runtime parse cases listed in <behavior> are validated by the Playwright e2e test in plan 06-06 task 1, which exercises real form input through RHF + zodResolver. Avoids needing tsx as a dependency just to invoke the schema from Node. -->
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File exports `contactSchema` (Zod schema) and `ContactFormValues` (inferred type)
    - `grep -nE 'z\.string\(\)\..*\.max\(100' src/components/contact/contact-schema.ts` matches (name cap)
    - `grep -nE '\.max\(254' src/components/contact/contact-schema.ts` matches (email cap)
    - `grep -nE '\.max\(5000' src/components/contact/contact-schema.ts` matches (message cap)
    - `grep -nE '\.max\(200' src/components/contact/contact-schema.ts` matches (company cap)
    - `grep -nE '\.email\(' src/components/contact/contact-schema.ts` matches (D-09)
    - `grep -nE 'z\.infer<typeof contactSchema>' src/components/contact/contact-schema.ts` matches
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Schema file committed; type-check passes; runtime parse behaviour deferred to plan 06-06 e2e.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build ContactForm Client Component (RHF + Zod + AJAX submit)</name>
  <files>src/components/contact/ContactForm.tsx</files>
  <read_first>
    - src/components/contact/contact-schema.ts (created in task 1)
    - src/data/site.ts (FORMSPREE_ENDPOINT)
    - src/styles/tokens.css (palette, type scale, radius for input styling)
    - .planning/phases/06-get-in-touch-form/06-CONTEXT.md (D-02 honeypot, D-10..D-16 timing/states, design discretion bullets)
    - /Users/axel/.claude/rules/web/coding-style.md (semantic form HTML)
    - /Users/axel/.claude/rules/web/design-quality.md (anti-template, intentional focus states)
    - /Users/axel/.claude/projects/-Users-axel-code-website/memory/feedback_ssr_hydration_determinism.md (no Math.random / Date.now)
  </read_first>
  <behavior>
    - Component is a Client Component: first line is `'use client'`
    - Renders semantic `<form>` with four labelled fields (`name`, `email`, `company` optional, `message`) plus a hidden uncontrolled honeypot input named `_gotcha`
    - Uses `useForm<ContactFormValues>({ resolver: zodResolver(contactSchema), mode: 'onBlur', shouldFocusError: true, defaultValues: { name: '', email: '', company: '', message: '' } })`
    - Each field has `aria-invalid={!!errors.field}` and `aria-describedby` linked to its error span (which has a stable id)
    - Inline error span renders only when the corresponding error is present (D-11)
    - Submit button is `type="submit"`, label 'Send message' by default, becomes disabled with label 'Sending…' while `formState.isSubmitting` is true (D-14)
    - On submit: reads honeypot value via ref and merges it into the JSON body, POSTs to FORMSPREE_ENDPOINT with `Accept: 'application/json'` and `Content-Type: 'application/json'` (D-13)
    - On `response.ok`: calls `router.push('/thanks')` (D-15)
    - On non-ok or thrown fetch error: sets a local `submitError` state to the email-fallback message (D-16) and renders an inline error block above the submit button; `submitError` clears when the form is re-submitted
    - No Math.random / Date.now / new Date() calls in the render path (per memory `feedback_ssr_hydration_determinism.md`); all stable id strings are static literals (e.g., 'contact-name-error') — RHF errors map to fixed ids
  </behavior>
  <action>
    Create `src/components/contact/ContactForm.tsx` as a `'use client'` React function component. Imports: `useForm` from `react-hook-form`, `zodResolver` from `@hookform/resolvers/zod`, `useRouter` from `next/navigation`, `useState` and `useRef` from `react`, `contactSchema` and `ContactFormValues` from `./contact-schema`, `FORMSPREE_ENDPOINT` from `@/data/site`. Body:
    1. Initialise `useForm<ContactFormValues>` per the behavior block.
    2. Local `submitError` state (`useState<string | null>(null)`) holding the user-facing fallback string from D-16: `"Something went wrong. Email me directly at axel.waserman@gmail.com."`
    3. Local `gotchaRef` (`useRef<HTMLInputElement>(null)`) for the honeypot input.
    4. `const onSubmit = async (values: ContactFormValues) => { ... }` that: clears `submitError`; constructs the request body as `JSON.stringify({ ...values, _gotcha: gotchaRef.current?.value ?? '' })` (D-13 uses AJAX with JSON body, so the honeypot must be merged in manually since it is uncontrolled and outside RHF's tracked values); calls fetch with the headers in D-13; on `!response.ok` sets `submitError` and returns; on success calls `router.push('/thanks')`. Wrap fetch in try/catch — network errors take the same fallback path.
    5. JSX: `<form onSubmit={handleSubmit(onSubmit)} noValidate>` containing each field with `<label htmlFor>` + `<input>`/`<textarea>` + conditional `<p id="...-error" role="alert">` for the error message. Company field label MUST be exactly `Company (optional)` per D-06. Honeypot is rendered as a plain uncontrolled input — NOT registered with RHF (the schema does not include it; do not use `register('_gotcha' as never)` — that violates the no-`any`/no-`never`-cast rule from typescript/coding-style.md). Exact markup: `<input ref={gotchaRef} id="contact-gotcha" name="_gotcha" type="text" tabIndex={-1} aria-hidden="true" autoComplete="off" style={{ display: 'none' }} />`. The submit-error block (D-16) renders above the submit button when `submitError` is non-null with `role="alert"`.
    6. Styling via Tailwind v4 + CSS-token utilities (`text-[length:var(--text-ui)]`, `text-[var(--color-text)]`, `bg-[var(--color-surface)]`, `border-[var(--color-muted)]`, `rounded-[var(--radius-card)]`, etc.). Anti-template policy: deliberate input borders, focus state via `focus:outline-2 focus:outline-[var(--color-accent)] focus:outline-offset-2` matching existing anchor pattern, label typography contrast (smaller, muted) vs input typography (body size, full text color). Single column, max-width matches the section's `max-w-2xl`. No animation beyond default focus transitions (FadeUp on the parent section is inherited from Contact.tsx; no in-component motion needed — D-04 of Phase 5 / SC-4 reduced-motion respected by absence).

    Submit button label: 'Send message' (per discretion bullet recommendation in CONTEXT.md). Sending label: 'Sending…' (single character `…`).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File first non-comment line is `'use client'` (regex check `head -3 src/components/contact/ContactForm.tsx | grep -c "use client"` >= 1)
    - `grep -nE "from 'react-hook-form'" src/components/contact/ContactForm.tsx` matches
    - `grep -nE "zodResolver" src/components/contact/ContactForm.tsx` matches
    - `grep -nE "FORMSPREE_ENDPOINT" src/components/contact/ContactForm.tsx` matches (no hardcoded URL)
    - `grep -nE "useRouter" src/components/contact/ContactForm.tsx` matches and `grep -nE "router\.push\(['\"]/thanks['\"]" src/components/contact/ContactForm.tsx` matches
    - `grep -nE "_gotcha" src/components/contact/ContactForm.tsx` matches (honeypot present)
    - `grep -nE "useRef" src/components/contact/ContactForm.tsx` matches (honeypot ref)
    - `grep -nE "as never" src/components/contact/ContactForm.tsx` returns no matches (no type bypass — coding-style.md)
    - `grep -nE "Sending" src/components/contact/ContactForm.tsx` matches (loading label)
    - `grep -nE "axel\.waserman@gmail\.com" src/components/contact/ContactForm.tsx` matches (D-16 fallback string)
    - `grep -nE "mode:\s*['\"]onBlur['\"]" src/components/contact/ContactForm.tsx` matches (D-10)
    - `grep -nE "shouldFocusError" src/components/contact/ContactForm.tsx` matches (D-12)
    - `grep -nE "Math\.random|Date\.now|new Date\(" src/components/contact/ContactForm.tsx` returns no matches
    - `grep -nE "aria-invalid|aria-describedby" src/components/contact/ContactForm.tsx` matches at least once each
    - `grep -nE "Company \(optional\)" src/components/contact/ContactForm.tsx` matches (D-06 verbatim label)
    - `npx tsc --noEmit`, `npm run lint`, `npm run build` all exit 0
  </acceptance_criteria>
  <done>ContactForm Client Component compiles, lints, builds; all D-02/D-06/D-10/D-12/D-13/D-14/D-15/D-16 markers present; honeypot uncontrolled+merged-into-body; SSR-deterministic.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Contact section to embed ContactForm and remove direct-link cluster</name>
  <files>src/components/contact/Contact.tsx</files>
  <read_first>
    - src/components/contact/Contact.tsx (current direct-link cluster — to be replaced)
    - src/components/contact/ContactForm.tsx (created in task 2)
    - src/data/cv.ts (contact.email for plain-text caption)
    - .planning/phases/06-get-in-touch-form/06-CONTEXT.md (D-20 verbatim copy, D-21 caption, D-23 anchors removed, discretion bullet on Download CV removal)
  </read_first>
  <action>
    Edit `src/components/contact/Contact.tsx` so it remains a Server Component (no `'use client'` at top) but its body becomes:
    1. Same `<section id="contact" aria-labelledby="contact-heading" className="py-[var(--space-section)] px-6 max-w-2xl mx-auto scroll-mt-16">` wrapper (preserved exactly — unblocks the Hero `#contact` anchor swap in plan 06-05 + Header anchor nav).
    2. Heading `<h2 id="contact-heading">Get in touch</h2>` and subtext `<p>Open to opportunities, collaborations, and interesting conversations.</p>` preserved verbatim per D-20.
    3. Replace the existing `<div className="flex flex-wrap gap-4 justify-center">…anchors…</div>` cluster with `<ContactForm />` (default-imported from `./ContactForm`).
    4. Below the form, render a small plain-text email caption `<p>` per D-21 — text content `Or reach me directly at axel.waserman@gmail.com.` — value pulled from `contact.email` (existing import) interpolated into the string template, so there is no hardcoded duplicate. Style: `text-[length:var(--text-ui)]` size, `text-[var(--color-muted)]` color, centered, top margin `mt-6`. CRITICAL: it must be a plain text node, NOT an `<a>` tag (D-21) — that is what makes it ATS-parseable yet not a `mailto:` resurrection.
    5. Remove all four anchors (mailto, LinkedIn, GitHub, Download CV) per D-23 + the discretion bullet that recommends removing the Contact-section Download CV link (Hero CTA + CV-section button still provide it; keeps the section purely form-focused).
    6. Keep the existing `import { contact } from '@/data/cv'` import; the `contact` import remains required for the email caption (`contact.email`), so do not remove it.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node -e "const fs=require('fs'); const html=fs.readFileSync('out/index.html','utf8'); /* contact section sanity */ if (!/Get in touch/.test(html)) { console.error('Heading missing'); process.exit(1); } if (!/Or reach me directly at axel\.waserman@gmail\.com/.test(html)) { console.error('Plain-text email caption missing (D-21 / SC-2 rendered)'); process.exit(1); } if (/<a [^>]*href=\"mailto:axel\.waserman@gmail\.com\"/.test(html)) { console.error('mailto anchor still present (D-23)'); process.exit(1); } /* honeypot present */ if (!/_gotcha/.test(html)) { console.error('Honeypot field missing'); process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "<ContactForm" src/components/contact/Contact.tsx` matches
    - `grep -nE "mailto:" src/components/contact/Contact.tsx` returns no matches
    - `grep -nE "LinkedIn|GitHub|Download CV" src/components/contact/Contact.tsx` returns no matches (D-23 + discretion-bullet removal)
    - `grep -nE "Or reach me directly at" src/components/contact/Contact.tsx` matches (D-21)
    - `grep -nE "Get in touch" src/components/contact/Contact.tsx` matches (D-20 heading preserved)
    - `grep -nE "Open to opportunities" src/components/contact/Contact.tsx` matches (D-20 subtext preserved)
    - `grep -nE "'use client'" src/components/contact/Contact.tsx` returns no matches (still a Server Component)
    - `out/index.html` contains 'Or reach me directly at axel.waserman@gmail.com' (SC-2 rendered-HTML half)
    - `out/index.html` contains the form (`name="message"` etc.)
    - `out/index.html` contains '_gotcha' substring (honeypot rendered)
    - `out/index.html` does not contain `href="mailto:axel.waserman@gmail.com"` in the contact section
    - `npm run build`, `npm run lint`, `npx tsc --noEmit` all exit 0
  </acceptance_criteria>
  <done>Contact section renders form + plain-text email caption + heading/subtext only; all four anchors removed; SC-2 rendered half + SC-3 Contact half satisfied.</done>
</task>

</tasks>

<verification>
- `npm run build` produces `out/index.html` containing the form fields, honeypot, plain-text email caption, no mailto anchor in the contact section
- Lint + type-check pass
- Manual sanity (covered in plan 06-06): `npm run dev`, fill form with stub Formspree ID, network tab shows POST with the right headers and JSON body
</verification>

<success_criteria>
- SC-1 progress: Form posts JSON to FORMSPREE_ENDPOINT (final delivery validated in plan 06-06 with real ID)
- SC-2 (rendered HTML half): plain-text `axel.waserman@gmail.com` present in `out/index.html`
- SC-3 (Contact half): mailto / LinkedIn / GitHub anchors removed from Contact section; form is the primary CTA
- SC-4: Validation client-side (Zod via RHF), success/error states (D-15/D-16), reduced-motion respected (no JS animation)
</success_criteria>

<output>
Create `.planning/phases/06-get-in-touch-form/06-03-SUMMARY.md` when done.
</output>
</content>
</invoke>