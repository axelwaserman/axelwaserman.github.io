---
phase: 06-get-in-touch-form
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/thanks/page.tsx
autonomous: true
requirements: [SC-1]
# SC-1: form posts to Formspree | SC-2: JSON-LD + plain-text email visible | SC-3: anchors removed/swapped | SC-4: validation/states/reduced-motion
must_haves:
  truths:
    - "/thanks renders as a static page in the export output (out/thanks.html or out/thanks/index.html exists after npm run build)"
    - "/thanks page reuses the existing Header component"
    - "/thanks shows a thank-you heading + body copy + a plain-text fallback email + a Back-to-home link"
    - "/thanks is excluded from search engine indexing via metadata.robots.index = false (D-18)"
    - "/thanks has no client-only data fetching and no server-only APIs (compatible with output: 'export')"
  artifacts:
    - path: "src/app/thanks/page.tsx"
      provides: "Static /thanks confirmation route"
      contains: "export const metadata"
  key_links:
    - from: "src/components/contact/ContactForm.tsx onSubmit"
      to: "src/app/thanks/page.tsx"
      via: "router.push('/thanks')"
      pattern: "/thanks"
---

<objective>
Add the static `/thanks` confirmation route the form navigates to on successful submit (D-15, D-17, D-18). Page reuses the existing Header, shows a centered thanks block, restates the plain-text fallback email, and provides a Back-to-home link. Marked `noindex` because it is not a content page.

Purpose: The form's success state needs a real destination. Without `/thanks`, `router.push('/thanks')` 404s on the deployed static site.
Output: New `src/app/thanks/page.tsx` rendered as a static export-compatible Server Component with `metadata.robots.index = false`.
</objective>

<execution_context>
@/Users/axel/code/website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-get-in-touch-form/06-CONTEXT.md
@CLAUDE.md
@src/app/layout.tsx
@src/components/header/Header.tsx
@src/data/cv.ts
@src/styles/tokens.css

<interfaces>
<!-- Existing assets the /thanks page reuses -->

From src/components/header/Header.tsx:
- Default export: sticky header with anchor nav. Reused unchanged on /thanks.

From src/data/cv.ts:
- `contact.email = 'axel.waserman@gmail.com'` (used in plain-text fallback line)

From next:
- `Metadata` type from 'next'
- `metadata.robots = { index: false, follow: false }` is the App Router noindex pattern (D-18)

From next/link:
- `Link` component for the Back-to-home anchor
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create static /thanks page</name>
  <files>src/app/thanks/page.tsx</files>
  <read_first>
    - src/components/header/Header.tsx (component to reuse — confirm default export name)
    - src/data/cv.ts (contact.email)
    - src/app/layout.tsx (existing metadata shape — match the pattern)
    - .planning/phases/06-get-in-touch-form/06-CONTEXT.md (D-17 layout, D-18 noindex, /thanks copy proposal)
    - src/styles/tokens.css (palette + type scale tokens)
  </read_first>
  <action>
    Create `src/app/thanks/page.tsx` as a Server Component (no `'use client'`). It must:
    1. Import `Header` from `@/components/header/Header`, `Link` from `next/link`, `contact` from `@/data/cv`, and the `Metadata` type from `next`.
    2. Export `const metadata: Metadata = { title: 'Thanks — Axel Waserman', robots: { index: false, follow: false } }` per D-18.
    3. Default-export a Server Component that renders `<Header />` followed by a `<main>` with a centered, single-column block. Suggested copy per D-17 + CONTEXT proposal:
       - `<h1>Thanks — message received.</h1>`
       - `<p>I read every message and will reply soon.</p>`
       - `<p>Or email me directly at axel.waserman@gmail.com.</p>` (plain text — interpolate `contact.email`, not a `mailto:` link)
       - `<Link href="/">← Back to home</Link>`
    4. Use the same token-based Tailwind utilities used elsewhere (`text-[length:var(--text-heading)]`, `text-[length:var(--text-body)]`, `text-[var(--color-muted)]`, `text-[var(--color-text)]`, `font-[var(--font-heading)]`, `bg-[var(--color-surface)]` if needed). Constrain main to `max-w-2xl mx-auto px-6 py-[var(--space-section)] text-center`.
    5. Do NOT add any client-only or server-only API calls. The page must be statically exportable under `output: 'export'`. No `dynamic`, no Route Handlers, no `cookies()`/`headers()` reads.
    6. The Back-to-home link must use `next/link` (not a raw anchor) so the App Router handles the navigation correctly.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node -e "const fs=require('fs'); const candidates=['out/thanks.html','out/thanks/index.html']; const found=candidates.find(p=>fs.existsSync(p)); if (!found) { console.error('No /thanks page in static export. Checked:', candidates); process.exit(1); } const html=fs.readFileSync(found,'utf8'); if (!/Thanks/.test(html)) { console.error('Heading missing'); process.exit(1); } if (!/axel\.waserman@gmail\.com/.test(html)) { console.error('Plain-text email missing'); process.exit(1); } if (!/(Back to home|← Back)/.test(html)) { console.error('Back link missing'); process.exit(1); } if (!/<meta\s+name=\"robots\"\s+content=\"[^\"]*noindex/.test(html)) { console.error('noindex meta missing (D-18)'); process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/thanks/page.tsx` exists and exports both `metadata` and a default component
    - `grep -nE "robots:\s*\{\s*index:\s*false" src/app/thanks/page.tsx` matches (D-18)
    - `grep -nE "import Header from '@/components/header/Header'" src/app/thanks/page.tsx` matches (Header default-imported and reused)
    - `grep -nE "axel\.waserman@gmail\.com|contact\.email" src/app/thanks/page.tsx` matches (plain-text fallback)
    - `grep -nE "from\s+'next/link'" src/app/thanks/page.tsx` matches (Back-to-home uses Link)
    - `grep -nE "'use client'" src/app/thanks/page.tsx` returns no matches (Server Component)
    - `npm run build` produces a /thanks page in `out/` (either `out/thanks.html` or `out/thanks/index.html`) and the verify command above exits 0
    - Generated HTML contains `<meta name="robots" content="noindex...">`
  </acceptance_criteria>
  <done>Static /thanks page exported with Header + thanks block + plain-text email + Back-to-home link + noindex.</done>
</task>

</tasks>

<verification>
- `npm run build` passes and produces /thanks in the static export
- Lint + type-check pass
- Visual verification deferred to plan 06-06 (Playwright + Axel screenshot review)
</verification>

<success_criteria>
- SC-1 (success-state half): valid form submit can navigate to a real `/thanks` page
- D-17 / D-18 satisfied: Header + thanks block + plain-text email + Back link; noindex meta present
- Static-export compatibility preserved
</success_criteria>

<output>
Create `.planning/phases/06-get-in-touch-form/06-04-SUMMARY.md` when done.
</output>
</content>
</invoke>