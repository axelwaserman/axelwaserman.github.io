---
phase: 06-get-in-touch-form
plan: 05
type: execute
wave: 3
depends_on: ['06-03']
files_modified:
  - src/components/hero/Hero.tsx
autonomous: true
requirements: [SC-3]
# SC-1: form posts to Formspree | SC-2: JSON-LD + plain-text email visible | SC-3: anchors removed/swapped | SC-4: validation/states/reduced-motion
must_haves:
  truths:
    - "Hero CTA cluster contains exactly four anchors in this order: GitHub, LinkedIn, Get in touch, Download CV (D-19)"
    - "The 'Get in touch' anchor is a same-page anchor href='#contact' (not mailto:) and scrolls to the Contact section (D-19)"
    - "The previous mailto: anchor is removed from Hero"
    - "GitHub, LinkedIn, and Download CV anchors are preserved with their existing hrefs (form-first only replaces Email, not all socials)"
  artifacts:
    - path: "src/components/hero/Hero.tsx"
      provides: "Updated Hero CTA cluster (form-first direction)"
  key_links:
    - from: "src/components/hero/Hero.tsx 'Get in touch' anchor"
      to: "src/components/contact/Contact.tsx (id='contact')"
      via: "<a href='#contact'>Get in touch</a> — Header anchor-nav pattern reused"
      pattern: "href=[\"']#contact[\"']"
---

<objective>
Replace the Hero `mailto:` anchor with a same-page `<a href="#contact">Get in touch</a>` that scrolls to the Contact section (D-19), preserving the other three CTAs (GitHub, LinkedIn, Download CV). Closes the Hero half of SC-3.

Purpose: Hero CTA cluster must be consistent with the form-first direction. The Email anchor cannot survive when the Contact section's mailto link has been removed.
Output: Edited `src/components/hero/Hero.tsx` with the swapped CTA — only the Email anchor changes.
</objective>

<execution_context>
@/Users/axel/code/website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-get-in-touch-form/06-CONTEXT.md
@CLAUDE.md
@src/components/hero/Hero.tsx
@src/components/contact/Contact.tsx
@src/components/header/Header.tsx

<interfaces>
<!-- Hero current CTA cluster (to be edited) -->

From src/components/hero/Hero.tsx (current):
- Four anchors in order: GitHub (target=_blank), LinkedIn (target=_blank), Email (mailto:axel.waserman@gmail.com), Download CV (/cv.pdf)
- All share the same className for consistent visual treatment
- Section has id="hero" and the page has id="contact" elsewhere — same-page anchor `#contact` resolves correctly

From src/components/header/Header.tsx (pattern reference):
- Existing `#contact` anchor link in nav — confirms scroll-to behavior already works site-wide (Phase 2 D-22, sticky header + scroll-mt-16 on Contact section)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap Hero Email anchor for #contact same-page link</name>
  <files>src/components/hero/Hero.tsx</files>
  <read_first>
    - src/components/hero/Hero.tsx (current 4-CTA cluster)
    - src/components/header/Header.tsx (pattern reference for #contact anchor)
    - src/components/contact/Contact.tsx (confirm `id="contact"` is preserved post-plan-06-03)
    - .planning/phases/06-get-in-touch-form/06-CONTEXT.md (D-19 exact wording)
  </read_first>
  <action>
    Edit `src/components/hero/Hero.tsx`. Locate the third anchor (`<a href="mailto:axel.waserman@gmail.com">Email</a>`) and replace it with `<a href="#contact">Get in touch</a>`. Preserve the existing className verbatim so the four CTAs remain visually consistent. Remove `target="_blank"` and `rel="noopener noreferrer"` if (incorrectly) added — the anchor is same-page. Do NOT add JS scroll handlers; the existing Contact section's `scroll-mt-16` handles the sticky-header offset (Phase 2 pattern). Do NOT touch the GitHub, LinkedIn, or Download CV anchors. Order in the markup must be: GitHub → LinkedIn → Get in touch → Download CV (matches D-19's stated CTA list left-to-right).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node -e "const fs=require('fs'); const html=fs.readFileSync('out/index.html','utf8'); /* mailto removed from hero region — page-wide check is sufficient since Contact also lost its mailto */ if (/mailto:axel\.waserman@gmail\.com/.test(html)) { console.error('mailto: still present in HTML — should be fully removed (Hero + Contact)'); process.exit(1); } /* Get in touch anchor present */ if (!/href=\"#contact\"[^>]*>\s*Get in touch/.test(html)) { console.error('Hero #contact anchor missing or wrong text'); process.exit(1); } /* Other Hero CTAs preserved */ if (!/href=\"https:\/\/github\.com\/axelwaserman\"/.test(html)) { console.error('GitHub Hero CTA missing'); process.exit(1); } if (!/href=\"https:\/\/www\.linkedin\.com\/in\/axel-waserman-9753221a6\/\"/.test(html)) { console.error('LinkedIn Hero CTA missing'); process.exit(1); } if (!/href=\"\/cv\.pdf\"/.test(html)) { console.error('Download CV Hero CTA missing'); process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'href="mailto:' src/components/hero/Hero.tsx` returns no matches (Hero Email anchor gone)
    - `grep -nE 'href="#contact"[^>]*>\s*Get in touch' src/components/hero/Hero.tsx` matches (D-19 exact wording)
    - `grep -cE 'href="https://github\.com/axelwaserman"' src/components/hero/Hero.tsx` >= 1 (GitHub preserved)
    - `grep -cE 'href="https://www\.linkedin\.com/in/axel-waserman-9753221a6/"' src/components/hero/Hero.tsx` >= 1 (LinkedIn preserved)
    - `grep -cE 'href="/cv\.pdf"' src/components/hero/Hero.tsx` >= 1 (Download CV preserved)
    - The four anchors appear in the file in the order GitHub → LinkedIn → Get in touch → Download CV
    - `npm run build`, `npm run lint`, `npx tsc --noEmit` all exit 0
  </acceptance_criteria>
  <done>Hero CTA cluster matches D-19; mailto removed; other three anchors untouched.</done>
</task>

</tasks>

<verification>
- `npm run build` passes; built HTML contains the four expected hero anchors and zero `mailto:` references
- Manual scroll behaviour validated in plan 06-06 (Playwright)
</verification>

<success_criteria>
- SC-3 (Hero half): mailto anchor replaced with `#contact`; GitHub / LinkedIn / Download CV preserved
- D-19 satisfied verbatim
</success_criteria>

<output>
Create `.planning/phases/06-get-in-touch-form/06-05-SUMMARY.md` when done.
</output>
</content>
</invoke>