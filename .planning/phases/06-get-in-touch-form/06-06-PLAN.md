---
phase: 06-get-in-touch-form
plan: 06
type: execute
wave: 3
depends_on: ['06-01', '06-02', '06-03', '06-04', '06-05']
files_modified:
  - e2e/uat-phase-06.spec.ts
  - src/data/site.ts
autonomous: false
requirements: [SC-1, SC-2, SC-3, SC-4]
# SC-1: form posts to Formspree | SC-2: JSON-LD + plain-text email visible | SC-3: anchors removed/swapped | SC-4: validation/states/reduced-motion
must_haves:
  truths:
    - "Playwright E2E spec exists for Phase 6 covering: validation errors on empty submit, valid submit triggers POST to FORMSPREE_ENDPOINT (mocked), success navigates to /thanks, network error renders fallback block"
    - "Playwright captures screenshots of the contact section + /thanks at 320, 768, and 1440 widths under e2e/screenshots/, plus a reduced-motion screenshot"
    - "JSON-LD <script type='application/ld+json'> Person block verified to be in the rendered HTML at the live (built) site"
    - "Plain-text 'axel.waserman@gmail.com' verified to be present in the contact section of the rendered HTML (SC-2 visible-HTML half)"
    - "Real Formspree form ID is pasted into src/data/site.ts (replacing the placeholder) and a real submission delivers to axel.waserman@gmail.com (manual checkpoint)"
  artifacts:
    - path: "e2e/uat-phase-06.spec.ts"
      provides: "End-to-end happy-path + error-path coverage of the contact form"
      contains: "test("
    - path: "e2e/screenshots/"
      provides: "Visual review screenshots at 320/768/1440 + reduced-motion"
    - path: "src/data/site.ts"
      provides: "Real Formspree endpoint pasted in (replaces placeholder from plan 06-01)"
  key_links:
    - from: "e2e/uat-phase-06.spec.ts mocked POST"
      to: "FORMSPREE_ENDPOINT"
      via: "page.route('**/formspree.io/f/**', ...)"
      pattern: "formspree\\.io"
---

<objective>
End-to-end verification of Phase 6 against all four success criteria, plus the user-action checkpoint to provision the real Formspree form ID and confirm a real email lands in `axel.waserman@gmail.com`. Per memory `feedback_visual_review_static_export.md`, curl/grep checks alone are insufficient — Playwright + Axel eyeball is required before merge.

Purpose: This is the gate plan. It catches anything missed by individual plans' `<verify>` blocks (e.g., visual regressions, anti-template policy slips, focus-state misses, the form not actually being usable end-to-end) and confirms the Formspree pipe is live.
Output: A new `e2e/uat-phase-06.spec.ts` covering happy-path + error-path + visual screenshots; an updated `src/data/site.ts` with the real Formspree ID; an explicit human checkpoint confirming a real test submission was received.
</objective>

<execution_context>
@/Users/axel/code/website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-get-in-touch-form/06-CONTEXT.md
@.planning/phases/06-get-in-touch-form/06-01-PLAN.md
@.planning/phases/06-get-in-touch-form/06-02-PLAN.md
@.planning/phases/06-get-in-touch-form/06-03-PLAN.md
@.planning/phases/06-get-in-touch-form/06-04-PLAN.md
@.planning/phases/06-get-in-touch-form/06-05-PLAN.md
@CLAUDE.md
@playwright.config.ts
@e2e/uat-phase-02.spec.ts
@e2e/uat-phase-03.spec.ts
@/Users/axel/.claude/projects/-Users-axel-code-website/memory/feedback_visual_review_static_export.md
@/Users/axel/.claude/projects/-Users-axel-code-website/memory/feedback_phase_pr_workflow.md

<interfaces>
<!-- Playwright config + prior phase test patterns -->

From playwright.config.ts:
- baseURL: http://localhost:3001
- testDir: ./e2e
- outputDir: ./e2e/screenshots
- webServer: `npm run dev -- --port 3001`, reuseExistingServer: true

From @playwright/test:
- `test`, `expect`, `Page` types
- `page.route(urlGlob, handler)` for mocking the Formspree POST so the test never hits the real endpoint
- `page.setViewportSize({ width, height })` for breakpoint screenshots
- `page.screenshot({ path, fullPage: true })`

From src/data/site.ts (created in plan 06-01):
- `FORMSPREE_ENDPOINT` — placeholder URL until this plan's user-action task pastes the real ID
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Playwright E2E spec covering form happy + error paths and visual screenshots</name>
  <files>e2e/uat-phase-06.spec.ts</files>
  <read_first>
    - e2e/uat-phase-02.spec.ts (reference pattern — viewport sizing, screenshot path conventions, test structure)
    - e2e/uat-phase-03.spec.ts (reference pattern)
    - playwright.config.ts (baseURL, output dir)
    - .planning/phases/06-get-in-touch-form/06-CONTEXT.md (D-10..D-16 expected behaviors)
    - /Users/axel/.claude/projects/-Users-axel-code-website/memory/feedback_visual_review_static_export.md (visual-review requirement)
  </read_first>
  <action>
    Create `e2e/uat-phase-06.spec.ts` with the following tests (use `test.describe('Phase 6 — Contact form', ...)`):

    1. **Validation errors**: navigate to `/`, scroll to `#contact`, click the submit button without filling anything, expect inline error messages to be visible under `name`, `email`, `message` fields (use `getByText` with the human-readable Zod messages from plan 06-03 task 1, or `getByRole('alert')` if errors use role=alert).

    2. **Happy path with mocked Formspree**: use `page.route('**/formspree.io/**', route => route.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify({ ok: true }) }))` BEFORE navigating. Then visit `/`, fill the four fields (use `getByLabel('Name')`, `getByLabel('Email')`, `getByLabel('Company (optional)')` (D-06 verbatim), `getByLabel('Message')`), submit, expect `await expect(page).toHaveURL(/\/thanks\/?$/)`. Verify `/thanks` shows the thanks heading, the plain-text email, and the Back-to-home link.

    3. **Error path with mocked 4xx**: route Formspree to fulfill with status 500, submit a valid form, expect the inline error block to render with substring `axel.waserman@gmail.com` (D-16 fallback message).

    4. **Honeypot present + hidden**: assert that `page.locator('input[name="_gotcha"]')` exists (`toBeAttached()`) but is `not.toBeVisible()`.

    5. **JSON-LD presence (SC-2)**: navigate to `/`, evaluate `document.querySelector('script[type="application/ld+json"]')`, assert its parsed JSON `@type === 'Person'` and `email === 'axel.waserman@gmail.com'`. (This is the authoritative parse-correctness check for plan 06-02 — uses a real DOM parser, not regex.)

    6. **Visible plain-text email (SC-2)**: assert the contact section contains the literal text `Or reach me directly at axel.waserman@gmail.com` and that this text node is NOT inside an `<a>` element.

    7. **Hero CTA cluster (SC-3)**: assert no `mailto:` anchors exist in the rendered DOM (`page.locator('a[href^="mailto:"]')` count === 0). Assert Hero contains an `<a href="#contact">Get in touch</a>` and the GitHub / LinkedIn / Download CV anchors.

    8. **Visual screenshots**: in a separate test or `test.beforeEach` hook, set viewport to each of 320×800, 768×1024, 1440×900 and capture `e2e/screenshots/phase-06-contact-{width}.png` (full page). Repeat for `/thanks` (with mocked Formspree to avoid real submit) → `e2e/screenshots/phase-06-thanks-{width}.png`. Total = 6 breakpoint screenshots.

    9. **Reduced-motion smoke test (SC-4)**: launch a context with `reducedMotion: 'reduce'`, navigate to `/`, screenshot to `e2e/screenshots/phase-06-contact-reduced-motion.png`, confirm the page renders without exceptions. Note: SC-4's `prefers-reduced-motion` requirement is satisfied by the absence of JS animation in the form (no `framer-motion` / `gsap` imports in `ContactForm.tsx` — confirmed by acceptance grep below). FadeUp on the parent section is unchanged from prior phases and already respects reduced-motion. Total screenshots written: 7.

    Tests must NOT contain `Math.random` or hardcoded timestamps that could make snapshots flaky. Use `process.env.CI` only if needed.
  </action>
  <verify>
    <automated>npx playwright test e2e/uat-phase-06.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/uat-phase-06.spec.ts` exists and `npx playwright test e2e/uat-phase-06.spec.ts` exits 0
    - All 9 cases above are present (`grep -cE 'test\(' e2e/uat-phase-06.spec.ts` >= 7; some may be combined into describe blocks)
    - Screenshots exist at all SEVEN paths: `e2e/screenshots/phase-06-contact-320.png`, `phase-06-contact-768.png`, `phase-06-contact-1440.png`, `phase-06-thanks-320.png`, `phase-06-thanks-768.png`, `phase-06-thanks-1440.png`, and `phase-06-contact-reduced-motion.png`
    - Spec uses `page.route('**/formspree.io/**', ...)` so no real network call goes to Formspree during automated tests
    - Reduced-motion is a smoke test: SC-4's reduced-motion requirement is satisfied by absence of JS animation in the form. Verify with `grep -lE 'framer-motion|gsap' src/components/contact/ContactForm.tsx` returns no output (no match = file does not import either library).
  </acceptance_criteria>
  <done>Playwright spec passes; seven screenshots written; no real Formspree traffic; ContactForm.tsx imports no JS animation library.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Provision Formspree form and paste real endpoint into src/data/site.ts</name>
  <what-built>
    - Form, /thanks page, JSON-LD, and Hero CTA swap are all implemented and Playwright-verified against a mocked Formspree endpoint.
    - `src/data/site.ts` still contains the `PLACEHOLDER_FORM_ID` from plan 06-01.
  </what-built>
  <how-to-verify>
    Axel-only step (Claude has no Formspree credentials):

    1. Sign in to https://formspree.io and create a new form named e.g. "axelwaserman.github.io contact form".
    2. Set the receiving inbox to **axel.waserman@gmail.com**.
    3. Confirm Formspree's email verification flow if prompted.
    4. Copy the form's endpoint URL — it has the shape `https://formspree.io/f/<id>` where `<id>` is an alphanumeric token.
    5. Open `src/data/site.ts` and replace the `PLACEHOLDER_FORM_ID` value in the `FORMSPREE_ENDPOINT` constant with the real ID. Save.
    6. Run `npm run build` locally — it must still pass.
    7. From the deployed site (or `npm run dev`) submit a real test message via the form. Confirm:
       - The browser navigates to `/thanks`.
       - Within ~1 minute, an email arrives at `axel.waserman@gmail.com` containing the test message body.
    8. If a real submission fails, paste the failing response into the conversation; otherwise type "approved — Formspree live".
  </how-to-verify>
  <resume-signal>Type `approved — Formspree live` once the test email lands, or describe the failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual + functional review (per memory feedback_visual_review_static_export.md)</name>
  <what-built>
    - Phase 6 implementation: Contact form, /thanks, JSON-LD, Hero CTA, plain-text email caption.
    - Seven Playwright screenshots in `e2e/screenshots/phase-06-*.png` (six breakpoint + one reduced-motion).
    - Real Formspree endpoint live and verified in task 2.
  </what-built>
  <how-to-verify>
    1. Open the seven screenshots and confirm each looks intentional (anti-template policy: deliberate input borders, focus styling, label hierarchy, restrained motion). Both contact section and `/thanks` should match the parchment palette and Phase 5 D-02 centered editorial baseline. Reduced-motion screenshot should look identical to the 1440 contact screenshot (no animation in the form means no visual difference).
    2. Run `npm run dev` and tab through the form — confirm focus rings are visible and use `--color-accent`, error messages appear after blur on each field, the submit button shows the disabled "Sending…" state when in flight, and the email-fallback block appears on a real (or briefly faked) error.
    3. View page source on the deployed site and confirm:
       - `<script type="application/ld+json">` Person block is present with the email.
       - The literal `Or reach me directly at axel.waserman@gmail.com.` is in the contact section as plain text.
       - No `mailto:` anchors anywhere in the rendered HTML.
    4. Confirm `/thanks` has `<meta name="robots" content="noindex...">`.
    5. Open the PR (per memory `feedback_phase_pr_workflow.md` — push branch + open PR after subagent validates; human merges manually).
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- All four Success Criteria explicitly checked:
  - SC-1: Real Formspree submission → email delivered (task 2)
  - SC-2: JSON-LD + plain-text email both visible in rendered HTML (Playwright + manual source-view)
  - SC-3: Contact + Hero anchor changes verified (Playwright)
  - SC-4: Validation, success/error states, reduced-motion all exercised (Playwright)
- Visual review per memory `feedback_visual_review_static_export.md` (Playwright + Axel eyeball)
- PR opened per memory `feedback_phase_pr_workflow.md` for manual merge
</verification>

<success_criteria>
- All Phase 6 SC-1..SC-4 verified end-to-end
- Real Formspree ID committed in `src/data/site.ts`
- Phase 6 ready for PR + human merge
</success_criteria>

<output>
Create `.planning/phases/06-get-in-touch-form/06-06-SUMMARY.md` when done.
</output>
</content>
</invoke>