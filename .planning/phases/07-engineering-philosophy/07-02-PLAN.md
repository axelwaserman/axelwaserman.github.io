---
phase: 7-engineering-philosophy
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/mandala.ts
  - src/app/icon.tsx
  - src/app/apple-icon.tsx
  - scripts/generate-og-image.mjs
  - package.json
  - .github/workflows/deploy.yml
  - .gitignore
autonomous: false
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-03

must_haves:
  truths:
    - "Visiting any page in a browser tab shows a deterministic mandala favicon (no 'AW' letters anywhere in the rendered icon)"
    - "Mandala renders crisply at both 16x16 (tab favicon) and 32x32 (icon route) — no fine detail loss"
    - "Sharing https://axelwaserman.github.io on Twitter/X, LinkedIn, Discord, or Slack shows a 1200x630 preview that visually matches the live homepage hero (NOT the favicon, NOT a placeholder)"
    - "out/og-image.png is regenerated on every deploy by GitHub Actions — the artifact reflects the current state of the page, not a stale committed image"
    - "Zero new runtime dependencies; only build-time use of Playwright (already present as a devDependency)"
    - "Mandala generation is deterministic given a commit SHA — same SHA always produces the same mandala (verified by running the generator twice with the same seed)"
    - "src/app/icon.tsx remains static-export compatible (preserves `export const dynamic = 'force-static'`)"
    - "Local dev: `npm run build && npm run og:generate` produces public/og-image.png and out/og-image.png with exact 1200x630 dimensions"
  artifacts:
    - path: "src/lib/mandala.ts"
      provides: "Pure mandala generator: xorshift32 PRNG + seedFromCommit + generateMandala(seed) returning ReactElement[] for icon JSX"
      contains: "export function generateMandala"
      forbids: "'use client'"
      min_lines: 60
    - path: "src/app/icon.tsx"
      provides: "32x32 favicon route — seeded mandala (no AW monogram)"
      contains: "generateMandala"
      forbids: "AW"
    - path: "src/app/apple-icon.tsx"
      provides: "180x180 apple-touch icon route — same generator at higher res"
      contains: "generateMandala"
    - path: "scripts/generate-og-image.mjs"
      provides: "Build-time Node ESM script: serves out/, launches Playwright Chromium, screenshots homepage at 1200x630, writes public/og-image.png and out/og-image.png"
      contains: "chromium"
    - path: ".github/workflows/deploy.yml"
      provides: "CI step to install Playwright Chromium and run og:generate AFTER build, BEFORE upload-pages-artifact"
      contains: "og:generate"
    - path: ".gitignore"
      provides: "Excludes generated OG image artifacts from git"
      contains: "og-image.png"
  key_links:
    - from: "src/app/icon.tsx"
      to: "src/lib/mandala.ts"
      via: "import generateMandala + seedFromCommit"
      pattern: "import.*generateMandala.*from '@/lib/mandala'"
    - from: "src/app/apple-icon.tsx"
      to: "src/lib/mandala.ts"
      via: "import generateMandala + seedFromCommit"
      pattern: "import.*generateMandala.*from '@/lib/mandala'"
    - from: "scripts/generate-og-image.mjs"
      to: "out/og-image.png"
      via: "Playwright screenshot write"
      pattern: "out/og-image.png"
    - from: "scripts/generate-og-image.mjs"
      to: "public/og-image.png"
      via: "Playwright screenshot write (local dev preview)"
      pattern: "public/og-image.png"
    - from: ".github/workflows/deploy.yml"
      to: "npm run og:generate"
      via: "CI step ordered build → playwright install → og:generate → upload"
      pattern: "og:generate"
    - from: "src/app/layout.tsx"
      to: "/og-image.png"
      via: "openGraph.images[0].url with width 1200, height 630 (verify only — already correct)"
      pattern: "/og-image.png"
---

## Phase Goal

**As a** recruiter or collaborator who lands on the site or sees it shared in a chat, **I want to** see a unique, intentional favicon AND a real preview of the homepage in social-share embeds, **so that** the brand feels deliberate and the link preview reflects the actual product (not a missing image).

<objective>
Replace the AW-monogram favicon with a deterministic, per-build-randomized mandala SVG (commit-SHA-seeded) generated at build time via the existing `src/app/icon.tsx` route, and replace the missing `/og-image.png` reference with a real Playwright-rendered miniature of the production landing page captured during CI/CD by a post-build step that boots a local server against the static export and writes `public/og-image.png` (1200x630) before the GitHub Pages artifact is uploaded.

Closes BRAND-01 (favicon mandala), BRAND-02 (OG image is real landing-page screenshot), BRAND-03 (OG image referenced from layout.tsx metadata + present in static export).

> **REQUIREMENT NOTE FOR EXECUTOR + HUMAN VERIFIER:** BRAND-01, BRAND-02, BRAND-03 are introduced by this plan. They are **not yet listed in `.planning/REQUIREMENTS.md`**. Before phase verification closes Phase 7, the human must append entries for these three IDs to REQUIREMENTS.md (one-line description per ID). The executor should surface this in the SUMMARY's "Open follow-ups" section. Do NOT block this plan's execution on the requirements file edit — the plan ships its own grep gates against artifact behavior.

Output:
- Pure mandala generator module (xorshift32 PRNG, commit-SHA seed)
- Replaced 32x32 favicon route + new 180x180 apple-touch icon route
- Build-time OG screenshot script reusing existing Playwright devDep
- CI workflow step wired between build and upload
- .gitignore entries for generated artifacts
- Human verification of mandala readability at 16/32px AND social-share preview at opengraph.xyz / X / LinkedIn
</objective>

<execution_context>
@/Users/axel/code/website/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axel/code/website/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@CLAUDE.md
@src/app/icon.tsx
@src/app/layout.tsx
@src/styles/tokens.css
@.github/workflows/deploy.yml
@package.json
@next.config.ts

<interfaces>
<!-- Tokens available in src/styles/tokens.css that the mandala MUST reuse. -->
<!-- Satori (the renderer behind next/og ImageResponse) does NOT support oklch() — -->
<!-- the existing src/app/icon.tsx documents oklch source-of-truth values and uses -->
<!-- precomputed sRGB equivalents in inline styles. PRESERVE this convention. -->

Color tokens (with sRGB equivalents already used in src/app/icon.tsx):
  --color-surface  oklch(97% 0.01 75)  →  rgb(249, 244, 238)
  --color-text     oklch(18% 0.01 75)  →  rgb(20, 17, 13)
  --color-accent   oklch(62% 0.19 55)  →  approx rgb(214, 99, 47) — compute equivalent during impl
  --color-muted    oklch(55% 0.03 75)  →  approx rgb(140, 128, 113) — compute equivalent during impl

Existing icon route exports to PRESERVE:
  export const size = { width: 32, height: 32 } as const
  export const contentType = 'image/png' as const
  export const dynamic = 'force-static'  // REQUIRED for output: 'export'

apple-icon.tsx convention (per Next.js 16 App Router):
  same exports as icon.tsx, with size = { width: 180, height: 180 }
  contentType remains 'image/png'

Mandala generator contract (new src/lib/mandala.ts):
  export function seedFromCommit(): number
    // Reads process.env.GITHUB_SHA (CI) or process.env.VERCEL_GIT_COMMIT_SHA;
    // falls back to a constant string 'local-dev' for offline/dev builds.
    // Hashes the input string via djb2 to a 32-bit unsigned integer.

  export function xorshift32(seed: number): () => number
    // Returns a stateful PRNG closure producing floats in [0, 1).

  export interface MandalaParams {
    ringCount: number          // 3..5
    petalCounts: number[]      // length === ringCount, each 6..12
    rotationOffsets: number[]  // length === ringCount, each 0..360 (deg)
    bgColor: string            // sRGB string
    strokeColor: string        // sRGB string
    accentColor: string        // sRGB string
  }

  export function generateMandala(seed: number, canvasSize: number): {
    children: React.ReactElement
    params: MandalaParams
  }
  // children = a single <svg viewBox="0 0 {canvasSize} {canvasSize}"> element
  // containing concentric rings of petals (e.g. <circle> / <path> primitives).
  // Petal stroke widths and radii MUST scale with canvasSize so output is legible
  // at both 32px and 180px renders.

OG image script contract (scripts/generate-og-image.mjs):
  - ESM module (.mjs).
  - Imports `chromium` from 'playwright' (already a devDependency via @playwright/test).
  - Boots a static file server against ./out (use Node http.createServer + fs OR
    spawn `npx serve out -l 4173 --no-clipboard --single` — pick whichever works
    without prompting; document choice).
  - Polls the server until it responds 200 to GET /.
  - Launches headless chromium at viewport 1280x720, navigates to http://127.0.0.1:4173/,
    waits for networkidle, then takes a screenshot with explicit clip { x:0, y:0,
    width:1280, height:720 } — then resizes to 1200x630 either by re-screenshotting
    at deviceScaleFactor=1200/1280 OR by using the deprecated 'fullPage:false' +
    page.setViewportSize({ width: 1200, height: 630 }) approach.
    PRACTICAL APPROACH: set viewport to exactly 1200x630 from the start, take a
    fullPage:false screenshot — yields a native 1200x630 PNG with no resize step.
  - Writes the PNG to BOTH ./public/og-image.png AND ./out/og-image.png
    (mkdirSync recursive on both).
  - Closes browser, terminates server, exits 0 on success / non-zero on any error.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Mandala favicon generator + apple-touch icon</name>
  <files>
    - src/lib/mandala.ts
    - src/app/icon.tsx
    - src/app/apple-icon.tsx
  </files>
  <read_first>
    - src/app/icon.tsx (current AW-monogram impl — preserve exports, font loading, oklch comment convention; replace ONLY the inner JSX)
    - src/styles/tokens.css (token names + oklch values + sRGB equivalents in comments)
    - CLAUDE.md (Next.js 16 App Router + static export constraints)
    - .planning/REQUIREMENTS.md (note: BRAND-01 to be added by human after phase verification)
  </read_first>
  <behavior>
    - `src/lib/mandala.ts` exports `seedFromCommit`, `xorshift32`, and `generateMandala`
    - `seedFromCommit()` returns the same 32-bit integer for the same input string (deterministic)
    - `xorshift32(seed)` returns a function; calling it twice on the same seed yields the same first 5 values (verified by automated grep / unit-style assertion in verify step)
    - `generateMandala(seed, size)` returns `{ children, params }` where `children` is a single React element (an `<svg>` root) and `params.ringCount` ∈ [3,5]
    - `src/app/icon.tsx` does NOT contain the literal string "AW" (the monogram is gone)
    - `src/app/icon.tsx` imports from `@/lib/mandala`
    - `src/app/icon.tsx` preserves `export const size`, `export const contentType`, and `export const dynamic = 'force-static'`
    - `src/app/apple-icon.tsx` exists, exports size 180x180, dynamic = 'force-static', and imports from `@/lib/mandala`
    - `npx next build` exits 0 with both icon routes present in the static export
  </behavior>
  <action>
    Create the mandala generator and rewrite the icon routes.

    **1. src/lib/mandala.ts (NEW, ~80–120 lines, pure module, no React hooks, no 'use client'):**

    - Implement `djb2(str: string): number` (32-bit unsigned). Document why djb2 (deterministic, no crypto dep, sufficient for visual seeding).
    - Implement `seedFromCommit(): number`:
      - Read `process.env.GITHUB_SHA ?? process.env.VERCEL_GIT_COMMIT_SHA ?? 'local-dev'`.
      - Return `djb2(input)`.
    - Implement `xorshift32(seed: number): () => number`:
      - Standard xorshift32 (`x ^= x << 13; x ^= x >>> 17; x ^= x << 5;`).
      - Return a closure that mutates internal state and returns `(state >>> 0) / 0xffffffff` ∈ [0, 1).
      - If incoming seed is 0, replace with 1 (xorshift32 stalls on 0).
    - Define `MandalaParams` interface (per `<interfaces>` block above).
    - Implement `generateMandala(seed: number, canvasSize: number)`:
      - Instantiate PRNG via `xorshift32(seed)`.
      - Choose `ringCount = 3 + Math.floor(prng() * 3)` (3..5).
      - For each ring, choose `petalCount = 6 + Math.floor(prng() * 7)` (6..12) and `rotationOffset = prng() * 360`.
      - Compute geometry:
        - Center: `cx = cy = canvasSize / 2`.
        - For ring `i` (0-indexed), `radius = ((i + 1) / (ringCount + 1)) * (canvasSize * 0.45)`.
        - Petal: a small `<circle>` at `(cx + radius * cos(theta), cy + radius * sin(theta))` where `theta = (k / petalCount) * 2π + rotationOffset`.
        - Petal radius: `Math.max(1, canvasSize * 0.04 * (1 - i / ringCount))` — outer rings have smaller petals so the design reads at 16px.
      - Stroke widths: `Math.max(0.75, canvasSize / 64)`. AT 32px this gives 0.75–1px lines; at 180px this gives ~2.8px lines.
      - Colors: use the parchment background `rgb(249, 244, 238)` (existing SURFACE_SRGB), accent stroke `rgb(214, 99, 47)` (D-20 --color-accent equivalent — verify exact sRGB equivalent of `oklch(62% 0.19 55)` during impl; if your computed value differs slightly, document the chosen sRGB value alongside the oklch source-of-truth comment), text strokes `rgb(20, 17, 13)`.
      - Return `{ children: <svg ...>{rings}</svg>, params }`.
    - All exports MUST have explicit return types per typescript/coding-style.md (avoid `any`).
    - Add file-level comment: `// BRAND-01: deterministic mandala favicon generator. Seed from commit SHA → xorshift32 PRNG → concentric rings of petals.`

    **2. src/app/icon.tsx (MODIFY in place):**

    - Preserve the file's existing exports: `size`, `contentType`, `dynamic`, plus the font-loading block and oklch/sRGB token comments (D-20 traceability).
    - Add imports: `import { generateMandala, seedFromCommit } from '@/lib/mandala'`.
    - Inside `Icon()`, after the `fontData` read:
      - `const seed = seedFromCommit()`.
      - `const { children: mandala } = generateMandala(seed, 32)`.
    - Replace the `<div>...AW...</div>` JSX with a `<div>` of the same surface background that contains `{mandala}` (the `<svg>`) centered.
      - The wrapping `<div>` keeps `width: '100%'`, `height: '100%'`, `display: 'flex'`, `alignItems: 'center'`, `justifyContent: 'center'`, `background: SURFACE_SRGB`. Drop the `fontSize`, `letterSpacing`, `fontFamily`, `color` props (no text now).
    - The `fonts:` array in the `ImageResponse` options can REMAIN (keeping it future-proofs against fallback text) OR be removed. EXECUTOR DECISION: **remove** the `fonts` array AND the `readFile` call AND the `fontPath`/`fontData` variables — there's no text to render anymore, so loading the TTF is dead weight. Keep imports only for what's still used.
    - Update the file-level comment block: replace the "D-18: AW monogram" line with "BRAND-01: seeded mandala from commit SHA".

    **3. src/app/apple-icon.tsx (NEW, ~40 lines):**

    - Mirror the post-edit `src/app/icon.tsx` structure but with:
      - `export const size = { width: 180, height: 180 } as const`
      - `export const contentType = 'image/png' as const`
      - `export const dynamic = 'force-static'`
    - Inside the default-exported `AppleIcon()`:
      - `const seed = seedFromCommit()` (same seed → consistent mandala family between favicon and apple-touch icon).
      - `const { children: mandala } = generateMandala(seed, 180)`.
      - Return an `ImageResponse` with the same parchment-background `<div>` containing `{mandala}`.
    - Add file-level comment: `// BRAND-01: 180x180 apple-touch icon — same seeded mandala generator as src/app/icon.tsx, scaled up.`

    Forbidden in this task: `'use client'`, hardcoded color hex without an oklch source-of-truth comment, introducing new design tokens, animating any property (icons are static images).
  </action>
  <verify>
    <automated>
      cd /Users/axel/code/website && \
      test -f src/lib/mandala.ts && \
      test -f src/app/apple-icon.tsx && \
      grep -q "export function generateMandala" src/lib/mandala.ts && \
      grep -q "export function seedFromCommit" src/lib/mandala.ts && \
      grep -q "export function xorshift32" src/lib/mandala.ts && \
      grep -q "from '@/lib/mandala'" src/app/icon.tsx && \
      grep -q "from '@/lib/mandala'" src/app/apple-icon.tsx && \
      ! grep -q '>AW<' src/app/icon.tsx && \
      ! grep -q "fontSize: 22" src/app/icon.tsx && \
      grep -q "force-static" src/app/icon.tsx && \
      grep -q "force-static" src/app/apple-icon.tsx && \
      grep -q "width: 180, height: 180" src/app/apple-icon.tsx && \
      ! grep -q "'use client'" src/lib/mandala.ts && \
      ! grep -q "'use client'" src/app/icon.tsx && \
      ! grep -q "'use client'" src/app/apple-icon.tsx && \
      npx tsc --noEmit && \
      node -e "
        const m = require('./src/lib/mandala.ts');
        // If the project uses ESM-only TS, fall back to a tsx require — but tsc passing above
        // is sufficient evidence that the module is well-typed.
      " 2>/dev/null || true && \
      npx next build && \
      test -d out
    </automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/mandala.ts` exists; exports `seedFromCommit`, `xorshift32`, `generateMandala` with explicit return types; no `'use client'`; no `any`.
    - File `src/app/icon.tsx` no longer contains the string `>AW<` or `fontSize: 22`; imports from `@/lib/mandala`; preserves `size`, `contentType`, `dynamic = 'force-static'` exports; the `fonts` array, `readFile` call, and TTF path are removed (no text to render).
    - File `src/app/apple-icon.tsx` exists; exports `size = { width: 180, height: 180 }`, `contentType = 'image/png'`, `dynamic = 'force-static'`; imports from `@/lib/mandala`.
    - `npx tsc --noEmit` exits 0.
    - `npx next build` exits 0; `out/` directory is produced.
    - `seedFromCommit()` returns the same value for the same input on two consecutive runs (deterministic — verified manually OR via a tiny inline node -e check during verify).
    - File-level comments tag BRAND-01 in both `src/lib/mandala.ts` and `src/app/icon.tsx`.
  </acceptance_criteria>
  <done>
    Mandala generator committed; icon and apple-icon routes render the seeded mandala at build time; type-check passes; static export builds successfully; favicon visible in dev server browser tab (manual sanity check before Task 4 checkpoint).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: OG screenshot script + package.json wiring + .gitignore</name>
  <files>
    - scripts/generate-og-image.mjs
    - package.json
    - .gitignore
  </files>
  <read_first>
    - package.json (verify @playwright/test is in devDependencies; identify existing scripts pattern; identify package manager — npm, per CLAUDE.md `npm ci` lock sync feedback)
    - .gitignore (current entries; add new lines without disturbing ordering)
    - src/app/layout.tsx (confirm openGraph.images[0] points to `/og-image.png` with width 1200, height 630 — expected to already be correct)
    - next.config.ts (confirm `output: 'export'`; the script depends on `out/` existing after `next build`)
  </read_first>
  <behavior>
    - File `scripts/generate-og-image.mjs` exists; is a Node ESM module; imports `chromium` from 'playwright'
    - The script boots a local static server against `./out`, navigates with Playwright at 1200x630 viewport, screenshots, writes `./public/og-image.png` AND `./out/og-image.png`, then exits 0
    - `package.json` has a new script: `"og:generate": "node scripts/generate-og-image.mjs"`
    - `.gitignore` excludes both `public/og-image.png` and `out/og-image.png`
    - Running locally `npm run build && npm run og:generate` exits 0
    - After running, `out/og-image.png` exists, dimensions are exactly 1200x630, file size > 10KB and < 500KB
    - The script kills the local server before exiting (no orphan processes)
  </behavior>
  <action>
    Create the OG generation script and wire it into the package.

    **1. scripts/generate-og-image.mjs (NEW, ~80–120 lines):**

    Implement as Node ESM:

    - Top of file: `// BRAND-02 + BRAND-03: build-time OG screenshot generator. Boots out/ via a tiny http server, screenshots the homepage at 1200x630 with Playwright Chromium, writes public/og-image.png and out/og-image.png. Runs in CI between `next build` and `actions/upload-pages-artifact`.`
    - Imports: `http`, `fs/promises`, `fs`, `path`, `url` from node:* and `chromium` from `playwright`.
    - Define constants:
      - `const OUT_DIR = path.resolve(process.cwd(), 'out')`
      - `const PUBLIC_DIR = path.resolve(process.cwd(), 'public')`
      - `const PORT = 4173`
      - `const VIEWPORT = { width: 1200, height: 630 }`
    - Pre-flight: assert `out/index.html` exists; if not, throw with message instructing to run `npm run build` first.
    - Static server: implement a minimal `http.createServer((req, res) => ...)` that:
      - Resolves `req.url` against `OUT_DIR`.
      - If URL is `/` or has no extension, append `index.html`.
      - Reads the file via `fs.promises.readFile`; sets a Content-Type header by extension (`.html` → text/html, `.css` → text/css, `.js` → application/javascript, `.png` → image/png, `.svg` → image/svg+xml, `.ico` → image/x-icon, default → application/octet-stream).
      - On ENOENT, respond 404.
      - Listens on `127.0.0.1:PORT`.
    - Wait for server to listen (await a Promise wrapping `server.listen`).
    - Launch `chromium.launch({ headless: true })`. Wrap the rest in try/finally to guarantee `browser.close()` and `server.close()`.
    - Open a context with `viewport: VIEWPORT, deviceScaleFactor: 1`.
    - Open page; `await page.goto('http://127.0.0.1:4173/', { waitUntil: 'networkidle', timeout: 30000 })`.
    - Take screenshot with `await page.screenshot({ type: 'png', fullPage: false, clip: { x: 0, y: 0, width: 1200, height: 630 } })` — buffer return.
    - `await fs.promises.mkdir(PUBLIC_DIR, { recursive: true })` and `await fs.promises.mkdir(OUT_DIR, { recursive: true })` (out should already exist; mkdir is idempotent).
    - Write the buffer to BOTH `path.join(PUBLIC_DIR, 'og-image.png')` AND `path.join(OUT_DIR, 'og-image.png')`.
    - Sanity-check the written file: `const stat = await fs.promises.stat(outPath); if (stat.size < 10_000 || stat.size > 500_000) throw new Error(...)`.
    - Console-log the success line: `OG image written: 1200x630, ${stat.size} bytes`.
    - In `finally`: close browser, close server.
    - Surround top-level `await` with an `async function main(){...}` invoked at the bottom: `main().catch(err => { console.error(err); process.exit(1); })`.

    **2. package.json (MODIFY):**

    - Add `"og:generate": "node scripts/generate-og-image.mjs"` to the `scripts` object, alphabetically grouped near other build-related scripts.
    - Do NOT add new dependencies. Verify `@playwright/test` (or `playwright`) already provides the `chromium` import. If only `@playwright/test` is present (which re-exports `chromium`), the import line should be `import { chromium } from '@playwright/test'` instead of `from 'playwright'` — pick whichever resolves with the existing devDeps. Document the chosen import path in the script's top comment.

    **3. .gitignore (MODIFY):**

    - Add the following two lines under a new comment block (or append to existing build-artifacts section):
      ```
      # Generated OG image (rebuilt by scripts/generate-og-image.mjs in CI / locally)
      public/og-image.png
      out/og-image.png
      ```
    - Confirm via `git status` that an existing `public/og-image.png` (if any local one was created during testing) is no longer tracked.

    Forbidden in this task: adding new runtime or dev dependencies, hardcoding the production URL (script must point at the local server only), committing a generated `og-image.png` file.
  </action>
  <verify>
    <automated>
      cd /Users/axel/code/website && \
      test -f scripts/generate-og-image.mjs && \
      grep -q "chromium" scripts/generate-og-image.mjs && \
      grep -q "og-image.png" scripts/generate-og-image.mjs && \
      grep -q "1200" scripts/generate-og-image.mjs && \
      grep -q "630" scripts/generate-og-image.mjs && \
      node -e "const p = require('./package.json'); if (!p.scripts['og:generate']) { console.error('og:generate script missing'); process.exit(1); } console.log('og:generate ok');" && \
      grep -v '^#' .gitignore | grep -q "public/og-image.png" && \
      grep -v '^#' .gitignore | grep -q "out/og-image.png" && \
      npm run build && \
      npx playwright install chromium && \
      npm run og:generate && \
      test -f public/og-image.png && \
      test -f out/og-image.png && \
      node -e "
        const fs = require('fs');
        const buf = fs.readFileSync('out/og-image.png');
        // PNG IHDR: width at bytes 16..19, height at bytes 20..23 (big-endian)
        const w = buf.readUInt32BE(16);
        const h = buf.readUInt32BE(20);
        if (w !== 1200 || h !== 630) { console.error('dims wrong: '+w+'x'+h); process.exit(1); }
        if (buf.length < 10000 || buf.length > 500000) { console.error('size out of bounds: '+buf.length); process.exit(1); }
        console.log('og dims ok: '+w+'x'+h+' '+buf.length+' bytes');
      "
    </automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/generate-og-image.mjs` exists; imports `chromium` from `playwright` OR `@playwright/test` (whichever resolves with existing devDeps; documented in the file's top comment).
    - `package.json` `scripts` object has key `"og:generate"` with value `"node scripts/generate-og-image.mjs"`.
    - `.gitignore` (excluding comment lines) contains both `public/og-image.png` and `out/og-image.png`.
    - Running `npm run build && npx playwright install chromium && npm run og:generate` exits 0.
    - `out/og-image.png` exists with PNG IHDR width=1200 height=630, file size ∈ [10_000, 500_000] bytes (verified by reading bytes 16..23 of the file as big-endian uint32).
    - `public/og-image.png` exists with the same content (file size matches `out/og-image.png`).
    - Script terminates cleanly (no orphan node/server process — verified by `ps -ax | grep generate-og` returning only the grep itself after script exits).
    - No new dependencies were added to `package.json` (`devDependencies` and `dependencies` keys identical to pre-task aside from the new script entry).
  </acceptance_criteria>
  <done>
    OG generation script committed; package.json wired; .gitignore protects against accidentally committing the generated PNGs; local end-to-end run produces a valid 1200x630 PNG in both `public/` and `out/`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire OG generation into deploy workflow + verify layout metadata</name>
  <files>
    - .github/workflows/deploy.yml
  </files>
  <read_first>
    - .github/workflows/deploy.yml (current build job — steps ordered: checkout → setup-node → configure-pages → npm ci → cache → build → upload)
    - src/app/layout.tsx (verify-only: openGraph.images[0] already has url '/og-image.png', width 1200, height 630)
    - scripts/generate-og-image.mjs (the script being invoked from CI — must already exist from Task 2)
    - package.json (verify the og:generate script is present)
  </read_first>
  <behavior>
    - `.github/workflows/deploy.yml` contains a step that runs `npx playwright install --with-deps chromium` (installs the Chromium browser binary that Playwright drives)
    - `.github/workflows/deploy.yml` contains a step that runs `npm run og:generate`
    - Step ordering inside the `build` job: `Build Next.js static export` → `Install Playwright Chromium` → `Generate OG image` → `Upload Pages artifact`
    - The `actions/upload-pages-artifact` step still uploads `./out` (so the regenerated `out/og-image.png` is included in the artifact, satisfying BRAND-03)
    - `src/app/layout.tsx` openGraph.images[0] entry still has `url: '/og-image.png'`, `width: 1200`, `height: 630` (verify-only — no edit expected; if the file diverges, fix it and document)
    - GitHub Actions deploy on the resulting PR succeeds (manual / out-of-band — recorded in SUMMARY)
  </behavior>
  <action>
    Insert two new steps in `.github/workflows/deploy.yml` between the existing `Build Next.js static export` step and the `Upload Pages artifact` step.

    **Insert after the `Build Next.js static export` step and BEFORE the `Upload Pages artifact` step:**

    ```yaml
          - name: Install Playwright Chromium
            run: npx playwright install --with-deps chromium

          - name: Generate OG image
            run: npm run og:generate
    ```

    Notes for the executor:
    - Use `--with-deps` to ensure the runner has the system libs Chromium needs on ubuntu-latest.
    - Do NOT add a separate `cache` step for the Playwright browser binary in this plan — keep the change minimal. (Cache optimization is a follow-up if CI time becomes an issue.)
    - Do NOT change the `Upload Pages artifact` step's `path: ./out` — the regenerated `out/og-image.png` is implicitly picked up.
    - Do NOT alter the `deploy` job, the `concurrency` block, the `permissions` block, or the `on:` triggers.

    **Verify-only edits to src/app/layout.tsx:** Read the file. Confirm:
    - `openGraph.images[0].url === '/og-image.png'`
    - `openGraph.images[0].width === 1200`
    - `openGraph.images[0].height === 630`
    - `openGraph.images[0].alt` is present and non-empty.

    If any of those four conditions is false, fix the entry in place to the values above and add `src/app/layout.tsx` to this task's files_modified in the SUMMARY (it is intentionally NOT in the plan-level files_modified because we expect the file is already correct — but executor should self-correct if needed).

    Tracking: this task closes BRAND-02 (CI step ensures the OG screenshot is regenerated on every deploy) and BRAND-03 (artifact upload includes `out/og-image.png`, so the deployed site has a real image at `/og-image.png` matching `layout.tsx` metadata).
  </action>
  <verify>
    <automated>
      cd /Users/axel/code/website && \
      grep -q "playwright install" .github/workflows/deploy.yml && \
      grep -q "og:generate" .github/workflows/deploy.yml && \
      node -e "
        const fs = require('fs');
        const yml = fs.readFileSync('.github/workflows/deploy.yml', 'utf8');
        const buildIdx = yml.indexOf('Build Next.js static export');
        const installIdx = yml.indexOf('Install Playwright Chromium');
        const ogIdx = yml.indexOf('Generate OG image');
        const uploadIdx = yml.indexOf('Upload Pages artifact');
        if (buildIdx < 0 || installIdx < 0 || ogIdx < 0 || uploadIdx < 0) { console.error('missing step'); process.exit(1); }
        if (!(buildIdx < installIdx && installIdx < ogIdx && ogIdx < uploadIdx)) {
          console.error('order wrong: build='+buildIdx+' install='+installIdx+' og='+ogIdx+' upload='+uploadIdx);
          process.exit(1);
        }
        console.log('CI step order ok');
      " && \
      node -e "
        const fs = require('fs');
        const src = fs.readFileSync('src/app/layout.tsx', 'utf8');
        if (!/url:\s*['\"]\/og-image\.png['\"]/.test(src)) { console.error('og url missing'); process.exit(1); }
        if (!/width:\s*1200/.test(src)) { console.error('width missing'); process.exit(1); }
        if (!/height:\s*630/.test(src)) { console.error('height missing'); process.exit(1); }
        console.log('layout og metadata ok');
      " && \
      npx tsc --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/deploy.yml` contains the strings `playwright install` AND `og:generate` AND `Install Playwright Chromium` AND `Generate OG image`.
    - In the YAML source, character positions of the four step name strings satisfy: `Build Next.js static export` < `Install Playwright Chromium` < `Generate OG image` < `Upload Pages artifact`.
    - `src/app/layout.tsx` has `openGraph.images[0]` with `url: '/og-image.png'`, `width: 1200`, `height: 630`, and a non-empty `alt`.
    - `npx tsc --noEmit` exits 0.
    - The PR's GitHub Actions deploy run succeeds end-to-end (recorded as a manual confirmation in SUMMARY — not gated by automated grep here).
  </acceptance_criteria>
  <done>
    Deploy workflow regenerates the OG image on every push; the upload artifact includes the regenerated PNG; layout metadata matches the produced image dimensions; type-check passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human visual review — favicon at 16/32px + OG share preview validation</name>
  <what-built>
    Deterministic mandala favicon (32x32 + 180x180 apple-touch icon) generated from commit SHA, replacing the AW monogram. Real Playwright-rendered 1200x630 OG image written to `out/og-image.png` by a build-time script wired into the GitHub Actions deploy workflow. Per memory `feedback_visual_review_static_export.md`: curl/grep is insufficient — always Playwright screenshot + Axel eyeball before merge.
  </what-built>
  <how-to-verify>
    Two independent verifications: (A) favicon legibility, (B) social-share preview correctness.

    **A) FAVICON LEGIBILITY (16px + 32px):**

    1. Build and serve the static export locally:
       ```
       cd /Users/axel/code/website
       npm run build
       npx serve out -l 3001
       ```

    2. Open http://localhost:3001 in Chrome.

    3. Capture a Playwright screenshot of the browser tab favicon at native 16x16 (Chrome tab favicon size):
       ```
       npx playwright cr --viewport-size=1200,800 --screenshot=tmp/tab-with-favicon.png http://localhost:3001/
       ```
       Then crop the favicon region from the resulting screenshot OR open Chrome → DevTools → Network tab → click `favicon.ico` (or `/icon`) → Preview pane shows the rendered icon.

    4. Open `out/icon` (Next.js emits the icon as a build asset) directly in a browser at full size to inspect the 32x32 render.

    5. **Visual checks:**
       - Mandala is visible (no "AW" monogram).
       - At 16x16, the design reads as a mandala (concentric symmetry) rather than blurry pixels — outer ring detail should still be discernible.
       - At 32x32, ring structure is clean, strokes are crisp, no anti-aliasing blowout.
       - Background is the parchment surface tone (not white, not gray).
       - Mandala has visual character — not so dense it becomes a blob, not so sparse it looks like three dots.

    6. **Determinism check:**
       ```
       GITHUB_SHA=test123 node -e "import('./src/lib/mandala.ts').then(m => console.log(JSON.stringify(m.generateMandala(m.seedFromCommit(), 32).params)))"
       GITHUB_SHA=test123 node -e "import('./src/lib/mandala.ts').then(m => console.log(JSON.stringify(m.generateMandala(m.seedFromCommit(), 32).params)))"
       ```
       Both runs MUST print identical JSON. If they diverge, the PRNG seed isn't deterministic — fix before approval.

       (If TS-from-node ESM import is awkward, run `npx tsx` or compile the module to JS for this check; the determinism property is what matters.)

    **B) OG SHARE PREVIEW (1200x630):**

    1. After deploying to GitHub Pages (the PR's deploy run completing successfully), open `https://axelwaserman.github.io/og-image.png` directly in a browser. Confirm:
       - Page loads (200 OK), not 404.
       - Image is 1200x630 (browser dev tools → image properties).
       - Visual content matches the live homepage (hero copy, Axel's name, the actual rendered design — NOT a placeholder, NOT a blank canvas, NOT the favicon).

    2. Validate the social embed on at least TWO of these tools:
       - https://www.opengraph.xyz/url/https%3A%2F%2Faxelwaserman.github.io
       - X (Twitter) Card Validator: https://cards-dev.twitter.com/validator (or post a test draft to a private account and inspect the preview)
       - LinkedIn Post Inspector: https://www.linkedin.com/post-inspector/
       - Discord: paste the URL into a private channel; observe the embed unfurl.

    3. **Visual checks for the OG embed:**
       - Title and description from `layout.tsx` metadata match the homepage (Axel Waserman — Senior Engineering Manager / portfolio + CV description).
       - The 1200x630 image visibly represents the homepage hero, not a tab/icon zoom.
       - No broken-image icon, no Cloudinary/placeholder, no 404.

    4. **Local OG check (in case CI hasn't deployed yet):**
       ```
       cd /Users/axel/code/website
       npm run build
       npx playwright install chromium  # one-time
       npm run og:generate
       open out/og-image.png  # macOS; Linux: xdg-open / view via npx serve
       ```
       Confirm the local PNG matches the homepage hero.

    5. **Cleanup:** stop any local `serve` processes; delete `tmp/` screenshots after review.

    **Approval rules:**
    - Approve only if BOTH (A) favicon mandala reads at 16px AND 32px AND (B) social-share embed shows the real 1200x630 homepage screenshot.
    - If the mandala for the current commit SHA looks visually awkward (e.g. petals overlap unpleasantly, density looks off), the executor can either (i) tune `MandalaParams` ranges in `src/lib/mandala.ts` (e.g. tighten ringCount to 3..4, petalCount to 8..10) and re-run, OR (ii) accept it — the design is meant to vary per build.
    - If the OG embed is wrong (still 404, wrong dimensions, blank), check the deploy workflow ran the new steps in order.
  </how-to-verify>
  <resume-signal>
    Type `approved` if BOTH the favicon (legible at 16px + 32px, no "AW") AND the OG share preview (real 1200x630 homepage screenshot, validates on at least two preview tools) pass. Otherwise reply with specific issues to fix (e.g. "mandala too dense at 16px, tighten ringCount range" or "OG image still 404 on production, deploy step ordering broken").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → static HTML/PNG | All brand assets (favicon SVG, OG screenshot) are authored at build time and shipped as static artifacts; no runtime input crosses any boundary in this phase |
| CI runner → published artifact | The GitHub Actions runner produces the OG screenshot under controlled conditions (Playwright Chromium, no third-party JS executed except what's already in the static export itself) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-04 | Tampering | Mandala generator (src/lib/mandala.ts) | accept | Pure deterministic function; output is a static SVG embedded in a build-time PNG; no user input flows in. Seed source (commit SHA) is controlled by repo write access, already protected by branch protection. |
| T-7-05 | Information Disclosure | OG screenshot content | mitigate | The screenshot captures the rendered homepage. Audit the homepage at human-verify step (Task 4) to confirm no draft / private content is visible above the fold at 1200x630 viewport. |
| T-7-06 | Tampering | scripts/generate-og-image.mjs | mitigate | Local server binds to 127.0.0.1 only (not 0.0.0.0); script only loads the local out/ directory; no external URLs; Content-Type whitelist prevents serving unexpected file types as HTML. |
| T-7-07 | Denial of Service | CI build time | accept | Playwright Chromium install + screenshot adds ~30–60s to each deploy. Acceptable for a daily cron + push trigger. If unacceptable later, add a `actions/cache` step keyed on the Playwright version. |
| T-7-SC | Tampering | npm/playwright install | mitigate | `@playwright/test` is already in `devDependencies` from prior phases; this plan adds NO new packages, so no Package Legitimacy Gate is triggered. The `npx playwright install --with-deps chromium` step downloads a browser binary from Microsoft's CDN — supply-chain risk delegated to existing Playwright trust. |

(No new package installs in this phase — Package Legitimacy Gate not triggered.)
</threat_model>

<verification>
Phase-level checks (run after Task 4 approval):

1. `npx tsc --noEmit` exits 0.
2. `npm run build` exits 0; produces `out/`.
3. `out/icon/route.js` (or whatever Next.js emits for the static-exported icon route) exists and is non-empty; the rendered icon does NOT contain the string "AW".
4. `out/apple-icon` route artifact exists and is non-empty.
5. `npm run og:generate` (after `npm run build`) exits 0 and produces `out/og-image.png` with PNG IHDR dimensions 1200x630 and file size ∈ [10_000, 500_000] bytes.
6. `public/og-image.png` exists with identical content to `out/og-image.png`.
7. `.gitignore` excludes both PNG paths; `git status` confirms neither is tracked.
8. `.github/workflows/deploy.yml` step order: build → install Playwright → og:generate → upload-pages-artifact.
9. `src/app/layout.tsx` openGraph.images[0] entry: url '/og-image.png', width 1200, height 630, non-empty alt.
10. Determinism: two consecutive runs of `seedFromCommit()` (same env) yield the same integer.
11. Visual review (Task 4) confirms favicon legibility at 16px + 32px AND OG share preview shows the real 1200x630 homepage on at least two social-preview validators.
</verification>

<success_criteria>
1. **BRAND-01 (favicon mandala):** AW monogram is gone; src/app/icon.tsx and src/app/apple-icon.tsx render a deterministic mandala from a commit-SHA-seeded xorshift32 PRNG; mandala is legible at 16px and 32px — verified by automated grep + Playwright screenshot in Task 4.
2. **BRAND-02 (OG image is real homepage):** `scripts/generate-og-image.mjs` produces a 1200x630 PNG that visually matches the live homepage hero, regenerated on every deploy by GitHub Actions — verified by PNG-IHDR check + human visual review in Task 4.
3. **BRAND-03 (OG image referenced + present in static export):** `src/app/layout.tsx` openGraph.images[0] points to `/og-image.png` at 1200x630, AND `out/og-image.png` is included in the GitHub Pages artifact upload — verified by deploy.yml step ordering + post-deploy 200-OK check at `https://axelwaserman.github.io/og-image.png`.
4. **Zero new dependencies:** package.json `dependencies` and `devDependencies` are unchanged aside from a new `og:generate` script entry — verified by package.json diff inspection at SUMMARY time.
5. **Static-export compatibility preserved:** Both icon routes keep `dynamic = 'force-static'`; `npm run build` exits 0 with no new warnings.

> **Open follow-up for human:** Append BRAND-01, BRAND-02, BRAND-03 entries to `.planning/REQUIREMENTS.md` before phase verification closes Phase 7. The executor surfaces this in the SUMMARY's "Open follow-ups" section.
</success_criteria>

<output>
Create `.planning/phases/07-engineering-philosophy/07-02-SUMMARY.md` when done. Include:
- Final mandala parameters used for the current commit SHA (ringCount, petalCounts, rotationOffsets) for SUMMARYs digest
- Confirmation that `'use client'` was NOT added to any new file
- Path + dimensions + size of generated `out/og-image.png` from the local build
- Screenshot paths or links from the human-verify checkpoint (favicon at 16/32px + OG embed validators)
- Confirmation of CI step ordering (build → playwright install → og:generate → upload)
- Open follow-up: human must append BRAND-01/02/03 to REQUIREMENTS.md
- Any deviations from the plan and their rationale (e.g. if `chromium` import resolved from `@playwright/test` rather than `playwright`)
</output>
